# FinAudit Agent 项目记忆

## 长期有效的基线事实

- 2026-08-12 起，活跃开发规格只包含 `Request/README.md`、`PRODUCT_REQUIREMENTS.md`、`TECHNICAL_SPEC.md` 和 `IMPLEMENTATION_PLAN.md`。旧九份文档字节级保存在 `Request/archive/legacy-v1/`，其历史身份仍由 `docs/baseline-manifest.md` 记录，但不参与解释当前行为。未批准 CR/DEP 的 registry、pin、签名或治理条件不能反向成为 P0 全局前置；只有可从活跃需求或机器事实独立复现的歧义才阻断受影响的最小切片。Provider/其他外网、真实数据、部署与 production 始终另行授权。

## 2026-08-20 Local MVP 0.1.1 收口

- 027 整批变更以 `61b2c17039d08205afe7cc2ac4114aeda05dbad4` 冻结；历史备份恢复修复后最终应用源码为 `03b6bbc41a1c330df3be538e4c46b74dfe383671`。两次提交都只在本地 `release/local-mvp-0.1.0`，未 push。0.1.1 是不可覆盖 0.1.0 的后继记录。
- 发票重复候选浏览器 Gate 真实完成登录→列表→源详情→候选→pair，保护 completion 接受两个 200 GET；入口 wrapper 仅在 Gate PASS 后自动计算 runner/shared wrapper/entry wrapper/frontend view 的 bytes 与 SHA-256并原子更新证据。最终完整离线门禁为 Backend `3127 passed / 147 skipped / 1 warning`、Ruff 520、mypy 283、Frontend 28 files/539 tests/151 modules PASS。
- 持久 `finaudit-local` 在本轮开始时已是 027，故不能声称本轮首次升级。先创建 backup ID `0016f52d-2ac4-4b8f-842c-42a74843f80a`；旧 backup `b94b77a1-1221-49bf-af87-927f97cb3300` 被独立确认是 024、17 表 22 行且 MinIO 哈希匹配，再在隔离项目用当前镜像迁移到 027。首次完整启动因当前 runtime marker 的管理员用户名与历史备份不同而在 admin-bootstrap 安全失败；恢复脚本改为从已验证数据库读取唯一活动 bootstrap 身份、校验 Compose 安全字符并重写隔离 marker/env，不重置密码。相同备份清洁重跑后行数/MinIO/派生重建/运维/冷启动 PASS，隔离资源清理为 0。
- 持久实例随后重建到镜像 revision `local-mvp-0.1.1`，Backend/Frontend image ID 匹配、head 027、权威行数 7→7、依赖失败 0、Frontend 200、Operations Readiness PASS。0.1.1 最小技术 UAT PASS；没有读取或复用持久管理员凭据，因此真实管理员浏览器复登 `NOT_RUN`，2026-08-18 的人工 UAT 签署不转移到新版本。

## 2026-08-21 P2 报告兼容性与可访问性技术闭环

- `local-p2-experience-compatibility-v1` 已关闭当前 local/test 的两项 P2 技术覆盖缺口。独立 Chrome Report Gate 在受保护完成前捕获 Blob `downloadWillBegin → downloadProgress=completed`，把落盘 XLSX 文件名、字节数和 SHA-256 对回数据库，console warning/error 为 0；最终输出 `REPORT_BROWSER_GATE=PASS`。固定宿主/Backend 镜像报告又由 Microsoft Excel 与 LibreOffice Calc `26.2.5.2` 实际打开/转换，三张表及关键表头一致，输出 `REPORT_ARTIFACT_COMPATIBILITY=PASS`。LibreOffice 旧挂起来自全新用户配置的 OpenCL 初始化；门禁只给一次性测试子进程设置 `SAL_DISABLE_OPENCL=1`、60 秒超时并按 profile 精确清理，不修改用户或系统全局设置。
- Edge `151.0.4129.93` 对 20 条 P0 业务路由完成单一 `main/h1`、AX Tree、skip-link、命名焦点、零页面溢出和有界焦点陷阱检查；五条轻量夹具的受控 401/409/503 只按精确 URL/状态单独计数，其他 console warning/error 为 0。Windows Narrator `10.0.22621.818` 在本轮唯一 Edge profile/Win32 前台窗口执行登录控件→报告→skip-link→主内容→XLSX 下载按钮焦点链；同 PID/同时间窗 ETW 为 `UiaFocus=13 / ProcessFocus=13 / InitiateSpeaking=19 / SapiTextSpeak=18 / GenerateAudio=19 / PlaybackCompleted=2`，原始 ETL/语音/文本随后删除。最终输出 `EDGE_NARRATOR_SCREEN_READER_GATE=PASS` 与 `ACCESSIBILITY_BROWSER_GATE=PASS`。
- 机器证据为 `tests/evaluation/local-p2-experience-compatibility-v1.json`，并由聚焦测试绑定 runner/wrapper/helper/frontend 的 bytes/SHA-256。最终完整离线门禁为 Backend `3165 passed / 147 skipped / 1 warning`、Ruff 528、mypy 283、Frontend 28 files/539 tests/151 modules PASS。该结论只关闭合成 local/test 技术覆盖，不构成残障用户人工验收、代表性业务 UAT、production、AC-014 或任何正式 AC；上述边界继续保持。

## 2026-08-20 P1 浏览器闭环与知识质量诊断

- `scripts/run-browser-gate.cjs` 以本机 Chrome CDP 和现有 Frontend `ws` 依赖自动闭合原先依赖人工控制的 Gate，不新增浏览器依赖。文档纠错 Gate 以 read_only/system_admin/contract_admin 覆盖禁止直达、纠错专用投影、非法原因、候选 Worker、独立激活、整页刷新恢复、console 0 和 PostgreSQL 终态；制度撤销 Gate 以普通读者/audit_reviewer/system_admin 覆盖无写入口、提交、两次刷新恢复、独立执行、撤销后普通读者不可见、console 0 和 PostgreSQL 终态。最终同轮输出 `DOCUMENT_CORRECTION_BROWSER_GATE=PASS`、`POLICY_REVOCATION_BROWSER_GATE=PASS`，当前运行专用容器/网络/端口为 0，历史失败轮遗留的 12 个无进程引用测试 auth 临时目录计数前后不增加；宿主文件策略阻止本轮删除这些旧目录。机器证据为 `tests/evaluation/local-p1-browser-closure-v1.json`。
- 两次历史百炼批量运行继续原样绑定 review v1，均为 49/50；runtime UUID 清理后不能反查 source case。review v2 先按离线候选修订 `SBCV1-N-INVOICE-02`，获授权真实复跑仍为 49/50，但稳定失败 source case 已保留为 `SBCV1-N-APPROVAL-01`。真实根因是“代理审批最长天数”与允许制度“临时权限明确到期”语义过近。review v3 只把它改为“付款审批通知邮件标题”缺失探针，离线近似 `0.547008→0.453996`，并保留历史资产。2026-08-20 的独立 v3 授权收据固定复用仓库既有百炼凭据、local/test、累计 10 请求/50000 input tokens/CNY 10 和只在剩余额度内复测；零费用环境预检后，正式运行一次通过 50/50、100/100 与 36 成员可丢弃索引激活。实际 10 请求、8874 input tokens、CNY 4439 microunits，零重试、零授权泄露/no-answer 假阳性；10 条成功调用审计/20 个 Event v2、2 个独立审批数据集、2 个通过运行与 150 条结果完成数据库核对，Collection/容器/网络/受管环境残留为 0。证据为 `synthetic-benchmark-runtime-evidence-v5.json`；该结果只关闭 owner-delegated 合成 local/test Gate，不改变业务代表性、人类 UAT、production 或正式 AC。
- 合同 85%、发票 95%、结构合法率 99%、确定性规则 100%/高风险漏报 0，以及 Markdown/映射/覆盖/追溯 100% 的计算和阈值均已存在。本轮公开搜索前，仓库受管 fixture 只有 `synthetic=true` 资产；后续公开原始数据也只进入 Git 忽略目录。没有经业务审批的客户代表性合同、发票、重复、风险标准答案和复杂多格式输入时，不能通过改名或 Agent 自审关闭该质量缺口。
- 当前交付目标仍是已批准 Local MVP：`AI_PROVIDER_CALLS_ENABLED=false`、`OCR_PROVIDER=not_configured`，确定性规则与人工流程保留。除受限、单次、明确授权的 local/test Gate 外，不把 Provider/OCR 默认打开，也不把本地智能完整版或 production 作为已完成。
- BOSS 随后明确要求 Agent 自行寻找公开数据，但没有提供 `approval_ref`。`public-business-benchmark-runtime-v1` 固定 CUAD 510 合同/12 类 PDF、10 份海口官方中文政府采购合同公告及附件、Zenodo `6371710` 的 813 份公司票据和 XFUND 50 份中文表单；原文、票面、税号、当事人和标注值只在 Git 忽略的 `data/public-benchmark/`，仓库证据只留计数、Hash 和状态。CUAD 首轮暴露 `pypdf` layout 全文为空但默认模式有文本时误转 OCR；只在 layout 为空时回退默认提取后 12/12、490 页通过，真正双空页仍 fail-closed 转 OCR。当前确定性合同字段命中仍为 0；10 份中文扫描合同、50 份真实票据和 50 份中文表单因 OCR 关闭失败。合同公开源覆盖 9/13、发票 7/13；55 个自然重复组缺产品必需 `invoice_code`，风险缺匹配的独立结果标签。因此来源资格 `PASSED`，总体 `MEASURED_FAILED`，公开跨域技术数据不能冒充客户业务代表性、85%/95%、人类 UAT 或 AC。最终完整离线门禁为 Backend `3161 passed / 147 skipped / 1 warning`、Ruff 527、mypy 283、Frontend 539/151，`LOCAL_OFFLINE_QUALITY=PASS`。
- 不改变默认 Profile 的后继 `public-windows-ocr-pilot-v1` 复用 Windows Media OCR 的 `en-US/zh-Hans-CN`，对 20 份真实票据、20 份 XFUND 表单和 3 份/37 页中文扫描合同共 77 页运行。质量分别为票据直接字段 `20%`、中文实体召回 `82.4401%`、合同直接字段可见率 `25.9259%`。预存 `qwen3:8b` 的生产 Schema grammar 因 `\d` 不兼容 HTTP 400，全量 JSON 模式 300 秒超时；字段关键词+相邻行的 80 block 模式在 107.8 秒完成但未通过生产 Validator、`0/9` 可采用。零自动重试/外网/费用、无输出采用，Ollama 进程与 11434/8764 listener 清理为 0。该结果证明本机存在 OCR/模型不等于智能完整版可用，扩大同类样本无望达到 85%/95%。
- 第 6 项公开数据后继已固定两份新证据。ExtractBench revision `f6180e9…e58` 的 370 份/4869 页中 325 份真实；8 份真实发票共 18 页，10 个直接字段的 80 条规则均已验证。通用英文确定性提取修复把默认 Profile 的代理精确匹配从 `16/104=15.38%` 提升到 `48/104=46.15%`，非空字段 `32/83`，但两份扫描件仍失败，`invoice_code`/买方税号仍是源 Schema 缺失假设。DocuBench revision `43a3f3b…cf42` 的 72 份/448 页覆盖 12 语言、10 格式；项目支持的 65 份格式只解析通过 34 份，23 份扫描 PDF、6 份图片、2 份无效 PDF 和 12 次旋转文本不完整警告均被精确分类。其最佳发票仍缺 `invoice_code`，最佳合同仅 4/13，旋转对是收据。原始 DocuBench 仓库位于 `%LOCALAPPDATA%/FinAuditAgent/public-benchmark/` 的 revision 专用缓存；公开 URL 中的 token-like 字符串不进入仓库 secret Gate。RealKIE 的 198 份资源合同虽有 23 类人工标签，仍缺编号、金额、币种、双方税号和付款条款，因此未下载 38.4 GB 包。两份新证据 SHA-256 为 `0A7F94B0A8941129D8A3CB759EF441FA35E074397606424504C5BC21AD2DECBC`、`2D6F9E8F56B24FCBC8349807CB8E215A0CD8190B2C850F7ECFAA481F9887F3CB`；结论仍是更强的 `MEASURED_FAILED`，不能冒充客户业务代表性、重复/风险标准答案或 AC。
- 本机 `qwen3:8b` 发票紧凑 pilot 固定使用 ExtractBench 8 份/18 页、数字 PDF 产品 Parser、扫描 PDF Poppler+Windows OCR、纯 CPU/2048 context/最多 40 个首尾相关块、temperature 0/seed 0/256 output tokens/8 threads。GPU 4096/2048 均在真实输入生成前 CUDA OOM。纯 CPU 最终 8/8 结构合法，模型事实 `81/104=77.88%`，确定性原文回绑后 `82/104=78.85%`，74 个非空事实对应 74 条证据，总推理 295.203 秒；号码/类型/红字/税额 8/8，但主体名称、卖方税号和汇总金额不足。较长语义 Prompt 降到 76.92%，已回退并停止调参。证据 SHA-256 `BC9DC120365269129BD71981E80BD93360B919860C4444297E3DE0F31E546C72`；零外网/费用/重试，默认 Profile 不变，不能关闭 95% 或正式业务代表性。
- BOSS 于 2026-08-20 通过 `CR-031` 明确把第 6 项代表性业务准确率移出当前 Local MVP Goal，并固定为 `OUT_OF_SCOPE_KNOWN_LIMITATION`。该范围决定不修改 85%/95% 等产品阈值，不改写公开技术测量和本机 pilot 的失败/不可计算结论，也不升级工作包、UAT、production 或正式 AC；后继正式质量工作必须使用独立目标、业务批准数据和新运行授权。

## 2026-08-18 CR-005-R2 文档纠错闭环

- BOSS 批准 `CR-005-R2/recommended-forward`、DOC-D-001～003、两个 Handler、PARSE-004/005/006 与 migration/downgrade，并授权同步 active Request 和 local/test 实现；正确的单 LF exact decision snapshot 为 `c47944096f7ad143044a2347b8b0656190e9f49f0854c5f31a898b4b70872f65`。旧 `3f13…` 使用两个尾随 LF，不是规范快照；v2 approval schema 与 pre-meta 已绑定 R2、snapshot 和 `api_delta=3`。
- accepted Alembic head 为 `20260818_025`。025 在空/可审查数据边界加入 correction result NOT NULL+UNIQUE、Markdown mapping `NULLS NOT DISTINCT`、Asset 安全证据列与触发器、纠错 Job/Outbox identity 和 Job/Parse 可延迟状态一致性，并以任何新证据阻断 downgrade；不自动迁移真实数据。
- `manual_correction_snapshot` 只从 PostgreSQL 不可变来源重建 pages/blocks/exclusions，对一个块应用一个字段，Worker 成功不自动激活；PARSE-005 另行执行 Markdown/source-map 质量门禁和 sibling parent CAS。dispatch 最终失败与单次 Lease 耗尽会把 Job/Parse 原子收敛为 failed。合同/发票 Frontend 复用现有证据 Block+Parse ID 提供文本候选和手工第二步激活，不猜测 ID、不自动发送。
- `local-document-correction-crash-recovery-v1` 已在新的独占 local Compose 栈实际验证上述单次 Lease fail-safe：合成补充协议经 HTTP/ClamAV/file_process 后请求纠错，测试锁阻塞 exclusion 读取，精确 Worker SIGKILL 为退出码 137；Maintenance 在 60 秒 Lease+15 秒宽限后 `exhausted`，Job/唯一 step/候选 Parse 原子 `failed/WORKER_LOST`，候选 Page/Block/Markdown 为 0，旧 active、来源文本 Hash 和原件 SHA/ETag 不变，失败候选激活 409。机器证据绑定 runner/wrapper/Dockerfile，专用资源和镜像标签残留 0；此门禁不增加 attempt 2，也不证明真实 Asset、主机断电或 production。
- 缺少 CR-010 current 环境 Profile 时 PARSE-006 固定 503 且不创建 Parse/Job/Outbox/log；manual 来源含 DocumentAsset 时固定 `DOCUMENT_ASSET_SNAPSHOT_UNSUPPORTED`，旧 active 保持不变。补充协议/制度的通用 Block+Parse 读取已由 2026-08-19 获批的 CR-029 以前向 `api_delta=+1` 单独闭合，不回写或扩大 CR-005 的固定 +3。Provider、production 和真实数据迁移仍禁止。
- 当前前向验证：PostgreSQL 16.14 File scope 16、Retrieval/Policy 10 与完整 154 项双轮 PASS；加入四项最新 local 回归后的完整离线门为 Backend `3121 passed / 147 skipped / 1 warning`、Ruff 518、mypy 283、Frontend 28 files/539 tests/151 modules PASS。默认离线门禁仍不运行 PostgreSQL、Compose、浏览器、Provider 或 production；PostgreSQL、性能、纠错强杀和发票 pair 浏览器为当前 checkout 的独立显式证据。
- INV-004 精确重复候选已从两条 READ COMMITTED 查询改为一个 `duplicate_source` CTE + candidate LEFT JOIN statement；source basis 与本页 UUID keyset 候选共享单次 PostgreSQL statement snapshot，编译 SQL与真实 PostgreSQL statement 计数均为 1，Full 154×2 PASS。`local-invoice-duplicate-browser-v1` 又以真实 in-app browser 完成登录→列表→源详情→候选→pair；保护门禁观测两个 200 GET，DOM source/candidate/common identity、disabled writes、console 0 后接受。运行时 cleanup 通过，Docker daemon 重启后归属容器/卷/网络、端口和 Browser Python 进程复核为 0；通用 MinIO 数据卷按既有策略保留。跨页冻结、代表性质量、容量和 production 未验证。

## 2026-08-19 CR-029/030 读取闭环

- BOSS 按 exact snapshot 批准 `CR-029-R1 / DOCREAD-D-001～005`（`c2f1f094dbd76575c3028665dc93e44fb1e8724d9937b0a711524120cb80f698 / 7910 bytes`）和 `CR-030-R1 / POLICYREAD-D-001～005`（`30e7c0b29a6d44f6c5ef8d9a656d59765b61d97a6527cb22b8df4d78c7aff1b1 / 7072 bytes`），授权同步 active Request 并实现 local/test；各自 `api_delta=+1`，禁止 Provider、production、真实数据迁移、提交和推送。
- CR-029 的唯一读取入口按文件锁定同组织 `stored+clean` 与当前 active Parse，只投影有效非空文本 Block；Cursor 绑定 Parse、Page/Block 顺序和 anchor，跨文件/伪造 Cursor 422，版本切换 409。权限与业务角色逐字复用 PARSE-004/005；system_admin 可从 FileDetail 读取纠错专用投影，但仍不能枚举或预览文件。
- CR-030 的唯一读取入口按知识库返回仍为 published 且没有 revoke execute 的 request 最小元数据；只允许 `knowledge.approve+audit_reviewer` 或 `knowledge.publish+system_admin`。Frontend 删除手工 request UUID，执行仍显式填写原因，并由 Backend 重验 request/Policy/row-version/SoD；不授予 system_admin `knowledge.use`，不自动或批量执行。
- 计数勘误：两份签署材料把 pre full OpenAPI 98 误标为 `/api/v1`。机器事实是 pre 95 API-v1 + 3 non-api 运维 operation；两个精确 +1 后为 97 API-v1 / 100 full OpenAPI。签署 snapshot 原文保留，delta 与路径合同不变。
- 2026-08-19 可丢弃 Report Gate 中，应用内 Browser 与外部 Chrome 都能登录、加载真实合成 ready 报告并触发 XLSX 动作，但控制面均不产生 Blob native-download event；页面无错误，不能据此修改安全下载架构或宣称原生下载已捕获。该轮未形成 Gate PASS；自动续接中断的 PostgreSQL/MinIO 容器随后按精确标签/Compose 身份清理，named volume 保留，端口与测试环境变量残留 0。
- AUD-003 旧矩阵尾注“implementation key/hash、规则目录发布持久化和 runtime 未运行”已被当前源码与运行证据推翻：静态 15 规则 key/hash、catalog manifest、原子发布、PostgreSQL 不可变持久化、快照绑定和执行均已实现；规则/预览/快照聚焦回归、Audit 4 与 Full 153×2 通过。该修正不替代代表性规则质量、production 或正式 AC-007/012。

## 2026-08-19 local-knowledge-performance-v5

- `SecurityBaseline` 在新的独占 `finaudit-security-*` HTTP Compose 项目中增加 local/test 知识性能门禁：Provider disabled，清洁合成制度穿过 ClamAV/Worker、独立审批、安全专用 100 条评测、真实 Qdrant 索引激活/发布；先前注入制度随后经两阶段撤销，PostgreSQL 允许集只保留清洁制度。
- 门禁以 `fixed_test` 1024 维确定性 Embedding 连续三轮、每轮 20 个完整 HTTP QA。nearest-rank 端到端 RAG P95 为 `40.774/38.884/41.064 ms`；因为端到端包含 Top-5，同值只作为 Top-5 子阶段保守上界，均低于 15 s/2 s，不宣称分阶段精确时延。数据库独立确认 60 条 answered Query、引用、1～5 命中和 60 条追加日志。
- 机器证据 `tests/evaluation/local-knowledge-performance-v5.json` 绑定 `scripts/smoke_local_security.py` 与 `scripts/verify-local-stack.ps1` 的 bytes/SHA-256。专用项目、容器、卷、网络、runtime/Secret、进程临时环境变量和 `security-kp-v5` 镜像标签最终为 0。该合成制度、单客户端、本机硬件和 fixed_test 不替代代表性 50/100 质量、Provider、正式容量、production、UAT 或 AC。

## 2026-08-19 local-performance-baseline-v3

- 现有专用 `finaudit-perf-*` 三轮门禁升级为 `finaudit-local-performance-v3`：每轮除既有列表、20 个单件 scan-only、20 件批次/重放、部分失败、21 件超限和 3 个审核任务外，新增一份 run-scoped 20 页合成文本合同，固定 `auto_process_requested=true`，从 HTTP 上传计时到 `stored/clean/file_process succeeded`。
- 当前本机 Docker 29.6.1、20 CPU、8,223,633,408 bytes、Worker concurrency 2、AI disabled、OCR not run；三轮受理为 `91.002/30.200/29.979 ms`，完整处理为 `892.206/573.639/567.867 ms`，均低于 120 s。PostgreSQL 逐轮确认唯一 file_process Job、attempt-1 scan/parse/markdown 三个成功 Step、published Outbox、唯一 active Parse 恰 20 Page，以及同 Parse、零 blocking issue 的唯一 active Markdown。
- 机器证据 `tests/evaluation/local-performance-contract20-v3.json` 绑定 benchmark/wrapper bytes 与 SHA-256；专用项目、容器、卷、runtime 目录和 Secret 均清理为 0。该文本 PDF 不含 OCR、真实 AI 提取或代表性复杂条款，只证明当前 Local Profile 的工程参考，不升级正式参考环境容量、业务 UAT、production 或 AC。

## 2026-08-19 local-performance-baseline-v4

- 同一受保护门禁每轮新增一张 run-scoped 合成 DOCX 发票，完整包含 13 字段与 1 条明细；AI Provider disabled 时生产 Worker 使用确定性提取器。计时从 HTTP 上传到候选可经真实发票列表/详情读取，三轮为 `635.969/603.018/605.273 ms`，受理为 `34.180/29.745/29.384 ms`，均低于本机 30 s 工程目标。
- PostgreSQL 逐轮确认唯一 invoice 文件业务绑定、file_process 和 invoice_extract 两个 succeeded Job/Outbox、scan/parse/markdown/extract 四个成功 Step、13 字段证据、1 条明细与 `draft/unconfirmed/unique` 精确事实；同轮 20 页合同为 `1165.315/830.661/576.725 ms`。机器证据 `tests/evaluation/local-performance-clear-invoice-v4.json` 绑定当前 runner/wrapper SHA-256。
- 专用项目、容器、卷、runtime/Secret 与 `perf-invoice-v4` 测试镜像标签最终为 0。该合成 DOCX 不构成经审批清晰发票集、95% 准确率、真实 AI/OCR、production、正式参考环境容量、UAT 或 AC。

## 2026-08-17 当前 goal 收口事实（覆盖下方早期快照）

- BOSS 于 2026-08-17 明确要求密码最小长度 6 在所有环境全局生效。`CR-023` 将策略升级为 `auth-password-v2`：6～128 Unicode code point、UTF-8 不超过 512 bytes、无字符组合要求；Argon2id、五次失败锁定 15 分钟、一次性换密和版本化弱密码黑名单保留，并新增拒绝 `123456/letmein/qwerty`。这是对未来公网密码熵下限的显式安全取舍，不得误记为仅 Local MVP override。
- BOSS 同日明确要求应用入口 HTTP 在所有环境全局生效。`CR-024` 移除内置 Nginx TLS、证书/私钥生成与挂载、本地 TLS 中继，并把公开 Origin、CORS、启动/恢复/门禁改为 HTTP；Refresh Cookie 的 `Secure` 仅按公开 Origin scheme 决定。当前仍只允许 `127.0.0.1` Local MVP；HTTP 不提供传输机密性或服务器身份认证，任何局域网/公网发布前必须形成新决定并恢复受信任 TLS。
- `CR-023/024` 当前运行证据：持久 `finaudit-local` 与全新一次性栈均以 HTTP 启动并依赖 ready，Nginx 无 SSL 指令、HTTPS 握手失败、唯一主机绑定为 `127.0.0.1:8443`，新运行时不生成/挂载 TLS 文件。一次性栈通过 5 字符与弱 6 字符拒绝、安全 6 字符创建/强制换密/普通登录、`/auth/me`、Refresh/Logout、无 Secure 的 HttpOnly/SameSite Cookie，以及 HTTP multipart → ClamAV → Worker → PostgreSQL/MinIO 预览；专用容器、卷、Secret 和镜像已清理。持久 `admin` 仍等待 BOSS 亲自完成最终换密。
- BOSS 已亲自完成持久 `admin` 最终换密并重新登录。数据库确认 active、失败数 0、未锁定、force-change false、普通活动会话 1；真实浏览器确认系统管理员工作台和用户管理授权读取成功、全局 6 字符提示可见、文件管理直达转为 `AUTH_FORBIDDEN`，console warning/error 为 0。恢复密码先清零后已删除；YHBX Local MVP UAT 只剩正式签署结论。
- YHBX 于 2026-08-18 明确签署 `Local MVP UAT通过`。签署制品为 `docs/testing/local-mvp-uat-2026-08-18.md`，只接受当前 Windows 本机、实际使用者 1 人但系统支持多账号/五角色、`127.0.0.1` HTTP、ClamAV、OCR/AI 关闭和本机数据/备份边界；不接受多人并发容量、传输加密、局域网/公网、production、正式 AC、正式 DAST/容量/RPO-RTO 或外部 Provider。
- BOSS 随后明确要求 AC 只按本地运行调整。`CR-025` 将 AC-001、AC-002、AC-015、AC-016 改为 Local MVP 口径；当前实际使用者 1 人不等于单用户系统，系统继续支持多账号/五角色，只是不承诺多人并发容量。当前 HTTP 安全基线和直接浏览器/数据库 Prompt Injection 终审重新 PASS，权威备份在独立 HTTP 项目完成行数/对象摘要恢复、Redis/Qdrant/ClamAV 重建和恢复后冷启动；`docs/testing/local-mvp-ac-acceptance-2026-08-18.md` 将四项均裁定为 Local MVP `ACCEPTED`，扩大环境必须重验。
- BOSS 于 2026-08-17 将当前交付阶段冻结为 YHBX Windows 本机 `Local MVP`：当前实际使用者 1 人但系统支持多账号/五角色且不承诺多人并发容量，Docker Desktop、`http://localhost:8443`、仅 `127.0.0.1`、全局 HTTP 入口、本地 ClamAV、OCR/AI 关闭、仓库外受管 Secret 挂载、本机 PostgreSQL/MinIO 数据卷、重要操作前本地备份；异地备份、保留期、正式容量/RPO/RTO/DAST、remote/分支保护均留待公网部署前，YHBX 是本阶段 UAT 与发布责任人。该决定只关闭本机 Profile，不升级 production 或正式 AC。
- BOSS 随后明确授权恢复本地 `admin`。操作先完成 PostgreSQL/MinIO 权威备份，再生成仓库外恢复密码并调用现有 `UserManagementService.reset_password()`；数据库确认账号 active、失败数 0、未锁定、活动会话 0、强制换密 true 和 `users.password_reset/succeeded` 自审计，真实登录返回预期换密挑战。密码与 Token 均未输出，未直接编辑 Hash，恢复容器零残留；普通业务会话和 YHBX UAT 仍等待 BOSS 完成交互式换密。
- 2026-08-17 起，从源码仓库在主机直接运行 Backend/Worker 时，`Settings` 默认加载由模块绝对定位的 `infra/env/.env`；优先级为显式参数 → 进程环境变量 → 运行时 Secret 文件 → 仓库 dotenv → 默认值。只有可验证的仓库源码布局启用该来源，Docker 镜像仍排除 `.env`，Compose/production Secret 注入和 `AI_PROVIDER_CALLS_ENABLED=false` 边界不变。
- `file_process` 已按 business type 创建 `contract_extract` 或 `invoice_extract` Job；合同链具有固定 Registry/Input/Summary artifact、确定性 extractor、fenced Executor、Repository 持久化、Worker/Dispatcher/恢复接线和人工修正/确认 API/UI。隔离浏览器 Gate 已实际完成合同与发票上传 → Worker 提取 → 人工修正/确认 → 合同来源供应商修正/激活 → 发票来源精确复用同一供应商 → 主合同关联 → 审核执行/复核 → PDF/XLSX 报告，并通过最终 PostgreSQL/MinIO manifest、零浏览器 warning/error 与受控清理；它使用合成数据、合成 clamd 和确定性 AI，不等于 production 或 AC。
- 2026-08-16 当前 checkout 已重新显式通过 `scripts/verify-financial-loop-browser-gate.ps1`，最终输出 `FINANCIAL_LOOP_BROWSER_GATE=PASS`。供应商 Gate 的长期不变量是“名称修正与确认激活必须由同一次候选更新聚合记录”：先保存字段、再确认会形成 2 条 correction/2 次 update，并被最终 manifest 拒绝；只有清理后全新重跑的 1 条聚合 correction、2 次 resolve、1 次 update 计为通过。门禁同时核对 2 个文件/绑定、唯一合同/发票/供应商/主关系/任务/执行/ready 报告、五角色登录/未授权直达、PDF blob/XLSX 动作与零 console warning/error。PostgreSQL/Redis 运行固定使用本地不可变镜像 ID 和 `--pull never`；专用容器最终为 0，本地 MinIO 数据卷按启动器策略保留。
- Dashboard、供应商、知识库/问答和审核 Frontend 已改为同源真实 API；文件链已增加批量独立结果、原件/文本预览、引用保护归档和同一失败 Job 重试。主要 P0 页面不再以静态 fixture 冒充运行状态。五个纯角色账号的浏览器导航与禁止直达矩阵已运行：财务/审计/合同角色进入授权业务页，审计不显示供应商写入口，只读仅有工作台/审核任务，系统管理员仅有工作台/用户管理/操作日志；2026-08-17 报告页桌面与 390×844/375 px 自动化 DOM 检查通过唯一 main/h1、无重复 ID、标题层级、可聚焦元素命名、iframe title 和页面级零横向溢出。共享 `PageTabs` 已使用 roving `tabindex` 和左右方向键/`Home`/`End` 聚焦，发票与知识页 `tabpanel` 关联到对应标签；257 个聚焦用例、typecheck/build 以及一次真实发票详情浏览器切换通过，console warning/error 为 0。浏览器控制层仍不能可靠模拟原生完整 `Tab` 路径，屏幕阅读器与正式可访问性验收仍未运行。
- 合同详情现已接通补充协议字段级详情、整组变更替换、确认/拒绝和基准日期有效字段投影；Frontend 严格校验 identity/date/typed JSON/排序/证据，复用同一逻辑提交的幂等键，并对 `financial.read` 保持只读。Backend 同步把 bbox 缺少完整 block/page/quote 证据在 Pydantic 入口拒绝为 422。财务只读的桌面/390 px 真实浏览器已核对 13 项有效字段和零 console warning/error，并由此修复窄屏 document horizontal overflow。独立 `supplementary-agreement-browser-v1` 在全新 PostgreSQL 中由真实 `contracts.manage` 浏览器会话完成 `amount: 100.25 → 120.50`、完整 block/page/quote 证据、人工确认和基准日生效查询；2026-08-17 当前 checkout 的重跑又以两个浏览器标签真实触发旧 `row_version=1` 的安全 409，并完成另一待确认协议的拒绝。系统管理员、财务、审计、合同和只读五个 Actor 均真实登录；manifest 核对主协议版本 3、拒绝协议版本 2、三条操作日志、三条成功幂等记录及 `PUT 200/409 + POST 200/200`，completion `accepted`，门禁 PASS、容器零残留。运行中还修复 `StatusTag` 遗漏 `unconfirmed/pending_confirmation` 中文标签。附件/版本替换、正式可访问性、production、业务 UAT 与 AC 仍未运行。
- `ai-call-audit-runtime-v1-v2` 已实现 provider-neutral AI-005 PostgreSQL primitive：同业务操作/Policy 的 transaction advisory lock 下执行 durable reserve，守住事件版本、单一币种、尝试、单请求/总 Token、预留费用和 deadline；completion 可与业务事实共用事务；Outbox 按 sequence 原子投影到 `ai_call_logs`，Maintenance 负责 `outcome_unknown/late_completion` 与脱敏 WARNING。Event v1 和 legacy USD 字段继续用于历史回放；Event v2 使用 `cost_currency + reserved/actual_cost_microunits`，仅允许 USD/CNY/内部不计费，禁止 FX 和跨币种汇总。OPS-005 按版本返回安全字段，权威实际费用不完整时总额为 `null`。
- BOSS 于 2026-08-17 批准 `CR-022 / option-A / event-policy-v2 / USD-CNY-only / no-fx`，并授权复用现有百炼密钥执行一次受限付费 Embedding 连通性 smoke。`minimax-m3-bailian-qwen37-local-v2` 固定 Chat/USD 与百炼 `qwen3.7-text-embedding`/CNY；Backend RAG、Worker 索引批次和评测 case 只有在 Event v2 completion 与业务事实同一 PostgreSQL 事务提交时才采用向量。首次 smoke 已成功：固定两条短合成输入、43 input tokens、2 个 1024 维向量、Event v2 `succeeded`、CNY 22 microunits；未输出密钥、输入或向量，一次性数据库已清理。BOSS 随后另行授权同范围完整知识库 E2E，结果见本节后续完整链记录；两次授权均已消耗，再次真实调用必须重新获得明确授权。
- `RedisAiRuntimeControl` 以 Redis server time 和 Lua 原子维护三类共享并发/RPM/TPM/burst 池及目标级滚动熔断，Chat/Embedding 严格在 durable reserve 前取许可；29 项聚焦测试、锁定 Redis 7.4.9 digest 的 3 项专项集成测试和当前 4 项 Redis/Celery wrapper 通过，两个独立客户端共享状态，并确认 half-open 状态 TTL 覆盖完整探针租约。local/test Chat/Embedding、USD/CNY 审计、Redis 门禁和受限完整百炼知识库链已验证；代表性质量、Docker 自动重启、主机断电、production 与正式 AC 仍未完成，所以 AI-001/AI-005 保持 partial。
- `local-compose-v1` 的 SSOT 为 `infra/compose/compose.local.yml`、两份 Dockerfile、`infra/env/.env.example`、受管启动/验证/停止脚本和 `docs/runbooks/local-stack.md`。`CR-024` 后完整栈只在 `127.0.0.1` 暴露 Nginx HTTP；app/data 网络 internal，只有 ClamAV update 网络允许 local 出站。运行时 Secret 与 JWT keyring 位于 `%LOCALAPPDATA%/FinAuditAgent/runtime/<project>`，不写仓库 `.env`，不再生成 TLS 证书；first-org/admin bootstrap 幂等且初始密码只输出文件路径。
- local Profile 使用官方 ClamAV 1.5.3 INSTREAM；完整栈实际通过 dependency 200、必需 Worker 故障 503/恢复 200，以及 TLS multipart → 角色边界 → ClamAV → Celery Worker → PostgreSQL `stored/clean/succeeded` → MinIO 原件预览。独立本体故障门禁已真实覆盖 `file_process`、`contract_extract`、`invoice_extract`、`audit_execute`、`report_generate` 与 `knowledge_index_build` 的精确 Worker SIGKILL（退出码 137）、`failed/LEASE_EXPIRED`、Maintenance attempt 2 和唯一事实收敛。报告门禁在 PDF/XLSX 已写 MinIO、数据库制品事实未提交时强杀，确认孤儿对象保持相同字节并由 attempt 2 收敛唯一 ready 报告；知识门禁在真实 Qdrant 点与 PostgreSQL 成员 Hash 已提交、ready 事务未提交时强杀，确认相同 Point ID 幂等 upsert、跨恢复不变摘要和唯一 ready 索引。各专用栈的容器、卷、网络、Secret、helper、临时状态和端口最终均为 0；测试镜像随后单独清理。这些结果不证明 Docker 自动重启、主机断电、production 或正式 RPO/RTO。OCR 仍为 `not_configured`；AI Provider 默认关闭，只在已批准的受限 local/test Profile 中显式启用并运行真实 smoke。
- 本地权威备份只包含 PostgreSQL custom dump 与 MinIO 停机一致归档，并记录 SHA-256、核心表行数、对象内容摘要/数量/字节；Secret、Redis、Qdrant 与 ClamAV 定义不进入备份。隔离恢复会校验清单与逐表/逐摘要一致性，重建派生状态并等待完整栈 ready；恢复和恢复后冷启动已实跑。该证据不提供正式 RPO/RTO、异地备份、加密密钥托管或 production 恢复结论。
- Qdrant local 保持 `1.10.0`。在保留现有 1.10 派生卷的前提下直接启动 1.18.2 已因 segment `storage_type` 格式不兼容失败；未删除数据并已回退恢复。未来升级必须显式快照或从 PostgreSQL 事实重建，启动器不得静默删卷。
- accepted Alembic head 已前进到 `20260817_024`，表数保持 57/57。`021` 封锁检索索引、评测集和评测运行的初始状态旁路；`022` 关闭未确认发票空币种与旧 `CNY` 默认语义差异；`023` 增加可降级 AI 风险解释和报告草稿事实；`024` 保留非空 v1 历史并增加 Event v2 USD/CNY 通用费用列。pending AI log/未发布 Outbox 阻断升级，任何 v2 事实阻断 downgrade。隔离 PostgreSQL 16.14 current-head Full wrapper 已对完整 146 项连续两轮通过；这不等于 production migration 或正式 AC。
- 上传内容 Prompt Injection 同时具有 PostgreSQL 服务级回归和 local Nginx/HTTP + 真实 Qdrant 运行证据：合成 PDF 经官方 ClamAV、Worker、Markdown/Chunk、制度提交/独立审批、安全专用 100 条全 `no_answer` 合成集和索引激活；直接注入问题在检索前拒绝，普通问题命中含注入内容的授权制度后在 PostgreSQL 终审拒绝。两条查询均持久化为 `refused/PROMPT_INJECTION_DETECTED`，无答案、引用、命中计数或哨兵/系统提示泄漏，操作日志只保存安全状态，业务容器日志无 canary。该合成集不代表 50/100 条业务检索质量集，也不证明浏览器、正式 DAST、真实 Provider、production 或 AC。
- 当前追踪矩阵为 `2 implemented / 84 partial / 0 planned`。2026-08-17 纳入两套合成候选、owner-delegated 非人类技术复核、真实失败运行证据和 Chrome 键盘机器证据后的完整 `verify-local-offline` 通过：Backend `3046 passed / 141 skipped / 1 Starlette deprecation warning`、Ruff check/format 496 files、mypy 271 sources、pip check，以及 Frontend typecheck、26 files/527 tests/147-module build 均 PASS；baseline/Git/分支保护 fixture/测试资产正负门禁也通过。accepted head 为 `20260817_024`；PostgreSQL 16.14 current-head Full wrapper 对完整 146 项连续两轮通过，Redis/Celery wrapper 4 项通过。此前百炼知识库 E2E 以 7 次请求、3231 input tokens、CNY 1616 microunits 完成 15 个成员索引、Top-5 和 5/5 smoke；更晚单次技术 Benchmark 完成 36 条款索引后在 50 条门禁失败，100 条与激活未运行。业务代表性、人类 UAT、Edge/屏幕阅读器、remote 和 production 仍未通过或 `NOT_RUN`，正式 AC 保持 0/16。
- `synthetic-non-acceptance-v1` 是 2026-08-17 经 BOSS 批准的 local/test 非验收检索资产：纯标准库生成器基于仓库 P-001、RET-001 与安全负例产生 120 条候选，按 NFKC/空白折叠/casefold/SHA-256 去重为 100 条并固定 50 条子集；100 条为 answerable/no_answer/unauthorized `60/20/20`，50 条为 `30/10/10`，P-001-V2 四锚点均衡覆盖。资产 SHA-256 为 `E15784ECA50B142FEE05F78DF212766196B95F7859117807E051E4890E2D7880`，8 项来源/证据检查通过；生成阶段 Provider 请求为 0、未访问数据库、未激活索引、未提交审批、runtime tier 为空。按当前每题一次 Embedding 请求，复用 ready 索引时 50/100 条需 50/100 次请求，重建四条源分块需 51/101 次，规划 CNY 上界 `0.001760/0.003458`；原 20 请求上限不足以覆盖两种完整评测。它不是业务代表性、正式质量、UAT、AC 或 production 证据。
- `synthetic-policy-corpus-v1` 与 `synthetic-benchmark-candidate-v1` 是 2026-08-17 经 BOSS 批准的第二套 local/test 合成候选资产：纯标准库生成 6 个虚构制度族、12 个有效期不重叠版本、36 条证据条款和 120 条候选，覆盖报销、供应商、发票、合同、审批权限、审计留痕，以及 answerable/no_answer/unauthorized、同义改写、基准日期、制度版本和跨权限；100 条提议集为 `60/20/20`，固定 50 条 `mvp_uat` 为 `30/10/10`。语料/候选 SHA-256 分别为 `2CE2BE5118ADAC7D185D239AFD4713D3F637BDF9D12D314D7690732A8DFAD9E3` 与 `A5A9C179B6F344C5CDA8B50089AC85254E87BDB62694D6B9360A6F5D46327C03`；8 项自动质量检查异常数为 0，18 条仍待人工复核。生成阶段 Provider 请求为 0，未访问数据库、未创建已批准数据集、未执行发布门禁、未激活索引或改变 AC。后续 50+100 两次真实评测复用兼容 ready 索引估算 150 请求/12497 input tokens/CNY `0.006249`，若先以两批次索引 36 条语料则合计 152 请求/16576 tokens/CNY `0.008288`；不含 Chat/重试，真实运行必须另行授权并以 Provider usage 为准。这些资产不是代表性、正式质量、UAT、AC 或 production 证据。
- Docker Scout 1.23.1 对当前本地 Backend Bookworm 镜像 `sha256:44b77246577e3d2d8ba1877fdfe03dc05afeba5f71e8e68bfedfeb1b399d1e11` 复现 `2C/2H`，`--only-fixed` 为 `0C/0H`。`pypdf` 已固定为 `6.14.2`，运行镜像已删除 `pip/setuptools/wheel`，以 `10001:10001` 运行。四项残余均来自不可移除的 `perl-base` 且无已修复版本；官方 Python 3.10.20 Trixie 锁定 digest 候选仍为相同 `2C/2H`，因此恢复 Bookworm 并删除候选镜像。未签署 VEX 或风险接受，production 零严重/高危门槛仍未通过。
- `report-artifact-compatibility-v1` 使用固定合成正式报告在宿主与当前本地 Backend 镜像分别生成 PDF/XLSX：两份 3 页 PDF 字节相同、文本语义相同且 Poppler 渲染通过，三页视觉检查无裁切/重叠；两份 XLSX 规范化工作表/单元格内容相同，虽 ZIP 字节不同但均由 Microsoft Excel 禁用宏、只读实际打开。LibreOffice 未安装而为 `NOT_RUN`；该证据不替代 production、UAT、AC-014 或正式 AC。
- BOSS 在本轮 `/goal` 中明确要求收口 Git 与文档基线；`main` 已建立首个本地 commit，源码、规格、测试、治理脚本和文档绑定的 `.finaudit/approval-artifacts` 已进入可复现 revision。可再生的 `tmp/`、`.codex-artifacts/` 与 `.finaudit/locks/` 保持忽略；没有配置 remote 或 upstream，也没有 push、tag、release 或远程分支保护证据。
- `ai-extraction-prompt-v1` 固定合同/发票独立 `purpose/prompt_id/v1/hash`、单一 parse version、唯一 block/位置和 canonical JSON renderer；正文只作为不可信 user data，Prompt 固定 13 个字段、逐字证据、无证据 `null` 与禁止业务决定边界。它现已通过真实 Gateway/EventSink 接到合同/发票业务链，采用前执行严格结构/证据/事实版本校验并与业务事实原子提交；代表性字段准确率、生产 Provider 与正式 AC 仍未运行。
- `local-performance-baseline-v2` 已在独立 local Compose 全新栈连续三轮通过，并在同一栈以新 run 再完整重跑三轮通过：Docker 29.6.1、20 CPU、8,223,633,408 bytes 内存、Worker concurrency 2。首个最终 run 的审核列表 P95 为 `9.337/8.465/8.973 ms`（阈值 800 ms），单文件上传受理 P95 为 `37.419/35.596/51.112 ms`（阈值 3 s）；默认最大 20 件批次受理为 `415.348/426.256/464.568 ms`，同键重放为 `144.390/148.449/177.125 ms`，60 个批量 scan-only Job 全成功。2 件部分失败批次稳定为 1 成功/1 `FILE_FORMAT_NOT_SUPPORTED` 并可重放；21 件批次在 `12.695 ms` 返回 413。PostgreSQL 对每个 run 精确确认 61 个文件、61 个唯一 `file_scan` Job、61 个 attempt-1 scan step、61 个 published Outbox，且超限名称零落库；第二个 run 的最大批次受理为 `421.161/426.826/422.209 ms`，证明 run-scoped 核验可重复。每轮 3 个审核任务仍无丢失/重复，但 concurrency 2 不证明三个任务物理同时执行。小型合成 PDF 的默认 20 件只关闭本地默认批次容量证据，不代表正式参考环境完整容量；清晰发票、20 页合同、Top-5/RAG、真实 Provider、生产 OCR/Scanner、生产 CA/TLS/Secret Manager、Docker 自动重启、主机断电、异地恢复、AC-001～016、UAT 和 production 仍为 `BLOCKED/NOT_RUN`。
- `local-security-baseline-v1` 已在独立 `finaudit-security-*` Compose 项目通过：除 TLS/CSRF/锁定/防枚举/权限/Trace/回滚/不可变日志/secret 哨兵/容器最小权限和 loopback 暴露外，现还穿过真实 Nginx/HTTP、官方 ClamAV、Worker、双人审批、安全专用 100 条合成集、真实 Qdrant 与 PostgreSQL 审计完成直接/间接 Prompt Injection 拒答。门禁实际暴露并修复两项根因：向量摘要必须按 Qdrant float32 存储精度规范化，不能对写入前 Python float64 做逐字节哈希；strict Pydantic 评测 DTO 必须在公共 JSON 边界把 tier/label 字符串及 cases/ID 数组显式规范化为内部 Enum/tuple。真实浏览器增量已在全新专用项目通过：浏览器正确拒绝短期自签名 CA，未绕过警告或修改系统信任；受显式门禁保护、仅绑定 loopback 的 HTTP 测试中继把页面/API 请求通过 TLS 送入原 Nginx → Backend → Qdrant 链。一次性审计角色完成登录、工作台到问答页导航、活动知识库加载和 run-scoped 直接注入提交，页面可见 `PROMPT_INJECTION_DETECTED`、控制台零 warning/error；PostgreSQL 独立确认唯一 Query、问题 Hash、Trace、空答案/引用、零命中和追加式操作日志，依赖/容器日志与最终零残留复核通过。P0 Schema 强制单组织，真实跨组织 IDOR 无法构造；浏览器直接信任自签名或 production CA、完整审计链、正式 DAST、production 镜像复扫/签署、production 与 AC-015 仍为 `NOT_RUN`。

- 2026-08-18 当前 Goal 的最新可复现 checkpoint 覆盖本节更早数字与 TLS/中继边界：`verify-local-offline` 为 Backend `3086 passed / 142 skipped / 1 warning`、Ruff 497 files、mypy 271 sources、Frontend 26 files/527 tests/147 modules；PostgreSQL 16.14 current-head `20260817_024` 为 149 项连续两轮 PASS，Redis/Celery wrapper 4 项 PASS。全新财务浏览器门禁 completion `accepted`、`FINANCIAL_LOOP_BROWSER_GATE=PASS`，manifest 精确为 2 文件/绑定和唯一合同、发票、供应商、主关系、任务、执行、ready 报告、5 纯角色 Actor。全新 HTTP 安全栈修复 `/metrics` 重复 nosniff、验证器旧指标名和 detach 自动重启探针后，从空栈通过 protected metrics、bounded logging、Worker `unless-stopped` RestartCount、ClamAV、制度双人审批、安全 100 条集、真实 Qdrant、直接/间接 Prompt Injection；真实浏览器加载活动知识库并显示 `PROMPT_INJECTION_DETECTED`，PostgreSQL/审计终审、依赖恢复和业务日志无 canary 均 PASS。专用财务容器清理且测试 MinIO 数据卷保留；安全栈、卷和受管测试 Secret 已清理。该证据不改变业务代表性 50/100 条质量、正式 DAST、production、Edge/屏幕阅读器和其余 AC 的未完成状态。
- 同一 Goal 的运维增量以 `docs/testing/test-operations-evidence-2026-08-18.md` 为汇总证据：持久 `finaudit-local` 在权威备份后原地重建并通过 `OperationsReadiness`；十个长期容器使用 `unless-stopped` 与 Docker `local` 5×10 MiB 日志上限，受管 `/metrics` 运行时 401/200 与单一 `nosniff` 通过。三份完整备份通过只读 manifest/bytes/SHA-256/Secret 排除/新鲜度审计，最新备份在独立项目完成 PG 行数、MinIO 摘要、Redis/Qdrant/ClamAV 重建、运维门禁和停止后冷启动，目标零残留。当前 OpenAPI 有 89 个唯一 `/api/v1` 操作，meta-gate 要求每个 operationId 均由 Backend 测试显式冻结，当前遗漏为 0。矩阵 13 个 TEST/DEP 行均引用该证据但继续保持 `partial`，因为 50 条真实质量门禁仍 FAILED，代表性合同/发票输入、100 条 formal-release、正式 DAST/性能/RPO-RTO、production 与 remote 未完成。
- 当前 HTTP 性能证据不得沿用旧 transport 标签：一次数值 PASS 的新 run 因仍输出 `https-nginx-backend` 只用于定位；`benchmark_local_performance.py` 修复并由单测固定为 `http-nginx-backend` 后，同栈另一新 run 三轮 PASS。列表 P95 `10.607/10.647/10.978 ms`，上传受理 P95 `36.794/41.385/38.023 ms`，20 件批次/重放、部分失败、21 件 413 与三审核任务唯一结果全部通过。专用栈与测试镜像零残留；该证据仍只适用小型合成 Local MVP。
- YHBX 明确选择路线 A：当前发布只按 CR-025 Windows 本机 Local MVP 收口，`docs/releases/local-mvp-0.1.0-2026-08-18.md` 的应用决定为 `GO / LOCAL MVP ONLY`；13 个 TEST/DEP 工作包继续保持 `partial`，正式代表性质量、production、DAST、异地备份、正式 RPO/RTO、remote 和分支保护转入后续目标。YHBX 同时授权在提供指定 remote 后配置并推送发布分支且验证分支保护，禁止 Force Push；当前尚未提供 URL，所以远程发布保持 `PENDING_REMOTE_URL`。`CR-026` 在该发布决定时只保留默认关闭候选；其后 BOSS 在当前对话另行授予并消耗一次 local/test Provider 权限，见下条，常驻 Local MVP 仍不启用 AI Provider。

- `CR-026` 记录 BOSS 于 2026-08-18 对同一冻结 owner-delegated local/test 技术集的单次新授权：复用现有百炼密钥、当前批准模型、评测批次 20、100 请求/50000 input tokens/CNY 10、零重试、50 失败即停且通过后才可运行 100。Production 默认仍逐题。Provider 前完整离线门禁为 3061 passed / 142 skipped，PostgreSQL current-head 完整目录双轮 PASS；真实运行以索引 2 批 + 50 条 3 批共 5 次请求、4971 input tokens、CNY 2486 microunits 得到 49/50 与授权泄露 0。唯一失败为 10 个 no-answer 中 1 个假阳性，故 100 条、激活和重试未运行；Collection、专用 PostgreSQL/Qdrant 和确认环境变量残留为 0。技术集仍固定 `human_review_claimed=false`，不能升级业务代表性、人类 UAT、production 或 AC。

- `CR-026` 的审计证据边界：5 个批次的 Event v2 completion 已随可见索引和 50 条结果事务采用；但质量失败发生在 runner 成功后置 `ai_call_logs` 投影核对之前，专用数据库随后清理，因此本次投影行数为 `NOT_RUN / not retained`，不得从 Provider usage 推断。
- Local MVP 0.1.0 发布前生成第 4 份 PostgreSQL/MinIO 权威备份，目录名 `20260818T052018Z`、backup ID `b94b77a1-1221-49bf-af87-927f97cb3300`、Secret excluded；持久 `finaudit-local` 随后按当前工作树重建为镜像修订 `local-mvp-0.1.0`，`OperationsReadiness` 与四份备份巡检均 PASS。发布分支为 `release/local-mvp-0.1.0`，remote URL 仍未提供。

- BOSS 随后再次以同一 CR-026 V2 边界授予一次性诊断运行。第二次结果完全复现为 5 请求、4971 input tokens、CNY 2486 microunits、49/50、授权泄露 0 与 1 个 no-answer 假阳性；100 条仍未运行，资源和确认变量残留 0。输出 runtime case UUID `7623955c-d413-44be-ab26-82ab25e24301`，但当次未保留到冻结 source case ID 的映射，可丢弃数据库清理后无法反查问题。Runner 已离线加固未来稳定合成 case ID 映射，但第二次授权已消耗且没有第三次 Provider 调用。
- DOC-008 当前本地证据已补强：Markdown 单元 49 项与 PostgreSQL 16.14 `File` 范围 12 项通过，覆盖 AST/raw-HTML 安全、精确字符/行 source map、100% 双覆盖、持久验证事实及重复调用零新增版本。结构块纠错仍由未批准 `CR-005 / BLOCKED-DOCUMENT-CORRECTION` 阻断，API/UI、并发 sibling、代表性复杂文档、production 和正式 AC 不得据此升级。
- DOC-002 修复 DOCX 伪根/重复主部件以及 PDF 非有限/非正 MediaBox、NUL/孤立 surrogate 文本可被接受的问题；解析器现覆盖八类畸形 DOCX、损坏/加密 PDF 与 6 个页面/文本漂移输入。解析器 28、相邻 OCR/解析/格式 79、PostgreSQL File 12 通过；代表性多格式、20 页容量、production OCR 和正式 AC-003/008 仍未运行。
- DOC-003 修复 OCR Port 非有限值与结构漂移可被采用的问题；图像/扫描 PDF 共用入口现拒绝 NaN/Infinity、空身份/行、零尺寸、非法/越界 bbox、page-text 漂移和不可持久化文本，并验证无 bbox 文本块持久化 `extractor_not_available`。解析器 28、相邻 OCR/解析/格式 79、PostgreSQL File 12 通过；production OCR Profile/Provider、代表性准确率/容量与正式 AC-003/008 仍未运行。
- BOSS 于 2026-08-18 提交 `+批准 CR-005 / BLOCKED-DOCUMENT-CORRECTION` 直接批准意图。审计发现 R1 实际 decision preimage `5bf91f...` 不等于记录 `739f20...`，current baseline manifest file hash `a0d1f...` 也不等于旧锚点，且角色/selected_option/当前 API delta 未绑定；需刷新 R2 后重新绑定，不能伪造正式批准或提前开放纠错 runtime。
- CON-002/003 的旧 009 checkpoint 已被当前事实取代：字段级补充协议载体、完整替换、确认/拒绝、纠错与操作日志、基准日逐字段来源投影均已实现；合同聚焦单元 140 项与 PostgreSQL 16.14 current-head 完整目录 149×2 通过。代表性合同 85%、production、业务 UAT 和正式 AC-003/004 仍未运行，两行继续保持 `partial`。
- INV-003/LINK-001 的旧只读/未实现描述已被当前事实取代：发票完整事实修正、确认/拒绝、重复候选/重检/决定，以及合同候选解释、建议、主关系确认/替换/取消/历史/双向读取均已接 Backend、PostgreSQL 和 Frontend。组合 Backend 222、Frontend 69 与 current-head PostgreSQL 149×2 通过；代表性发票 95%、业务 UAT、production、容量和正式 AC-005～007 仍未运行。
- INV-004 的旧 76/17 skipped、91×2 和“不写 duplicate_status”已被当前事实取代：精确候选/两票点查/重检/人工决定、候选重锁、CAS/幂等、纠错/日志和审核过期传播已实现。专属 Backend 80、Frontend 69、PostgreSQL Audit 4 与 current-head 149×2 通过；native pair 浏览器、代表性重复质量、单一 MVCC 分页、production 和正式 AC-005 仍未运行。
- AUD-005 的旧 API/database NOT_RUN 描述已被当前事实取代：非高/high 分权复核、confirmed/dismissed/adjusted 持久化、财务/审计复核、退回/取消/过期/重审、纠错/日志和报告投影已接真实事务与 Frontend。聚焦单元 125、PostgreSQL Audit 4、Frontend 页面门禁及 current-head 149×2 通过；代表性风险质量、production、正式安全与 AC-007/010/012 仍未运行。
- KB-004 的旧全量 NOT_RUN 描述已被当前事实取代：固定分块 Profile/JCS Hash、组织级 advisory-lock 配置、不可变 ChunkSet、审批触发分块和 Auth/API/Worker/索引链已实现。Backend 31、PostgreSQL Retrieval 8、Frontend 6 与 current-head 149×2 通过；可配置多 Profile、代表性复杂文档、production、容量和正式 AC-008 仍未运行。
- KB-003 的制度撤销/归档不能作为 Frontend-only 功能实现：当前 Backend/OpenAPI 只冻结 create/submit-review/approve/publish，SSOT 缺 URL、权限、允许源状态、索引失效传播、幂等、历史可见性和响应合同，记录为 `BLOCKED-POLICY-REVOCATION-CONTRACT`。REP-003 的数据库/MinIO/API/Auth/browser 旧边界已被当前实现取代；报告 Backend 96、Frontend 6、PostgreSQL Audit 4 与 current-head 149×2 通过，LibreOffice/代表性 UAT/production/正式 AC 仍未运行。

## 2026-08-15 早期 checkout checkpoint（已被上方 goal 收口事实覆盖）

- BOSS 已批准 `CR-018-R1/recommended-forward-v1` 并授权同步 Request 与本地实施。该早期 checkpoint 当时的 accepted Alembic head 为 `20260814_020`，SQLAlchemy/runtime catalog 与隔离 PostgreSQL 覆盖 57/57 张核心物理表；当前 head 见上方 goal 收口事实，下方所有 009/016/017、18/30 表和“剩余 39/27 表”记录只作历史追溯。
- 当前 `/api/v1` 已注册 Auth、用户写、Break-glass、操作日志、文件、合同/补充协议、发票、合同发票关系、供应商、制度/知识索引与评测、RAG/反馈、审核执行和正式报告 Router；实现遵循 Router → Service → Repository/Adapter。工作台摘要与依赖健康仍未注册，知识/问答和审核 Frontend 仍为演示。
- 文件链已包含单文件 multipart、同组织 SHA-256+大小去重、24 小时幂等、PostgreSQL 文件/Job/Step/Outbox/日志同事务、MinIO quarantine/originals、Scanner、PDF/DOCX/OCR、版本化页块、真实 Celery/Redis Dispatcher/Handler 和租约/恢复。2026-08-15 本地隔离浏览器 Gate 已验证真实 FormData → PG/MinIO → Outbox → Redis/Celery Worker → `stored/clean/succeeded`、reload/详情恢复和 403 不落事实；其 clamd 为合成端点，不是 production Scanner。批量上传、通用合同专用提取、强杀/断电、容量与 production Scanner 仍未验证或未实现。
- 财务链已包含补充协议字段级变更/有效投影、合同发票候选建议/确认/替换/取消/历史、发票提取/修正/确认/拒绝/重复处置，以及供应商合同/发票来源候选、精确复用、回填和纠错；均使用幂等、CAS、固定锁序和脱敏追加日志。供应商 Frontend 与完整浏览器写 E2E 未实现或未运行。
- 知识链已包含不可变 Markdown/source map/复杂表格制品、质量门禁、结构分块、确定性 Embedding、PG 索引成员、显式 Qdrant Collection、PG 允许集 → Qdrant must-filter → PG 终审、5 条 smoke 评测和引用白名单/拒答 RAG。50/100 条审批评测集、真实 Provider、知识 Frontend 与正式 AC 未运行。
- 审核链已包含静态 15 规则整批 publisher、任务/执行/不可变快照、规则结果/风险、财务初审、high 审计门禁、退回/取消/过期、事实失效传播和新执行重审；报告链在 completed execution 后自动排队，生成正式 PDF/XLSX，使用 API/Worker 分离 MinIO 身份持久化，并由 Backend 鉴权 inline/attachment 读取。客户端永不接收 Bucket、对象键、内部 endpoint 或预签名 URL。
- 当前完整离线质量门禁为 Backend `2668 passed / 124 skipped / 1 warning`、Ruff 382 files、mypy 218 sources、pip check、Frontend 21 files/488 tests/typecheck/136-module build PASS；它默认仍将 PostgreSQL current-head、Compose、浏览器、Provider、remote 和 production 记为 `NOT_RUN`。另有隔离 PostgreSQL 完整目录、真实 Redis/Celery、真实 Qdrant、真实 MinIO 报告写读/拒绝/篡改，以及 loopback 同源浏览器 PDF blob 预览、XLSX 动作审计和单文件 multipart → Worker 上传状态恢复的独立证据。矩阵为 `2 implemented / 75 partial / 9 planned`；完整合同提取/确认/关联/审核/报告浏览器写闭环、外部 Provider、完整 Compose/TLS/Nginx、容量/性能、备份恢复、正式 AC/UAT 与 production 均保持未完成或 `NOT_RUN`，不能从局部证据外推。

- 2026-08-11：UI-001～UI-014 的 15 个已登录业务路由模式已从 `ModulePlaceholderView` 切换为专用 Vue 静态演示页面；页面统一标注 API 未接入，工作台遵守 `GAP-067` 不伪造摘要或 Job，`GAP-064` 等未冻结写操作保持禁用。当时前端证据为 typecheck、无落盘 production build、226 个 Vitest 测试，以及此前 1280px/1024px 本地浏览器视觉与交互检查通过；该历史 checkpoint 只证明页面与请求层，不代表真实认证、业务 API、端到端业务闭环、AC 或 UAT 完成，最新数字见后续 `TEST-001` 记录。

- `Request/PRODUCT_REQUIREMENTS.md` 是当前最高产品需求基线；`TECHNICAL_SPEC.md` 只描述架构边界和事实源，`IMPLEMENTATION_PLAN.md` 只描述顺序与状态。旧 V1.3/V1.0 文档仅供追溯。
- P0 是单组织系统，固定五种角色，不包含多租户、多 Agent、GraphRAG、LangGraph、动态规则中心或自定义角色。
- PostgreSQL 是唯一业务事实来源；MinIO 保存文件与制品；Redis、Qdrant、Markdown、Chunk 和 Embedding 数据必须可恢复或重建。
- 字段提取与 Markdown 转换可以并行；制度分块只能读取质量通过且活动的 Markdown AST。
- 制度业务审批、索引技术批准和制度发布必须职责分离并分别留痕。
- 审核任务与审核执行版本是不同对象；规则、风险、快照和报告引用执行版本。
- P0 报告仅包括 PDF 审核报告和 Excel 风险明细。
- SRS 同时保留 MVP/UAT 最低 50 条制度检索评测集与正式 Hit@K 不少于 100 条经审批用例，二者如何共同构成 `TEST-001` 完成口径仍是 `GAP-001`；RET-001 的 5 条只用于功能验收和冒烟。
- SRS 的 `TBD-004` 以“后端架构设计前”为关闭时点；系统架构和开发计划已一致选定 Celery 5 + Redis，因此 `GAP-004` 已按设计链证据关闭，但这不证明 `BASE-006` 已实现。
- 本地作品环境可使用禁外联、类型白名单、隔离存储或占位扫描 Adapter，生产必须接入经验证的真实扫描方案；这是 SRS 与架构一致的环境分级门禁，`GAP-007` 已关闭，但不证明扫描功能或生产门禁已通过。
- `CR-001-R2` 曾于 2026-08-07 同步旧九份 `Request/`；其中 57 表、122 API、86 工作包现只作为历史盘点，不再冻结产品数量或实现形态。其业务语义已提炼进活跃规格；历史批准不等于 BASE-005/006、认证、文件链或 AC 通过。
- `CR-002-R4` 的 `environment_scope='contract'` 已于 2026-08-07 批准并同步；`AI_POLICY_VERSION=1`、`AI_PROVIDER_CALLS_ENABLED=false`。当前只允许不打开 socket 的离线 Mock；`fixed_test_provider` 与 `production` 仍为 `PENDING`，不得调用 `/v1/models`、真实 Chat/Embedding、内部 vLLM HTTP 或执行 canary。
- `CR-012-R3` 已于 2026-08-09 由 YHBX 以九个必需角色在 `environment_scope='contract'` 范围批准，decision snapshot 为 `b9d789568dd5b813c5fff4da03a823b922fc263245aeb01e53655b51ba1179c0`，并完成九份 Request 原子同步；GAP-063 已关闭。授权只覆盖 006 空表 DDL/ORM 与本地/专用合成 PostgreSQL 16 验证，不覆盖 GAP-064 运行时、真实数据、Provider/其他外网、部署或 production。
- `CR-004-R2` 已于 2026-08-09 由 YHBX 以九个必需角色在 `environment_scope='contract'` 范围批准 `REL-D-001～005`；effective contract 绑定 R1 base snapshot `387ed8d9eafbddcf1ca768abb770b13d1383a5c751077194ea3b7ca63523e7c6` 与 R2 addendum snapshot `77a8a2e63a4668a5e7e6a028b18cc04f34ddc824d04096db8bede7bce844241b`，并已完成九份 Request、正式 baseline manifest 和 active-baseline pin 的 11 文件原子同步。授权仅覆盖 `20260807_007` 空 Job/Step/Outbox DDL/ORM 与本地/专用合成 PostgreSQL 16 Gate；Handler Registry 只批准 meta contract，Registry/Schema/Input/Summary/Handler artifact 均仍 `PENDING / NOT GENERATED / NOT APPROVED`，Repository/Dispatcher/Worker/API runtime、Redis/Broker、真实数据、Provider 网络、部署、canary 和 production 均不授权。
- `CR-003-R3` 是当前特权授权 active-baseline successor：九角色审批 9/9，Gate A、十一文件原子同步、Gate B 与 008 storage-schema Gate C 均 PASS。最终 PostgreSQL 16.14（`server_version_num=160014`）对唯一 head `20260807_008` 连续两轮通过 71/71，并输出 `POSTGRESQL_CURRENT_HEAD=PASS`；证据覆盖 R2 §6.3 的精确 catalog、状态/来源/双向、SoD/action-time、GiST、往返、downgrade 与失败原子性。`BASE-005`、`AUTH-005` 仍为 `partial`，Repository/Service/API/Worker、真实 ACL/调用者绑定、真实数据、Provider、浏览器、部署、production 与 AC 均未授权、未运行或未实现。
- 2026-08-11 已以 recovery candidate 的 DDL 合同为来源接入 accepted 线性迁移 `20260807_009`，新增空的 `supplementary_agreements/invoice_items/contract_invoices` 及精确 ORM；recovery 文件保持原字节不变，accepted 文件只有 Ruff 所需的字符串换行格式差异。最初 PostgreSQL 16.14 checkpoint 对唯一 head 009 连续两轮 71/71 并输出 `POSTGRESQL_CURRENT_HEAD=PASS`；当前 storage-schema 仍为 18/57、尚缺 39 表，最新数据库目录计数见后续 `TEST-001` 记录。该证据边界始终不等于 Worker、真实业务数据或任何 AC 完成。
- `CR-011-R4/R5/R6` 构成当前批准链：R4 contract/offline Gate B 的 273/273、27/27、双引擎、zero-socket 与防伪结果是历史封板证据；R5/R6 已 9/9 批准并完成 Gate B 同步，R6 是当前 startup evidence-boundary successor。R6 Gate C 仍 `BLOCKED BEFORE RUN / NOT RUN`，且 R4 persistent AI-005 Gate C、Provider、真实数据、部署、canary 和 production 仍未授权。`CR-005-R1/CR-015-R2/CR-016-R1` 继续为 `NOT APPROVED`；不得用候选或历史 Gate 替代当前授权与运行证据。
- `CR-014-R3` governance contract 已通过 BOSS direct approval `item-572`，绑定 `11956 bytes / e5c00e6e47081d0e7418b48957d7895fe5a4705a1d5008256a7bb291d15b7cd9`；派生回执严格为 `NON_AUTHORITATIVE_EVIDENCE_ONLY`，身份 `1801 bytes / 7421b7913ed31b12ba37da52f2a74d3ac28d7eab01bfeea0efc5bcde2c9bd08b`。`CR-006-R4` 与 `CR-013-R3` source snapshots 已分别生成为 `8945 bytes / 159dd1588d9366c85e8aa1ffc5f9b910ea9319732a491785ce2fdf10d8096e8c`、`9496 bytes / 6cecc269e6517bb5b30d535350cdcb7e9aa2a00cf827199c2c2cb3789eae3d1a`，`CR006_R4_SOURCE_A1` 已 `RECEIVED / VALID`，其 create-only 本地 receipt 仅为 `NON_AUTHORITATIVE_EVIDENCE_ONLY`，身份 `1807 bytes / d98c2556a07deb73b6683fdc8c325498a19815acb0034cbf2ac3f7905f2aab1c`，`source_message_text_sha256=69886a5974497a981f8649f8071f476487319fe2f0abdfc9636b3618ef331695`；`CR013_R3_SOURCE_A1` 已通过 platform event `item-684` `RECEIVED / VALID`，其 create-only 本地 receipt 仅为 `NON_AUTHORITATIVE_EVIDENCE_ONLY`，身份 `2607 bytes / 1454233d212979663e26e2699b924689cb081e893b456daa30530e81a778b9c8`，`source_message_text_sha256=572c102327f313c2a6e82ad582c4369f827e62c613c74ab14aecfe6b8971a37b`；两份 A1 source approvals 已闭合。CR-013-R3 blocklist A2 仍为 `NOT GENERATED / NOT RECEIVED`，原因是获准本地目录未找到合同精确 source/license bytes；resolver 可内嵌于 A1 已冻结的两条隔离单文件脚本并由 implementation-manifest → 十键 payload 传递绑定，无需新 resolver CR，现有两份 A1 保持有效；expected-post/inverse bundle 仍为 `NOT GENERATED`，joint authorization 仍为 `NOT RECEIVED`。临时 transport drafts `CR-014-R4/CR-006-R5/CR-013-R4` 已撤回且不可批准。候选 lineage 仍为 009 → 010 → 011；010/011 migration 仍为 `NOT GENERATED / NOT AUTHORIZED / NOT RUN`，runtime 与 AC 仍为 `NOT AUTHORIZED / NOT RUN`，20/58 仍只是后续全部批准、同步、制品与门禁成立时的候选结果。

## 已确认但尚未裁决的基线差异

- 测试方案把字段提取阈值描述为首次运行确定，但需求已给出合同、发票和 JSON 合法率最低值。
- 测试方案称性能阈值待基线运行确认，但需求已给出多项 P95 数值。
- 开发计划把 `/metrics` 列为 P0，但需求 P0 API 清单未明确该接口。
- `TEST-007` 引用了不存在的 `OPS-001～OPS-005` 任务标识。
- Qdrant Collection 粒度在架构与数据库之间仍有冲突；原活动索引权威 `GAP-009` 已于 2026-08-07 证据复核关闭，`document_index_versions.status='active'` 条件唯一和事务切换是单一持久化事实，API 的活动版本 ID 是入参或派生值。
- `BASE-004` 不为 Qdrant Collection 提供默认名称；`GAP-008` 关闭前必须显式注入且占位符会被拒绝，避免配置层静默裁决架构冲突。
- `BASE-001` 的高置信 secret 门禁必须扫描 `.env.example`、`Request/`、`Demo/` 以及常见 `.pem/.key` 私钥文件；不得用整目录豁免处理示例误报，应使用精确占位符或窄 allowlist。隔离自测使用运行时构造的合成哨兵，只允许错误输出文件位置，不回显匹配值。本地 Git 门禁要求项目分支模型与 `git check-ref-format --branch` 同时通过，把 `RootPath` 绑定到同一仓库根与 `.git`，拒绝 `GIT_DIR/GIT_WORK_TREE/GIT_CONFIG_*` 重定向，并分别校验原始配置记录及 Git 解析后的 fetch/push URL；凭据、控制字符、百分号编码、remote-helper、本地/UNC 路径和明文协议均失败关闭。项目工作流的分支保护 synthetic fixture 只接受固定 `--stdin` 有界字节流，以严格 UTF-8/重复键/类型/分支与 check 规范顺序门禁证明本地五元组投影自洽；它不接收或打开 fixture 文件，不是 Request-exact、远程配置或演练证据。上述门禁均不 fetch/push、不访问托管平台；远程分支保护未配置或演练，因此任务状态必须保持 `partial`。
- `BASE-002/003` 的基础 HTTP 契约：前端只允许同源 `/api/v1`，既接受相对 API 路径也接受 Backend 返回的完整 `/api/v1/...` 路径且不会重复加前缀；path pathname 不接受百分号编码、反斜杠、fragment 或规范化后逃出前缀的路径。FormData 请求删除调用方 `Content-Type`，由浏览器生成 multipart boundary。`AUTH_PASSWORD_CHANGE_REQUIRED` 在所有路径固定使用中文消息并丢弃不可信 `details`，只有精确 `POST /api/v1/auth/login`、HTTP 403 且 `data` 合法时才把受限 Token 保留为不可枚举错误数据；成功响应 body 与错误响应 body/header 的 `trace_id` 只接受标准小写 UUID，不可信候选安全降级且排除与 Token 相同的 UUID 形态值。HTTP 204 仅由显式 `expectNoContent` 调用接受，JSON/204 契约漂移必须保留净化后的 Trace ID。Backend 的运行时 422 与 OpenAPI 都使用 `ErrorResponse`，当前 `/health` 固定中文 OpenAPI 摘要/响应说明和合成示例，Router 由 AST 静态测试禁止直接导入数据库、存储或模型层；`APP_ENV=prod` 时关闭 OpenAPI、Swagger UI 和 ReDoc。
- `BASE-004` 只接受 `postgresql+psycopg` 数据库 URL、`redis/rediss` Redis/Celery URL 与不含 userinfo/query/fragment 的 `http/https` base URL；数据库使用 SQLAlchemy 同源解析，并在 Settings 与 Alembic 迁移入口统一拒绝 `host/hostaddr/port/dbname/service/servicefile` 覆盖 query，全部 URL 拒绝歧义主机、控制字符与端口 0。MinIO endpoint 还必须是无 userinfo/path/query/fragment 的 HTTP(S) origin，scheme 与 `MINIO_SECURE` 一致且 production 强制 HTTPS；七个 Bucket 满足 S3 兼容命名规则并组内唯一。Celery 七个逻辑队列名称同样拒绝空白与不可见控制字符且组内唯一，两个命名空间之间允许同名。分块配置必须满足 `min <= target_max <= max` 且 `0 <= overlap < max`。Qdrant 与 Embedding 配置维度必须一致，Settings 启动与类级校验错误的字符串及结构化表示均不得保留原始输入。
- `BASE-004` 当前为 `partial`：`CR-011-R5/R6` 已 9/9 批准并完成 Gate B 同步；严格 Settings、固定本地 Policy/resources loader、approval pin 与 cross-binding 已进入 Backend/Worker pre-construction 路径，合成 Settings → Policy/resources → loader/pin/cross-binding → FastAPI/Celery object → 进程内 `/health` 的最小链有局部离线运行证据。R6 exact locked-subject Gate C 仍 `BLOCKED BEFORE RUN / NOT RUN`；局部测试不能冒充 Gate C、真实依赖健康、生产启动、Provider、业务 API 或 AC。
- `BASE-003` 的通用 `StatusTag` 已覆盖 Request 页面中 62 个去重正式状态码的简体中文和 ARIA 文案，未知 code 保持原样；`TraceIdCopy` 只复制当前 Trace ID，以 attempt identity 忽略属性切换后的旧异步结果，并提供脱敏的可访问反馈。这只是全局组件合同补齐，不代表任一 FE 业务页面完成或已做真实浏览器剪贴板、屏幕阅读器及视觉验收。
- `FILE-001` 当前为 `PARTIAL / S1-S4 + MVP-VS-01 + OQ-09 预备链`：除上传意图 Schema 外，已离线覆盖精确字节大小/SHA-256、批量数量、扩展名/MIME/Magic Bytes 一致性及有界 DOCX OOXML/ZIP 校验；组合用例在消费流前严格重验证 intent，再以一次遍历、有界缓存串联计量、格式与 clean-only 准入，非法构造或创建后篡改均脱敏失败且不消费流。pending quarantine intake、文件类型权限范围与 quarantine-only MinIO Adapter 已实现并测试；另已显式运行 real local MinIO Adapter Gate，使用合成 PDF 验证真实 quarantine PUT、精确内容/长度/SHA metadata 读取、DELETE，以及两个确定性补偿路径，Gate PASS。该 Gate 不包含 FILE Router、数据库/去重事务、Job/Worker、Scanner、通用迟到 timeout reconcile、进程崩溃清理、容量、production 或 AC；严格布尔字段的 multipart 文本解析仍留待获批 Form Router 边界处理。
- 2026-08-12 已完成当前计划中所有可安全执行的 MVP-VS-01～05 离线/内部能力增量：VS-01 组合上传意图重验证、一次遍历有界缓存计量、格式与 clean-only 准入，FILE 七文件 144 项通过；VS-02 新增组织隔离、软删除过滤、基准日期确认协议 header、精确 Decimal/冻结 JSON 的 PostgreSQL 财务只读 Repository；VS-03 新增逐字税号/名称/日期三态候选与稳定原因/排序，并由最小 Application Service 把 VS-02 指定发票和全部合同逐字映射到候选引擎，当存在生效补充协议但字段级投影不可得时失败关闭，候选与 Service 聚焦套件 79 项通过。accepted 009 数据库目录含 Repository→Service 组合路径，在当时 PostgreSQL 16.14 checkpoint 连续两轮各 80 项通过。VS-04 新增调用方显式元数据的 RULE-001～015 离线预览、四态语义、确定性风险与 AI-disabled，组合套件 462 项通过；VS-05 新增固定字段/行顺序、安全文本、精确 Decimal 字符串和字节稳定 JSON 报告负载，聚焦套件 129 项通过。上述均不是业务 API、Worker、浏览器 E2E 或正式 AC；最新矩阵计数见后续 `TEST-001` 记录。
- VS-02 仍存在最小技术阻塞：accepted `20260807_009` 只有 `supplementary_agreements` header，没有字段级 supplementary-agreement change 物理载体，故 Repository 不能从 PostgreSQL 生成 `SupplementaryFieldChange` 或把变更安全投影为合同有效字段。不得猜表、JSON 键或持久化语义；当前仅返回已确认且在 exact baseline date 生效的 header，并继续复用独立纯领域投影。该缺口只使 VS-02 保持 `partial / minimal BLOCKED`，不阻断其他 READY 离线切片。
- `FILE-002` 当前为 `PARTIAL / S1`：后端冻结 `pending/clean/infected/scan_failed/unsupported/not_configured` 六态，只有经过 Pydantic 校验得到的 `clean` 枚举实例可进入纯解析准入判断；Scanner、状态转换、错误码映射、数据库/Job/Worker 和生产扫描仍未实现。
- `AUD-003` 当前为 `PARTIAL / S1-S9 + MVP-VS-04`：RULE-001～RULE-015 的 15 条纯谓词均已有严格类型、缺失事实、适用性和关键边界单测，离线组合服务会各调用一次并生成稳定四态结果与 pending 风险；调用方事实、规则元数据与引用仍须显式传入，AI 固定 disabled。输入 Schema、实现键/代码哈希、规则注册发布和 RULE-001 多版本投影仍由 GAP-066 跟踪；数据库持久化、正式任务执行器、API 与 AC-007 运行链路未实现。
- `AUD-005/REP-003` 已在纯风险汇总和七类表格公式前缀保护上增加离线预览风险生成与稳定报告负载：hit rule、risk ID、原始/有效等级和 pending 状态强制一致，JSON 字段/行顺序、Decimal 字符串、引用、降级/过期标记及敏感 repr 已验证；此前报告负载聚焦套件 129 项通过。2026-08-13 又交付两个内部离线 Writer：`report-xlsx-writer-v1` 以锁定的 `XlsxWriter==3.2.9` 将冻结 `ReportPayload` 生成固定 `Summary/Rules/Risks` 三表、有界且无公式/超链接/外部关系/宏/媒体/绘图的 XLSX 字节；`report-pdf-writer-v1` 以锁定的 `ReportLab==5.0.0` 生成固定 A4、有逐页非正式预览标识、可见控制符转义、仅未解析引用 ID、内嵌 `/ToUnicode` TrueType 字体且无主动内容的有界 PDF 字节。PDF 使用 AOSP `android-15.0.0_r25` 两份字体、原始 NOTICE/README 与本地来源记录，渲染前校验字体 SHA-256；5 个资源已通过离线 wheel 构建安装/入包，四份上游原始文件 exact hash PASS。PDF unit 9 项、PDF+XLSX 相关 80 项和代表性 3 页 A4 Poppler/pypdf/pdfplumber QA 通过；两种 Writer 的字节相等都只承诺同一运行环境。`REP-003` 仍为 partial；人工复核状态写入、数据库/`report_versions`、MinIO/下载、Auth、浏览器 Excel/PDF viewer、跨镜像复现、production 和 AC-014 均未运行或未实现。
- 配置加载的受支持安全入口是 `Settings()` 与 `Settings.model_validate*`。不得改用 `TypeAdapter(Settings).validate_json()` 解析配置：畸形 JSON 在进入模型前失败，模型内脱敏器无法拦截其原始输入；项目当前没有该调用路径。
- 制度撤销已由 `CR-028-R1` 闭合为 audit_reviewer 请求、system_admin 独立执行和 related record 一一绑定；`published→revoked` 是唯一新边，普通新检索排除 revoked、历史事实保留。archived 仍未开放。
- 统一异步 202 `AcceptedJobResponse`、状态动作乐观锁和 RAG 权限过滤顺序尚未统一；文件安全扫描六态已由 CR-001 关闭。
- AI 发票明细字段与页面仍不一致；具体 Provider 模型、Embedding 维度、价格、IP allowlist 和目标环境容量仍是环境签署值，不得由 contract 值替代。
- `GAP-065` 跟踪文档链三项独立差异：数据库 `block_type` 含 `quote` 而 PARSE-003 过滤漏值；DOC-003 要求块坐标不可得原因可追踪但 `document_blocks` 未冻结原因载体/必填条件/查询投影；Markdown parser、HTML 白名单和复杂表格格式仍由 `TBD-013/ARC-TBD-004` 待裁决。不得自行扩展枚举、猜 `metadata_json` 键或选 parser；`TBD-018` 与 `ARC-TBD-005` 不得被扩大成所有解析路径的前置。
- `GAP-066` 跟踪 AUD-003 真实发布差异：15 条纯谓词不等于已冻结 registry。未批准的 `CR-016-R1` 推荐七类/15 行静态元数据、仅五条无主合同规则、封闭 Draft 2020-12 输入 Profile、稳定 key→adapter、release-bound canonical reason、key-bound 双源文件 hash、0/exact-15 显式 publisher 和 RULE-001 全版本/current 目录；它不新增 API/表/migration/P0，获批并同步后仍必须等待 `BASE-005、AUD-002` 才可实现或发布。
- `GAP-067` 跟踪 UI-002/FE-002 数据来源差异：五角色工作台、最近任务和失败 Job 需要刷新后由 Backend 恢复且禁止前端全量拉取，但 AUDIT-001 排除 `system_admin`、摘要键未冻结，OPS-001 又只支持已知 `job_id`，正式 API 没有权限裁剪的 Job/dashboard discovery。合同未裁决前不得枚举 Job ID、编造 Mock DTO 或用孤立组件宣称工作台完成。
- API 与部署文档对 `/health/dependencies` 的字段名、状态枚举和依赖集合不一致；`DEP-001` 完成前不实现该探针，`DEP-004` 前必须关闭 `GAP-031`。
- 2026-08-12 BOSS 已批准并同步 `auth-mvp-v1 + p0-permissions-v1`，关闭 GAP-038/039；Backend 已实现 Argon2id 密码边界、五次锁定、Ed25519 Access/强制换密 Token、7/30 天绝对 Refresh 旋转/重放处置、五 Auth API、统一数据库 Actor 与 26-code/五角色/deny-overrides，Frontend 已替换 `signInMock/sessionStorage` 为内存 Session、single-flight refresh、generation-safe replay/logout 和 permission UX。当前 authenticated financial read API PostgreSQL 组合测试已生成真实临时 Ed25519 PEM/JSON 文件，经 keyring loader 和 `create_app` lifespan 在零 `dependency_overrides` 下构造 AuthService 与七条财务读链所用五个 QueryService；随后真实 login/JWT 形成 `Actor + financial.read`，依次通过七个已冻结 Router/Service/Repository 读取同一 PostgreSQL，并验证 logout 后再次 refresh 返回 401 `AUTH_REFRESH_EXPIRED`。ASGI `https://testserver` 模拟下还验证登录响应 `no-store` 与 Refresh Cookie 的 `Secure/HttpOnly/SameSite=Strict/Path=/api/v1/auth`。该用例不证明真实 TLS、Nginx、production secret mount/文件 ACL、部署、浏览器或 AC；显式本地浏览器 Gate 后续也已消费同一 keyring loader/`create_app` lifespan，并由 bootstrap 构造 Service，见后续专门记录。用户创建/启停/重置/角色替换、break-glass Application runtime、operation-log 审计链，以及其余业务 Router 的 permission/object-state/SoD 消费仍未实现，production 与 AC-001 仍未运行。
- Auth application startup 已修复 Service 构造失败时的 Engine 泄漏边界：lifespan 的 `try/finally` 现覆盖 keyring 之后的 Engine 与 Service 构造，新增回归确认 AuthService 构造异常仍精确调用一次 `engine.dispose()`。
- 2026-08-12 已交付 `invoice-list-read-v1 → invoice-detail-read-v1`：Backend 的 `GET /api/v1/invoices` 使用 canonical keyset cursor、组织与软删除边界读取稳定摘要，`GET /api/v1/invoices/{invoice_id}` 对不存在/跨组织/软删除统一 404；公开 DTO 使用严格枚举、Decimal 字符串和详情 BIGINT 字符串，不暴露内部组织、供应商、证据哈希或操作人事实。Frontend 已移除发票 Demo fixture/假筛选/指标/主合同，真实列表用 cursor 栈翻页并以 canonical UUID 进入真实详情，具备严格解码、分页不变量、加载/错误/Trace 重试、Abort 与迟到响应保护；筛选/汇总、证据/重复处置/关联/供应商/历史区块和所有写动作仍未接通。该切片收口 checkpoint 新增真实 `InvoiceQueryService → FinancialReadRepository → PostgreSQL` 列表/详情组合测试，但当次完整离线运行未设置 `TEST_DATABASE_URL`，这些测试安全跳过，且当时既有 PostgreSQL 81/81 证据早于新增用例；后续当前 PostgreSQL 证据见 `TEST-001` 记录。显式本地浏览器 Gate 后续已覆盖该读链；production 与 AC-005/007 仍为 `NOT_RUN`。
- 2026-08-12 已交付 `contract-list-read-v1 → contract-detail-read-v1`：Backend 以 `effective_date DESC NULLS LAST + id DESC` canonical keyset cursor、数据库 Actor、`financial.read`、组织与软删除边界读取 accepted `contracts` 当前持久化行；详情对不存在/跨组织/软删除统一 404，严格 DTO 不暴露组织、供应商、关键哈希或操作人，也不把主合同原始字段冒充补充协议生效后的有效字段。Frontend 已移除合同 Demo fixture、假筛选/指标/证据/供应商/附件/历史，以真实 UUID 列表进入详情并具备严格解码、分页不变量、加载/错误/Trace 重试、Abort 与迟到响应保护；写入和补充协议有效字段投影仍禁用或不展示。`ContractQueryService → FinancialReadRepository → PostgreSQL` 组合测试在默认离线运行因未设置 `TEST_DATABASE_URL` 而跳过，随后已纳入 current-head 88/88 双轮 Gate；显式本地浏览器 Gate 后续已覆盖合同详情、补充协议原始 Header 与当前主发票读链，production 与 AC-003/004 仍为 `NOT_RUN`。
- 2026-08-12 已交付 `invoice-primary-contract-read-v1`：Backend 的 `GET /api/v1/invoices/{invoice_id}/primary-contract` 以数据库 Actor、`financial.read`、发票锚点、合同/发票双组织和关系/合同/发票三端软删除边界读取 accepted 009 条件唯一索引保护的当前 `confirmed_primary`；无主合同为 200 `null`，不可见发票统一 404，严格 DTO 不公开任意 `match_reasons_json`、关系/Actor/历史或写语义。Frontend 由真实发票详情进入主合同页，展示真实合同或空态并可继续进入合同详情，移除了顶级入口和全部 Demo 候选/冲突/写模拟。原拟当前关系列表因 009 缺少匹配 `invoice_id + contract_id + id` 的有界读取索引而主动收窄，未占用被候选 lineage 保留的迁移序号。该 PostgreSQL 组合用例在默认离线运行因未设置 `TEST_DATABASE_URL` 而跳过，随后已纳入 current-head 88/88 双轮 Gate；显式本地浏览器 Gate 后续已覆盖当前主合同读链，但候选查询/解释、确认/替换/取消/历史、production 与 AC-006 仍未实现或 `NOT_RUN`。
- 2026-08-12 已交付 TECH §6.12 `supplementary-agreement-header-list-read-v1`：Backend 的 `GET /api/v1/contracts/{contract_id}/supplementary-agreements` 以可见父合同为锚点，按 `effective_date DESC,id DESC` canonical keyset 读取 accepted 009 当前未软删除原始 Header；公开项仅含七字段，两个状态独立原样展示，不返回字段级 change、`row_version`、证据或有效性推导。Frontend 在真实合同详情内独立加载、分页并安全处理 401/403/404/Trace/Abort/stale/valid→invalid；它不声明协议已生效、适用或进入审核快照，写操作保持禁用。
- 2026-08-12 已交付 TECH §6.13 `contract-primary-invoice-list-read-v1`：Backend 的 `GET /api/v1/contracts/{contract_id}/primary-invoices` 先验证可见父合同，再使用 accepted 009 `(contract_id,invoice_id)` active-pair 条件唯一索引按 `invoice_id ASC` keyset 有界读取当前 `confirmed_primary` 发票摘要；DTO 复用严格发票列表项，不返回关系 ID、候选、匹配依据、历史或写语义。Frontend 从真实合同详情进入 `/contracts/{uuid}/primary-invoices`，严格校验 exact 列表、ID 升序及 canonical base64url UTF-8 JSON `{id,v}` cursor，并可继续进入真实发票详情；UUID 顺序仅用于稳定遍历，不表示业务时间。
- 2026-08-13 显式本地 `financial-read-browser-v1` Gate 已升级并 PASS：test-only 同源宿主 `backend/tests/manual_financial_read_browser.py` 继续使用精确授权 token、受限 loopback 端口、逐字 `http://127.0.0.1:{port}` origin、`frontend/dist` 完整性预检及显式可丢弃 PostgreSQL marker 五重失败关闭门禁；运行时生成临时 Ed25519 PKCS8 私钥与 public keyring，经生产 keyring loader 和 `create_app` lifespan 构造 AuthService 与七条财务读链所用五个 QueryService，并断言 `dependency_overrides=0`、六个 Service state 在 lifespan 前不存在且进入后为真实类型。运行中发现并修复 `frontend/src/services/api.ts` 默认原生 `fetch` 丢失 receiver，现以 `globalThis.fetch.bind(globalThis)` 绑定并由 `frontend/tests/api.test.ts` 回归覆盖。真实浏览器路径 `login → dashboard → invoice list/detail → primary contract → contract list/detail + supplementary raw header → contract primary invoices → invoice detail` 对应七条只读 API，精确读取隔离 PostgreSQL 合成事实，0 alert、0 console error；深链 reload 保持 URL 与用户，logout 后 reload 保持匿名且未出现远端退出状态未确认提示。正常 `CTRL_BREAK` 后 OwnedSyntheticRows、临时 key 目录、宿主进程和专用容器均为 0。它与默认离线编排分离，后者仍固定 `BROWSER_E2E=NOT_RUN`；本证据仅适用于 loopback HTTP，不证明 TLS/Nginx、production secret mount/文件 ACL、部署、强杀/断电清理或任何 AC。
- Uvicorn 0.52.1 的端口 bind failure 发生在 lifespan startup 之后；该失败分支依赖显式 shutdown 才清理已播种的合成事实，不能声称“bind 前无副作用”。
- 2026-08-12 BOSS 已批准并同步 `OQ-09: archived-reupload=conflict-v1 + local-minio-v1`，关闭 GAP-050：同组织相同 SHA-256+大小命中未软删除 archived 文件时，单文件稳定返回 409 `FILE_ARCHIVED_DUPLICATE`，不得复用、恢复或创建新文件/Job/业务对象/MinIO 副本。local MinIO 固定 tag+manifest digest、内部 HTTP 和七 Bucket；现已实现严格 MinIO Settings、quarantine-only Adapter、服务端 UUID 对象键、pending intake、文件类型权限门禁、单服务 loopback Compose 及独立应用身份/Bucket bootstrap。Adapter 在 SDK 实际消费上传流时校验精确长度/SHA 并写摘要 metadata；PUT 未正常返回时会尝试删除独占键，但无论该次删除是否返回成功都以脱敏 cleanup-required 错误携带只读 locator，不能把一次 DELETE 冒充通用迟到 PUT reconcile；已完成 PUT 后的内容错配仅在补偿删除失败时升级为 cleanup-required。除既有离线/静态门禁外，显式 real local MinIO Adapter Gate 已 PASS，证明合成对象的真实 quarantine PUT/读取核验/DELETE 与两个确定性补偿路径。完整 MVP-VS-07 仍因 files/knowledge_bases 物理 Schema、FILE multipart/202/幂等/错误合同、具体 Handler Registry/Input Schema、GAP-052 与 Scanner Profile 而 `BLOCKED`；该 Gate 不证明 FILE Router、数据库/去重事务、Job/Scanner、通用迟到 timeout reconcile、崩溃清理、容量、production 或 AC-002/016。
- 2026-08-13 的 `local-minio-restart-persistence-v1` 在修复 MinIO 策略 `Action/Resource` 集合响应顺序不稳定导致的 `APPLICATION_POLICY_DRIFT` 后，连续两轮显式真实 Gate PASS；失败轮不作为通过证据。每轮均 bootstrap 七个 Bucket 与 quarantine 权限，以原应用身份写固定合成 PDF 的精确 quarantine 对象键，root 核验逐字节内容、大小、Content-Type 与 SHA-256 metadata；不带 `-v` 地 `compose down` 后以 `up --pull never` 创建新容器，原 root/应用身份继续有效，root 再核验后由原应用身份删除并由 root 确认不存在。start/stop/wrapper 均 PASS；最终专用容器/网络为 0，managed/root/app/gate 环境变量均不存在，named volume 保留。该证据只证明单服务本地 named volume 上一次受控冷重启的固定对象持久性与同一应用身份连续性；不证明 FILE 数据库/Router/Job/Scanner、通用迟到 timeout reconcile、崩溃恢复、一般耐久性/容量、备份恢复、完整 Compose、TLS、production 或 AC-016，状态和 AC 不升级。
- `BASE-005` 的正式完成范围是 57 张 PostgreSQL 核心物理表、约束、触发器、五角色幂等种子和版本化种子机制，不能用 Alembic 脚手架、扩展迁移或合同批准替代。
- `BASE-005` 当前正式接受的 storage-schema 为 18/57：009 在既有 15 表上增加空的 `supplementary_agreements/invoice_items/contract_invoices`；较早的 PostgreSQL 16.14 current-head checkpoint 为双轮各 81 项，最新目录在 startup cleanup 修复后最终干净重跑连续两轮各 89 项通过。当前目录覆盖财务只读 Repository、Repository→候选 Service、Auth Repository→Service→HTTP、补充协议 Header、合同当前主发票、同组织用户列表，以及临时 Ed25519 文件→keyring loader→`create_app` lifespan→零 override Auth/Query Service→真实 login/JWT→Actor/`financial.read`→七 Router→同一 PostgreSQL→logout 后再次 refresh 返回 401 `AUTH_REFRESH_EXPIRED`→`system_admin` 重新登录→Actor/`users.manage`→用户 Router/Service/Repository 的组合路径。首次未通过尝试不是有效证据，当前结论只采用最终修复后的双轮 PASS。该实现不播种默认组织、管理员、密码、真实规则、业务数据或真实 ACL；ASGI HTTPS 模拟不证明 TLS、Nginx、production secret mount/文件 ACL、浏览器用户列表、部署、用户写 Service/API、Worker 或完整 `BASE-005/AUTH-005`。
- 破坏性迁移测试必须同时满足本机地址、`finaudit_*_test` 专用命名、URL 无 query 参数、显式重置确认和数据库可丢弃注释，不能仅凭名称含 `test` 执行 `downgrade base`；query 中的 `host`、`hostaddr`、`dbname`、`service` 等 libpq 参数可能覆盖实际目标，必须 fail-closed。
- `BASE-005` 的原合同阻断已由 CR-001 关闭；CR-003-R3 已交付并通过 008 两空表 PostgreSQL storage-schema Gate，CR-004-R2 交付 007，CR-012-R3 交付 006，009 已交付三个无歧义财务关系 root 表。当前尚缺 39 表；除合同/发票列表与详情、发票当前主合同、合同当前主发票及父合同范围补充协议 Header 只读切片外的业务跨表链、写 Repository/Service/API、Worker、bootstrap、Compose 与最小权限生产演练仍未完成，因此 `BASE-005` 和 `AUTH-005` 继续为 `partial`。GAP-064 继续独立阻断供应商 API、确认/复用、纠错和 AI generic tax 运行时；未批准的 CR-015-R2 不能替代该合同。
- `BASE-006` 当前只完成离线消息/路由与通用数据库会话基础：消息体严格为 `job_id + event_schema_version=1`，Trace Context 走 Header；Celery 使用七个精确 `app.workers.tasks.<logical_queue>.execute_job` 路由，未知 task 固定失败关闭，并设置 `task_publish_retry=false`、JSON/UTC、late ack、Worker 丢失重投和忽略结果。`app/db/session.py` 只提供惰性 PostgreSQL Engine/Session 工厂，复用 Settings DSN 校验、池参数、隐藏 SQL 参数和 10 秒连接超时，事务仍由 Application Service 控制；显式 `TEST_DATABASE_URL` 集成探针复用迁移 Gate 的 loopback、专用命名、无 query、确认值及可丢弃 comment 门禁，仍不能替代业务 Repository。Job/Step/Outbox 空表 DDL/ORM 已由 `BASE-005` 交付，当前授权仍不包含 Repository、Task、Dispatcher、Lease、Redis/Broker/Worker 集成、真实 Job 或崩溃恢复 runtime，状态保持 `partial`。
- 2026-08-13 交付 `handler-registry-loader-meta-v1`：通用 Loader 只接受调用方显式传入的 Registry Schema/Registry 与精确 Input/Summary Schema bundle，严格验证 UTF-8/JSON、整数、Draft 2020-12、JCS/raw SHA-256、精确 bundle、排序、scope→step、summary 引用及可解析的本地 `$ref/$dynamicRef`，并禁止远程 retrieval；测试目录 6 份 synthetic JSON 与 45 项聚焦测试只证明此 meta 能力。本实现没有 Settings/package data/callable/Job/Celery/runtime import；生产 Registry/Schema/Input/Summary/Handler 制品仍为 `PENDING / NOT GENERATED / NOT APPROVED`，Job/Worker runtime 仍为 `NOT AUTHORIZED`，因此 `BASE-006/FILE-001/TEST-001`、矩阵和全部 AC 不升级。
- `CON-003` 当前完成补充协议有效字段的纯领域投影、PostgreSQL 财务只读 Repository，以及父合同范围的原始 Header API/UI：纯投影对协议/变更确认、生效日、同日冲突、递归冻结和有限 Decimal 失败关闭；Repository 对 accepted 009 事实实施组织隔离、软删除、稳定排序、脱离 Session 的冻结视图和敏感 repr 隐藏。由于 009 没有字段级 change 载体，数据库视图尚不能调用纯投影生成有效合同字段；显式本地浏览器 Gate 只覆盖原始 Header 展示，SAGR 写状态转换、有效字段 API、过期传播、审核快照、完整 CON-003 浏览器 E2E 与 AC-004 仍未实现。
- 2026-08-12 较早的完整 `verify-local-offline` accepted-source checkpoint 覆盖当时 MVP-VS-01～06、OQ-09 离线预备链、Auth 凭据响应 `no-store/no-cache`、Frontend 会话代际保护，以及合同/发票列表→详情与发票当前主合同只读切片：Backend `2175 passed / 67 skipped / 1 Starlette deprecation warning`、Ruff check/format 186 files、mypy 101 sources、Frontend 325 Vitest/124-module build。同期此前的显式 PostgreSQL 16.14 Gate 为两轮 81/81。该段仅保留历史 checkpoint；当前离线、PostgreSQL 与 MinIO 证据见后续 `TEST-001` 记录。
- `BASE-002` production route config regression 已验证 `AppEnvironment.PROD` 下 `/health` 返回 200，而 `/openapi.json`、`/docs`、`/docs/oauth2-redirect`、`/redoc` 全部返回 404；`test_app.py` 聚焦 20 项通过，独立审查为 `GO`。这只证明配置 seam，真实 production Policy、runtime 与 deploy 仍为 `NOT_RUN`。
- GAP-040～042 已由获批并同步的 `CR-004-R2` 闭合合同阻断；007 已实现三表 revision ownership、Job/Step/Outbox 状态/CAS、无残留 downgrade 和对应空表数据库约束，通用 Handler Registry meta Loader 已有 test-only synthetic bundle 的离线证据。生产 Registry/Input/Summary/Handler artifact 仍为 `PENDING / NOT GENERATED / NOT APPROVED`，上述合同、空表与 Loader 门禁都不等于 Handler/Job runtime 获批或实现。GAP-043～045 仍由未批准的 `CR-005-R1` 跟踪，批准和同步前不得为追求表数提前建表或开放 API。
- `TEST-001` 当前为 `PARTIAL / S1-S9`，矩阵为 `2 implemented / 39 partial / 45 planned`。当前 PostgreSQL 16.14 对 `20260807_009` / 18 表的完整数据库集成目录最新干净重跑连续两轮各 91 项通过，覆盖真实临时 Ed25519 keyfile、零 override application lifespan、authenticated 九条 financial read API 与 admin `users.manage` 用户列表组合路径；历史 007/13、008/15 与此前 009/80、81、87、88、89、90 只保留为旧 checkpoint。最新完整 `verify-local-offline` 仍是纳入 `invoice-exact-duplicate-candidates-read-v1` 的 Backend `2398 passed / 75 skipped / 1 Starlette deprecation warning`、Ruff check PASS、Ruff format 212 files、mypy 114 sources、pip check、Frontend typecheck/14 files/412 Vitest/129-module build PASS；新增两票点查只有聚焦离线与 91/91×2 PostgreSQL 证据，尚未重跑完整离线门禁。默认 Compose、浏览器、Provider、remote、部署和 production 均为 `NOT_RUN`。另行显式运行的本地财务只读浏览器 Gate 尚未覆盖用户列表、精确重复候选或两票点查；这些结果、两个离线 Writer 与 real local MinIO Adapter Gate 均不等于真实 TLS/Nginx、production secret mount/文件 ACL、测试账号交付、完整业务 Schema、FILE Router、数据库上传事务、Job/Scanner、通用迟到 timeout reconcile、正式报告/持久化/下载、浏览器 Excel/PDF viewer、跨镜像、强杀/断电清理、容量、部署、production 或任何 AC 通过，CR-011 R6 startup Gate C 仍独立为 `BLOCKED BEFORE RUN / NOT RUN`。
- `user-list-read-v1` 是最小只读用户管理链：Backend 以数据库 Actor、`users.manage`、Actor 组织和 active 组织边界读取未软删除用户，仅返回 `id/username/display_name/status/fixed_roles/row_version`，Frontend `UI-014` 使用同源 `/api/v1/users` 与严格 cursor/响应 decoder，删除 Demo 筛选、创建和 Break-glass 假事实，写动作继续不可用。Repository 当前在普通 PostgreSQL `READ COMMITTED` Session 中分三条语句读取用户页、`clock_timestamp()` 和固定角色，因此不是单一 MVCC 快照，用户 `row_version` 也不证明角色投影同快照。canonical cursor 可逆携带用户名，虽不授予权限也不是凭据，但原始 query/cursor 不得写日志、错误或 Trace；该日志边界尚无 operation-log/runtime 证据。浏览器用户列表、用户创建/启停/重置/角色替换、break-glass Application runtime、operation-log、production 与 AC-001 仍未完成或 `NOT_RUN`。
- `invoice-exact-duplicate-candidates-read-v1` 是 `INV-004` 的最小只读前置：Backend 以数据库 Actor、`financial.read` 与源发票组织边界，按当前持久化 `invoice_code + invoice_number + seller_tax_no` 逐字段数据库等值读取候选，排除源票、软删除与 voided 候选并保留 archived；Frontend 在真实发票详情中以严格 decoder、UUID keyset 和独立 loading/empty/error/retry/Abort 状态展示。`source_voided` 优先于 `incomplete_identity`，`ready + 空页` 只表示本次读取未命中；本切片不做 trim/大小写折叠/模糊匹配，不确认重复或真伪，也不写 `duplicate_status`、风险、审核任务或 operation-log。普通 PostgreSQL `READ COMMITTED` 的源锚点与候选查询不是单一 MVCC 快照，现有索引前缀和 UUID tie-break 不构成容量或性能证明；浏览器、production 与 AC-005 均为 `NOT_RUN`。因此 `INV-004` 只从 planned 升为 partial。
- `invoice-exact-duplicate-pair-read-v1` 在上述候选读取后提供两个已知 UUID 的有界重新核对：Repository 用一条同组织、未软删除、非 voided、三元组非 NULL 且逐字段数据库等值的自连接点查；不存在、不可见、自比较、失效或漂移统一 404。严格 API/Frontend decoder 保留数据库原始非 NULL 字符串（包括空白），详情页只在用户显式点击时核对，并以请求身份防止迟到响应覆盖。2026-08-13 聚焦证据为 Backend `76 passed / 17 skipped / 1 warning`、Ruff/format/mypy PASS、Frontend `66 passed` 与 typecheck PASS；随后 PostgreSQL 16.14 current-head 91 项连续两轮 91/91 PASS，专用标签残留容器为 0。浏览器、production 与 AC-005 仍为 `NOT_RUN`。它仍不确认重复、不写 `duplicate_status`、风险、审核任务或 operation-log，`INV-004` 保持 partial。
- `GAP-068` 跟踪 H-root 六表的合同阻断：执行状态机没有任何进入 `failed` 的合法边，AUDIT-008 又返回数据库/SRS 闭集之外的 `cancel_requested`；终态更新白名单、high 风险完成门禁依据、跨父对象一致性、报告状态迁移与 downgrade 锁序也未冻结。必须先以 Request 修订或 CR 闭合这些合同，不得只按列定义生成缺少触发器和跨表约束的 010 Schema。
- S7 在 Frontend 子进程设置 `FINAUDIT_LOCAL_OFFLINE_QUALITY=1`，使 Vite `envDir=false` 且不加载 `frontend/.env*`，结束后恢复原值；Backend 配置负例会使用 `127.0.0.1` 本机网络栈做预期失败连接，编排输出必须单独披露，不能笼统声称“完全没有 socket”。Ruff accepted-source 范围必须精确固定为 `app`、`alembic/versions`、`tests`；`backend/alembic/recovery` 匹配 0 个 accepted files、格式门禁为 `NOT_APPLICABLE`。9000-byte verifier 的 SHA-256 为 `455ba2ea80534d594dabce6869bc8bf441892ac3f36414783d1c47df90a94f43`，负向自测必须封堵动态 scope 增宽、额外 Ruff 调用和伪造失败原因。
- `TEST-001` 使用两个独立版本轴：`dataset_version=1.6.0` 只跟踪四个 JSON fixture，S6 新增表格单元格角色覆盖与用户伪造候选 ID 两个结构化负例；`binary_fixture_contract_version=1.0.1` 跟踪 14 个二进制资产及其 manifest 契约。`1.0.1` 修正 C-001/C-002 主体名称和 P-001 多文档名称但不改变 ID、路径、场景或映射；生成器从已受 Request 门禁保护的 `core_business.json` 读取 C-001 名称/税号和 P-001 名称，二进制语义需要 JSON 门禁与精确 Python 生成器 `--check` 共同证明。
- 二进制 fixture 的逐字节复现固定使用 Python 3.12.13 和 `scripts/requirements-test-assets.txt` 中的精确依赖；其他 Python 版本会在生成前失败，不得据此运行 `--write` 覆盖基线。
- `AI-001` 当前保持 `PARTIAL / S1-S10`：在 S1～S8 的 provider-neutral 内存契约、显式目标单次分派、路由、Pricing/Budget、最坏 Token/费用 preflight、usage 对账和离线 Fake 上，S9 新增 `provider_calls_enabled=false` 的严格 pre-hash Policy payload 与 Settings 映射，S10 新增六类 purpose 到 `rag/async_generation/embedding` 三个独立限流池的不可变纯映射。Policy 校验错误不保留原始输入，JSON 重复 key、布尔/整数混用和 external HTTP 失败关闭；错误分类已补齐 CR-002 的四类结果和 5xx retry 语义，preflight 显式保留未来 repair 格。已批准并同步的 `CR-011-R4` 继承 R3 的 16 个 contract-offline JSON、Policy/Event Schema 与 companion 表示；Fake Sink 在 completed/late_completion 记录或签发 permit 前复用完整事件链校验，跨事件字段不一致返回 `CONFLICT`，且不记录冲突事件、不签发 permit。当前 events 47 项与内存 sink 22 项合计 69 项；Stage1 已以 275/275、negative runner PASS、zero-socket attempts=0 验证 strict loader/parser/companion/resolver、retry、response boundary、network policy、Event DTO/JCS/hash/replay 与 Fake Sink 行为，完整 Gate B 又以 27/27、negative vectors 25/25、Schema calls 20+20、engines=2/2、zero-socket attempts=0 与 anti-forgery 11/11 通过。AI-005 现在另提供同一 Port 的持久适配 primitive，24 个聚焦用例和 PostgreSQL AI scope 证明许可恢复边界；但 Gateway、真实 deadline/HTTP Adapter、Redis、Provider 网络、共享事务业务结果采用与 production 仍未运行或授权。预算、Policy 和限流池选择子集仍不调用数据库、Redis、网络或睡眠；不得访问真实 Provider。
- `AI-002` 当前为 `partial`：离线 repair orchestration 在本地严格结构校验失败后最多对同一实际目标修复两次，只传安全 JSON Pointer/类别并共享 attempts、Token、费用和 hard deadline；完整业务/权限/引用 validator 仅接收 `deep=True` 的 `model_copy`，其原地修改和返回值都不能替换严格解析对象。coroutine、同步 generator 或 async-generator validator 在 initial/repaired 路径都失败关闭、主体不执行，固定映射为 `INTERNAL_ERROR` 且无 cause/context；仅精确的 `CitationValidationError/RiskExplanationValidationError` 原样透传，repair 调用异常固定映射为 `MODEL_UNAVAILABLE`。structured output 46 项与 structured repair 56 项合计 102 项通过；四个 AI 测试文件合计 241 项（46 + 56 + 25 + 114）。Provider、数据库、Redis、持久化 EventSink、业务 extraction Schema、Service/API、真实网络与 AC 均为 `NOT_RUN`。
- AI 审计所有权合同已闭合：AI-001 定义脱敏 `AiCallEventV1` 并依赖 `AiCallEventSink` Port；Stage1 已实现 Event DTO 与内存 Fake Sink 行为证据。AI-005 独占的发送前 durable reserve、complete、Outbox、`ai_call_logs` 幂等投影和 `outcome_unknown/late_completion` 已形成 provider-neutral PostgreSQL runtime，并通过 `DurableAiCallEventSink` 把状态安全映射回同一 Port；当前只提供内部安全汇总，不注册尚未冻结权限/DTO 的 OPS-005 Router。Gateway 与共享事务业务采用未接线，真实 Provider 验收继续受环境审批阻断。
- 2026-08-17 BOSS 先以 `CR-021` 批准 local/test 的 Chat+百炼 Embedding 组合 Profile，随后以 `CR-022` 将其升级为 `minimax-m3-bailian-qwen37-local-v2`：Chat 继续固定 `MiniMax-M3`/USD，Embedding 固定百炼 OpenAI-compatible `qwen3.7-text-embedding`/CNY、1024 维、单批 20。Backend RAG 与 Worker 索引/检索评测统一经 `EmbeddingRuntime -> AiGateway -> openai_embeddings_v1`，索引持久化真实 adapter/model/dimension 身份；Hash 与百炼索引不得混用或原地覆盖。API Key 仍只在运行时 Secret 配置，不记录值。Event/Policy v2 和迁移 024 提供 no-fx 逐次费用审计。首次受限连通性 smoke 为 43 input tokens、2×1024 维、CNY 22 microunits；随后单独批准并消耗的完整知识库 E2E 为 7 次请求、3231 input tokens、CNY 1616 microunits、15 个索引成员、Top-5 返回 5、5/5 smoke、7 个 completed attempt/14 个 Event v2。索引遵守至少 100 条 `formal_release` 门禁并保持 `ready`，临时 Collection 与 PostgreSQL/Qdrant 容器均已清理。经审批 50/100 条代表性质量、production 和正式 AC 仍未完成，任何后续 Provider 调用都需新授权。
- 2026-08-17 BOSS 将 `synthetic-policy-corpus-v1` 的 local/test 技术语义复核委托给 Agent；`synthetic-benchmark-owner-delegated-review-v1` 处理 18 条队列并复核 20 条合理 no_answer，100/50 技术集可在可丢弃环境批准，但固定 `human_review_claimed=false`，不构成业务代表性或人类 UAT。批准的单次付费运行完成 36 条款两批索引并达到 `ready`，在 50 条门禁以 `MVP-UAT-050_EVALUATION_FAILED` 停止；100 条和激活未运行，无重试且 Collection/容器清理为 0。精确 usage 未留存，只能声明最多 52 请求、50000 input tokens、CNY 1 元；后续 Provider 必须重新取得明确单次授权。Chrome 原生登录/工作台 Tab 和 skip-link 局部路径通过；Edge、屏幕阅读器和全路由矩阵未运行。LibreOffice 26.2.5.2 已安装，但 Calc 对最小/项目 XLSX 均挂起，GUI 直接启动也未暴露窗口；测试进程/临时制品清理为 0，不能标记 AC-014 兼容性通过。

- 2026-08-18 当前前向事实：`CR-027-R1` 已实现资源/Job 双 row-version、审核技术重试/取消和文件同 Job 重试；`CR-028-R1` 已实现制度两阶段撤销；`CR-010-R2` 只批准可丢弃测试数据库中的 366-byte `fixed_test` Profile。普通 local 的 Scanner Profile 表默认空，PARSE-006 仍 503；只有测试显式安装 Profile 并注入无网络 Scanner/内存 Asset 存储时运行，不得外推到真实 Scanner、MinIO Asset、staging 或 production。
- 当前 Alembic head 为 `20260818_027`，ORM/catalog 为 58/58 张表；PostgreSQL 16.14 File 16、Audit 4、Retrieval/Policy 10 和完整 153 项连续两轮通过。OpenAPI 当前为 98 个唯一 operationId；CR-027/028 的 delta `+1/+2` 正确，但其 R1 绝对 pre-count 92 遗漏三个已授权 CR-005 operation，只保留为签署历史输入。

上述差异只阻断明确受影响的切片。改变产品范围、外部兼容性、持久化语义、状态/权限/安全边界或验收阈值时使用简短 CR；其余去重、实现补齐和内部设计直接更新对应事实源。开放问题见 `Request/IMPLEMENTATION_PLAN.md`，详细历史见 `docs/baseline-gaps.md`。
