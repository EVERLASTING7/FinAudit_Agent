#!/bin/sh

repo_root=$(git rev-parse --show-toplevel) || exit 1
exec powershell.exe -NoProfile -ExecutionPolicy Bypass \
  -File "$repo_root/scripts/verify-git-governance.ps1" \
  -CommitMessageFile "$1"
