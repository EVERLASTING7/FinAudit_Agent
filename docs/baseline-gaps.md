# 基线差异历史账本

状态：历史审计资料，不是当前开发入口。旧计数、CR 链和关闭记录仅供追溯，不是产品数量合同或实现前置。

当前活跃规范见 `Request/README.md`。实际阻断已收敛到 `Request/TECHNICAL_SPEC.md` 第 15 节和 `Request/IMPLEMENTATION_PLAN.md` 的 OPEN QUESTIONS；它们只阻断写明的最小切片。下表保留旧问题的发现过程和证据来源，但其中的旧文件行号、候选设计、签署形式和固定数量不得覆盖当前产品需求或机器事实源。

## 阶段阻塞级契约

| ID | 差异 | 当前安全口径 | 最迟关闭阶段 |
|---|---|---|---|
| GAP-008（CLOSED 2026-08-14） | 架构要求“一知识库一个稳定 Qdrant Collection”，数据库要求按“环境 + Embedding 模型/维度”建 Collection | `CR-018-R1/RFV1-D-003` 已批准并同步：Collection 身份固定为环境+Embedding 模型身份+向量维度，名称由配置显式注入且不可猜测；索引版本和成员仍由 PostgreSQL 管理 | CLOSED；019 Schema、索引运行时与真实 Qdrant Gate 已验证，production/AC 未运行 |
| GAP-009（CLOSED 2026-08-07） | 原记录误把架构的“原子切换活动索引 ID”和 API 的活动版本入参视为第二个持久化事实 | SRS 1278 与数据库 1086～1091 已统一规定 `document_index_versions.status='active'` 条件唯一和事务切换；架构 1230～1238 与 API 6001～6004 均可由该事实派生，不需要 CR，也不授权重命名 API 字段 | CLOSED |
| GAP-010（CLOSED 2026-08-07） | 架构要求 Job 阶段历史和幂等输入，原数据库契约缺少相应事实 | `CR-001-R2/D-002` 已批准并同步：`async_jobs` 持久化版本化输入、Lease/心跳/幂等键，`async_job_steps` 追加写尝试与阶段历史；技术重试沿用同一 `job_id`。关闭合同缺口不代表 `BASE-006` 已实现 | CLOSED |
| GAP-011（CLOSED 2026-08-07） | 上传意图原先在文件与 Job 中没有可靠持久化位置 | `CR-001-R2/D-003` 已批准并同步：上传分类、知识库和自动处理意图进入 PostgreSQL，并以四种去重矩阵冻结复用语义；关闭不代表 FILE-001 已实现 | CLOSED |
| GAP-012 | 评测导出返回非 `audit_reports` 的 `report_id`，数据库无对应制品表和查询/下载接口（API 8190～8198、8234～8236；数据库 1442～1461） | 不复用审核报告表；先补导出制品契约 | EVAL-007 前 |
| GAP-013（CLOSED 2026-08-07） | 强制换密原先缺少字段、受限 Token 和完成动作合同 | `CR-001-R2/D-004` 已批准并同步 `users.force_change_on_login`、五分钟一次性 `password:change` Token、AUTH-010 与重放/过期错误；关闭不代表认证实现或 AC-001 通过 | CLOSED |
| GAP-014（CLOSED 2026-08-07） | 文件安全扫描状态枚举原先跨文档冲突 | `CR-001-R2/D-005` 已批准并同步六态 `pending/clean/infected/scan_failed/unsupported/not_configured`；仅 `clean` 可进入解析，生产扫描依赖 fail closed | CLOSED |
| GAP-015 | AUTH-006 与 AUTH-008 都能完整替换用户角色，但并发和 `row_version` 语义不同（API 841～862、1021～1042） | 冻结一个写入口，另一个删除或改为委托同一服务 | AUTH-004 前 |
| GAP-016（CLOSED 2026-08-07） | break-glass 原先缺少批准对象、批准人资格、职责分离和动作合同 | `CR-001-R2/D-006` 已批准并同步独立 `break_glass_requests` 与 AUTH-011～AUTH-015；批准人必须是不同的有效 `system_admin`，临时角色限 allowlist，时长 `1..14400` 秒，禁止续期或原地改写 | CLOSED |
| GAP-017 | 制度撤销/归档要求审计确认和管理员执行，但缺少创建批准记录的动作和稳定参数（API 6075～6102、7309～7328） | 撤销/归档接口实现前补完整双角色状态机 | KB-003 前 |
| GAP-026（CLOSED 2026-08-07） | 真实审核规则种子原先在 BASE-005 与 AUD-003 之间所有权冲突 | `CR-001-R2/D-008` 已批准并同步：BASE-005 只写五角色与版本化种子机制，真实规则由 AUD-003 在实现键和代码哈希存在后发布 | CLOSED |
| GAP-027（CLOSED 2026-08-07） | 组织级分块配置原先缺少 bootstrap 与精确首版参数 | `CR-001-R2/D-009` 已批准并同步：离线 bootstrap 后由 KB-004 发布 `markdown_ast_structural/700/1200/50/100`，无已发布配置时失败，禁止隐式环境默认 | CLOSED |
| GAP-022（CLOSED 2026-08-07） | AI transport 的 deadline、尝试、5xx、`Retry-After` 与上下文处置原先跨文档冲突 | `CR-002-R4` contract 已批准并同步逐调用 deadline、固定尝试序列、错误分类、退避和共享预算；当前 `AI_PROVIDER_CALLS_ENABLED=false`，关闭合同缺口不授权 Provider 网络调用 | CLOSED |
| GAP-028 | `TEST-001` 的正式依赖只有 `BASE-001/004`，但输出和验收同时要求测试账号、独立 PostgreSQL/MinIO/Qdrant/Redis 环境及可重复环境重建；这些能力实际依赖尚未完成的业务 Schema、bootstrap 和 Compose（计划 1521～1533；测试 122～152；计划 1645～1661） | 已交付策略、86 项追踪矩阵、39 条合成 JSON、14 个确定性二进制样本、生成器、资产正负向门禁、默认离线质量编排与显式 PostgreSQL 16 Gate 的 `TEST-001-S1-S9`；另有独立 CR-011 Stage1 273/273 和完整 Gate B 27/27、engines=2/2、zero-socket attempts=0 证据，但它们不升级 S1-S9 编号。测试账号、完整业务 Schema、独立基础设施和可重复环境重建仍未完成，不能把 static/build/contract-offline 证据表述为解析/OCR/上传、注入防御、环境重建或任何 AC 通过 | TEST-001 完成前 |
| GAP-029（CLOSED 2026-08-07） | AI 调用审计与 OPS-005 所有权原先在 AI-001/AI-005 之间冲突 | `CR-002-R4/AI-D-006` 已批准并同步：AI-001 只依赖 `AiCallEventSink` Port；AI-005 独占 durable reserve/complete、Outbox、`ai_call_logs` 投影、补偿和 OPS-005 | CLOSED |
| GAP-030（CLOSED 2026-08-07） | 结构化输出的模型修复次数原先跨文档冲突 | `CR-002-R4/AI-D-007` 已批准并同步“本地确定性清理 + 最多两次模型修复”；清理不计修复次数，全部尝试共用总预算 | CLOSED |
| GAP-032（CLOSED 2026-08-07） | Chat/Embedding Provider 包络与出站边界原先未冻结 | `CR-002-R4/AI-D-001、AI-D-008` 已批准并同步非流式 Chat Completions/Embeddings、Bearer、固定 URL、TLS、禁代理/重定向、DNS/IP/peer 和字节上限；真实环境值及联网仍待另批 | CLOSED |
| GAP-033（CLOSED 2026-08-07） | fallback 必填性与跨 retry/fallback/repair 总预算原先未统一 | `CR-002-R4/AI-D-003、AI-D-004` 已批准并同步固定主备序列和统一请求/Token/费用预算；报告草稿由独立布尔控制，Embedding 无 fallback | CLOSED |
| GAP-031（CLOSED 2026-08-15） | `/health/dependencies` 的历史 API/部署字段、状态和依赖集合不一致 | `dependency-health-v1` 已固定 `postgresql|redis|minio|qdrant|worker|scanner|ai_provider`、`required`、`ok|unavailable|disabled` 和 200/503 脱敏聚合；单元、完整 local 200、必需 Worker 失败 503 与恢复 200 已验证 | CLOSED；production 监控/SLO 未运行 |
| GAP-034（CLOSED 2026-08-11） | `break_glass_requests` 只有 `approved_by` 且没有 `revoke_reason`，无法在该独立审批事实中保存 AUTH-014 的拒绝操作者和 AUTH-015 的撤销原因 | `CR-003-R3` effective contract 已批准并同步，继承 D-010～D-011，统一 `decided_by` 并新增 `revoke_reason`；008 source/ORM 与 PostgreSQL 16 storage-schema Gate C 已通过。关闭合同差异和 storage-schema Gate 不代表 AUTH-011～015 runtime 或 AC-001 通过 | CLOSED |
| GAP-035（CLOSED 2026-08-11） | `uq_user_role_active WHERE revoked_at IS NULL` 不感知临时授权 `expires_at`，自然过期但回收未执行时会阻止后续合法授权 | `CR-003-R3` effective contract 已批准并同步，继承 D-012，以 `[assigned_at, expires_at)` 的 `btree_gist` 重叠排除约束取代单纯未撤销唯一约束；008 source/ORM 与 PostgreSQL 16 storage-schema Gate C 已通过并验证时间边界，不以后台回收作为正确性前提 | CLOSED |
| GAP-036（CLOSED 2026-08-11） | 长期职责分离仅要求 production 生效，但数据库触发器没有可信且不可绕过的环境来源 | `CR-003-R3` effective contract 已批准并同步，继承 D-013，要求所有环境统一执行长期职责分离硬约束，并冻结 SECURITY INVOKER/固定 search_path 的内部 trigger 约束；008 source 与 PostgreSQL 16 storage-schema Gate C 已通过且禁止 session GUC 或客户端环境标志绕过 | CLOSED |
| GAP-037（CLOSED 2026-08-11） | “有效 system_admin”未区分长期或临时授权，AUTH-015 也未冻结撤销人与请求人、目标、决定人的关系 | `CR-003-R3` effective contract 已批准并同步，继承 D-014 并冻结 action-time 资格：治理 actor 仅限当前同组织有效长期管理员，历史主体失效不阻断 reject/revoke/expire；008 source/ORM 与 PostgreSQL 16 storage-schema Gate C 已通过。关闭合同差异不授权 app wrapper、ACL、Repository、Service 或 API runtime | CLOSED |
| GAP-038（CLOSED 2026-08-12） | 认证合同未冻结密码哈希参数、密码策略、登录失败阈值/锁定时长、`remember_me` 生命周期、JWT algorithm/issuer/audience/key rotation，以及 Refresh Token 重放后的会话族处置 | BOSS 前向批准的 `auth-mvp-v1` 已直接同步到活跃 `PRODUCT_REQUIREMENTS.md` 与 `TECHNICAL_SPEC.md`：冻结 NFC 密码边界、Argon2id 参数、五次/15 分钟锁定、Ed25519 Access/换密 Token、7/30 天 opaque Refresh 绝对会话、旋转/重放族撤销、严格 Cookie/Origin 和五个 Auth API。未使用或批准候选 CR；关闭合同差异不代表 Router、Service、Repository、Frontend 会话、运行时或 AC-001 已完成 | CLOSED |
| GAP-039（CLOSED 2026-08-12） | `/auth/me.permissions` 只有示例，没有 P0 permission code 的唯一完整字典及五角色映射 | BOSS 前向批准的 `p0-permissions-v1` 已直接同步到活跃产品/技术 SSOT：冻结 26 个 code、五角色直接映射、资源范围、有效角色并集后的 deny-overrides、break-glass 不越权和两条同人 SoD。Backend 仍须逐请求校验组织、资源与状态；关闭合同差异不代表 RBAC 代码、API、IDOR 负例或 AC-001 已完成 | CLOSED |
| GAP-040（CLOSED 2026-08-09） | 架构要求 `async_jobs` 至少含可重试标记和幂等键，数据库只定义 `idempotency_record_id`，没有重复的 `retryable/idempotency_key` 列 | `CR-004-R2/REL-D-001` effective contract 已批准并完成 11 文件原子同步：不增加重复列，按冻结状态、次数、retry/lease Profile 与 fencing 失败关闭派生。007 已交付空表 DDL/ORM 和 PostgreSQL 16 约束 Gate；通用 Handler Registry meta Loader 已有 6 份 test-only synthetic JSON/45 项离线证据，但生产制品仍 `PENDING / NOT GENERATED / NOT APPROVED`，Job runtime 仍未授权 | CLOSED |
| GAP-041（CLOSED 2026-08-09） | `async_job_steps.status` 无批准枚举，“追加写”与“已完成步骤禁止 UPDATE”也未定义未完成步骤的允许更新白名单 | `CR-004-R2/REL-D-002` effective contract 已批准并同步：冻结五态、唯一一次终态转换、Summary Schema 边界和精确更新白名单。007 已交付空表 DDL/ORM 与触发器 Gate；synthetic Summary Schema 只验证通用 Loader，生产 Summary Schema bundle 与 Step-writing runtime 仍 `PENDING / NOT GENERATED / NOT APPROVED` 或未授权 | CLOSED |
| GAP-042（CLOSED 2026-08-09） | Broker `event_schema_version` 与 `outbox_events.event_version/payload_json` 映射、AI `late_completion` 事件类型/顺序/载荷、投递字段更新白名单及非空表降级语义未冻结 | `CR-004-R2/REL-D-003～005` effective contract 已批准并同步：冻结 aggregate sequence、protocol-v2 wake-up、AI late-event 依赖边界、Outbox CAS/八次投递/租约/退避/错误码与非空降级阻断。007 已交付空表 DDL/ORM 和 PostgreSQL 16 Gate；CR-011-R4 contract 与 Stage1 Event/Fake Sink 已交付，但 Broker/Dispatcher/Worker/AI persistent runtime 仍未授权 | CLOSED |
| GAP-043（CLOSED 2026-08-18） | `document_block_corrections.result_parse_version_id` 既允许创建结果版本后回填，又与“纠错记录禁止 UPDATE”冲突 | `CR-005-R2/DOC-D-001` 已批准并同步：同一事务创建 queued 候选 Parse、不可变 correction、冻结七键输入的 Job/Outbox，result FK 为 NOT NULL+UNIQUE；025 deferred guard 与 PostgreSQL File Gate 证明原子创建、Worker、失败收敛和独立 sibling 激活 | CLOSED；Asset-bearing manual snapshot 与 production/正式 AC 不在本差异关闭结论内 |
| GAP-044（CLOSED 2026-08-18） | `document_assets.security_status` 未定义允许值，也未明确是否复用文件六态扫描枚举 | `CR-005-R2/DOC-D-002` 已批准并同步 `asset-security-v1`、六态、Policy/Scanner 证据矩阵、一次性 pending→终态和重建血缘；025 列/约束/触发器及安全 downgrade 已通过 File Gate。缺 CR-010 current Profile 时 PARSE-006 仍固定 503 且零事实 | CLOSED；关闭存储合同差异不等于 Asset Scanner runtime、production 或正式 AC 已通过 |
| GAP-045（CLOSED 2026-08-18） | `markdown_source_mappings` 的五列唯一键包含可空 `block_id`，普通 PostgreSQL UNIQUE 允许多个 NULL 重复，无法实现文档声称的真实唯一性 | `CR-005-R2/DOC-D-003` 已批准并同步；025 使用 PostgreSQL 16 `UNIQUE NULLS NOT DISTINCT`，集成测试验证约束身份和空值重复拒绝，downgrade 在新证据存在时失败关闭 | CLOSED |
| GAP-046 | `operation_logs.id UUID PK` 与 P0 按 `created_at` 月分区在 PostgreSQL 16 中不能原样共存；分区、迟交、每日链式哈希、可靠告警、备份和降级也未冻结 | 未批准的 `CR-006-R1` 推荐复合时间身份、UTC 月/default、HMAC cursor、可重算链与备份、受控 definer 边界，并为数据库封账水位提议新增第 58 张 `operation_log_chain_state`；它依赖 CR-004 Outbox，批准并同步 58 表口径前不创建迁移 | BASE-005 operation_logs 迁移前 |
| GAP-047 | `knowledge_bases.code` 的“活动唯一”可能表示全历史、未软删除或仅 active，`status/default_top_k` 的默认、范围与归档反向转换也未唯一 | 未批准的 `CR-007-R1/FILEKB-D-001～002` 推荐仅 active+未删除 code 唯一、创建即 active、正整数 top_k、nullable 阈值和不可逆归档；KB-004 参数变化还需批准评测证据 | BASE-005 knowledge_bases 迁移前 |
| GAP-048 | 计划要求知识库 CRUD 与权限配置，API 只返回授权知识库，但 `knowledge_bases` 与接口均没有知识库 ACL/角色范围事实 | 未批准的 `CR-007-R1/FILEKB-D-003` 推荐 P0 不新增 ACL 表，冻结有效角色、制度范围与 KB/POL/RET/QA 的 SQL 等价谓词，并保留 GAP-020 的检索执行顺序依赖 | BASE-005 knowledge_bases 迁移前 |
| GAP-049 | 文件归档 API 返回 `archived_at`，`files` 表没有该字段，且归档与 M1 软删除语义不同 | 未批准的 `CR-007-R1/FILEKB-D-004/008` 推荐新增独立 `archived_at`、完整展开 M1、仅 stored+clean 可归档且不写 `deleted_*`；批准前不建 files | BASE-005 files 迁移前 |
| GAP-050（CLOSED 2026-08-12） | 文件内容去重索引仍命中 archived 行，但状态图无恢复路径，重复上传时是复用、恢复、冲突还是新建没有合同 | BOSS 前向批准的 `archived-reupload=conflict-v1` 已直接同步到活跃产品/技术 SSOT：同组织 SHA-256+大小命中 archived 时，单文件稳定返回 409 `FILE_ARCHIVED_DUPLICATE`；不复用、不恢复、不创建新文件事实、Job、业务对象或 MinIO 副本，批量项目保留相同 code/409 且无业务/存储副作用。`local-minio-v1` 同步冻结 tag+digest、Compose 内部 HTTP、七 Bucket 与运行时凭据注入；除 local Profile/Compose/bootstrap、Settings/Adapter、intake/scope 的离线证据外，显式 real local MinIO quarantine Adapter Gate 已使用合成 PDF 和服务端固定对象键 PASS：真实 PUT、内容/长度/Content-Type/SHA metadata 核验、DELETE，以及完整 PUT 后摘要错配和合成 post-commit exception 两个确定性精确补偿路径。该 Gate 不包含 FILE Router、PostgreSQL 文件事实/去重事务、Job/Worker、Scanner、通用迟到 timeout reconcile、进程崩溃清理、容量、production 或 AC；这些边界仍未完成或 `NOT_RUN` | CLOSED |
| GAP-051 | 架构把扫描失败映射为文件 rejected，又允许 `scan_failed` 技术重试；文件状态图没有 `rejected -> validating`，扫描六态也没有重试转换 | 未批准的 `CR-007-R1/FILEKB-D-005` 推荐文件/扫描双状态矩阵、固定 `file-scan-outcome-v1` 和 failed→queued 用户重试；Lease 恢复仍依赖 CR-004 | BASE-005 files 迁移前 |
| GAP-052 | `auto_process_requested=false` 仍必须持久化和完成安全扫描，但架构只在自动处理时创建 Job，FILE-001 响应又把 `job_id` 定为非空 | 未批准的 `CR-007-R1/FILEKB-D-006～007` 推荐上传始终创建 scan_only/full Job、冻结响应投影且 policy 目标 KB 必须同组织 active；全局 AcceptedJobResponse 仍由 GAP-018 单独裁决 | FILE-001 前 |
| GAP-053 | `operation_logs.action_code/resource_type/result/producer/actor` 没有完整唯一字典；未知或畸形 Token 的 401 又无法满足组织非空的数据库合同 | 未批准的 `CR-008-R1` 已机械登记 73 个非 GET、5 个必须留痕 GET、92 个应用条目、48 个条件 Worker 条目和 5 个 System 条目；六项 emission/结果/身份语义、Handler Registry、CR-003/004/005/007 与 DEP-005 未闭合前，不生成最终 145 条注册表 JCS/hash，CR-006 wrapper 和 operation_logs 迁移保持阻塞 | BASE-005 operation_logs 迁移前 |
| GAP-054 | `chunking_configs` 同时声明 `draft/published/archived`、published 后不可修改和 I1 `archived_at`，却未冻结发布后归档是否是允许更新；`config_hash` 的精确 JSON、draft 更新白名单和发布/归档约束也不唯一 | 未批准的 `CR-009-R1` 推荐 25 列 I1+row_version、draft→published/archived 与 published→archived、`chunking-config-v1` JCS/hash 及 CHUNK-008；固定向量已离线复算，但批准前不建表、不增加接口，正式 API 仍为 122 | BASE-005 chunking_configs 迁移前 |
| GAP-055（CLOSED FOR ISOLATED FIXED_TEST 2026-08-18） | scan Step/evidence 需要获准 Scanner Registry version、JCS/hash、环境 class 与数据库 validator | `CR-010-R2` 已批准 366-byte fixed_test Profile、027 `scanner_registry_profiles`、current/history/selector 与 Asset consumer；Profile 表默认空，隔离测试显式安装后 PostgreSQL File 16/Full 153×2 通过 | CLOSED 仅指 fixed_test 合成合同；真实 local/production Scanner、Definition、MinIO Asset runtime 和正式 AC 仍 NOT_RUN |
| GAP-056 | CHUNK-001～007 没有分块配置列表或详情读取接口，UI-009 页面重载后无法发现未发布 draft、历史版本或可归档 config；仅增加已知 ID 的 CHUNK-008 不能闭合管理流程 | `CR-009-R1` 明确保留该阻断；须另行裁决复用现有读取合同还是新增 list/detail API，并同步权限、分页、状态过滤、API 总数、UI 空/错误态和操作日志注册表 | KB-004/UI-009 前 |
| GAP-057 | `policy_hash` 同时属于 Policy 顶层对象又声明为对 Policy 本身计算的 JCS+SHA-256，未冻结自引用字段的排除/置空/占位语义；嵌套 Schema 和 allowlist 数组顺序也不完整 | 已批准并同步的 `CR-011-R4/AI-D-009` 冻结删除且只删除顶层 `policy_hash` 的 JCS pre-hash、封闭 Schema 与固定向量；Stage1 strict parser/companion 与完整 Gate B 双 Draft 2020-12 engines 2/2 均已通过。production startup 和非测试 Settings/Policy 采用未验证，因此本 Gap 保持 open，不把离线 pre-hash 冒充完整运行 Policy | AI-001 完整 Policy 前 |
| GAP-058 | Profile 缺 `profile_id/adapter_id/endpoint_id` 和 network identity，operation 也没有主/备 Profile 引用，无法唯一形成路由或 `adapter_id+endpoint_id+model_id` 熔断键 | 已批准并同步的 `CR-011-R4/AI-D-010` 冻结 Profile map identity、显式目标字段和 operation 引用；Stage1 纯 resolver 与完整 Gate B 证据已通过。真实 endpoint/model/allowlist 值、运行时 Adapter 与路由集成仍未授权或未验证，因此本 Gap 保持 open | AI-001 调度器前 |
| GAP-059 | 已批准合同定义读取超时为可重试错误，却没有 read timeout 数值；“最小处理余量”无值，jitter、retry ordinal、cap 和 `Retry-After` 线级 grammar 也不能唯一实现 | 已批准并同步的 `CR-011-R4/AI-D-011` 冻结 read-idle/processing margin 与注入时钟/随机源的精确算法；Stage1 纯 retry 与完整 Gate B 证据已通过。真实等待、deadline 执行、Provider 调用与完整 HTTP Adapter 未运行，因此本 Gap 保持 open | AI-001 retry 前 |
| GAP-060 | 成功响应未知字段应拒绝还是忽略、重复 JSON key、请求序列化、Header 64 KiB 计数和 gzip 多 member/尾随/原始流上限未冻结，无法完成恰好命中与超一字节验收 | 已批准并同步的 `CR-011-R4/AI-D-012` 冻结请求 JCS、unknown-ignore-never-use、全层重复 key 拒绝与精确线级字节算法；Stage1 strict JSON/response boundary 与完整 Gate B 证据已通过。HTTP transport、真实压缩流和 Provider 未运行，因此本 Gap 保持 open | AI-001 HTTP Adapter 前 |
| GAP-061 | CR-002 要求 external 公网 allowlist、internal 服务/CIDR 和实际 peer 复核，但正式 Profile 无可加载的 network scope/hostname/CIDR 字段 | 已批准并同步的 `CR-011-R4/AI-D-013` 冻结字段结构与校验关系；Stage1 和完整 Gate B 均只以注入式 Resolver/peer fixture 验证 network policy，zero-socket attempts=0。DNS/TLS/socket、真实环境值和网络放行均未运行或未授权，因此本 Gap 保持 open | AI-001 出站 Transport 前 |
| GAP-062（CLOSED 2026-08-16） | `AiCallEventV1` 原只有描述性字段白名单，没有精确字段类型/nullability/hash 投影，Sink 的 reserve/complete 结果 DTO 也未冻结；AI-001/AI-005 无法据此生成同一持久事件 | `CR-011-R4/AI-D-014` 已冻结版本化 Event Schema、JCS/hash/replay、封闭 Sink 结果与 reserve→send→complete→采用顺序；Stage1 Event DTO、内存 Fake Sink和完整 Gate B 已通过。当前 AI-005 PostgreSQL primitive 直接复用同一 DTO，完成 durable reserve/complete、Outbox 顺序投影与补偿；`DurableAiCallEventSink` 又把六态安全映射到同一 Port，并限制 commit-unknown 恢复只发生于同实例、同 scope、尚未发送的窗口。24 个聚焦用例、PostgreSQL `-Scope AI` 和 Maintenance OS 级 SIGKILL local Compose 门禁均通过，故事件合同缺口关闭。Gateway/共享事务业务采用、公开 OPS-005、Redis、真实 Provider、production 与 AC 继续独立阻断，不因关闭本 Gap 获得授权 | CLOSED；AI-001/AI-005 partial |
| GAP-063（CLOSED 2026-08-09） | `contracts` 的非空编号唯一原未冻结状态/软删除范围；`suppliers` 的统一社会信用代码与税号原未冻结同值双列、跨列去重、规范化和唯一范围，`source_type/status` 也未写明 nullability/default | `CR-012-R3/FIN-D-001～006` 已批准并原子同步九份 Request：合同编号仅在未删除范围唯一、P0 单一供应商税务身份值仅在 active+未删除范围唯一，并冻结显式来源/状态和三表原子迁移。006 空表 DDL/ORM 及本地/合成 PostgreSQL 16 验证已完成；关闭合同缺口不批准 GAP-064 运行时、真实数据、Provider/外网、部署、production 或任何 AC | CLOSED |
| GAP-064（CLOSED 2026-08-14） | 供应商统一投影、状态、精确复用、来源回填、纠错、并发和错误映射不唯一 | `CR-018-R1/RFV1-D-001` 已以前向 current-baseline 批准并同步统一 `COALESCE` 投影、generic tax 写入、三态、组织→来源→supplier 锁序、单条 `supplier_field` 纠错和三个稳定冲突码；不恢复 CR-015 的不可得历史审批链 | CLOSED；Backend/PostgreSQL runtime 已验证，Frontend/browser write E2E 与 AC 未运行 |
| GAP-065（CLOSED 2026-08-14） | quote、坐标不可得原因、Markdown parser/raw HTML 和复杂表格资源格式不唯一 | `CR-018-R1/RFV1-D-002` 已批准并同步 quote、两类显式 reason、CommonMark/GFM pipe-table、禁 raw HTML、`table_asset_v1` 和单一分块 Profile；多业务文档拆分继续不进入本切片 | CLOSED；018 Schema、Worker/Repository runtime 已验证，Frontend/AC 未运行 |
| GAP-066（CLOSED 2026-08-14） | 15 规则元数据、实现绑定、整批发布和多版本 current 投影不唯一 | `CR-018-R1/RFV1-D-005` 已批准并同步应用内静态 15 规则、七类元数据、严格输入 Schema、release/manifest hash、整批原子发布、不可变历史和 current 派生 | CLOSED；publisher、审核执行器和数据库不可变约束已验证，正式 AC 未运行 |
| GAP-067（CLOSED 2026-08-15） | UI-002/FE-002 原缺少五角色工作台和失败 Job 的安全发现入口 | `dashboard-summary-v1` 由 Backend 按当前 Actor 权限投影摘要、待办和有界最近失败 Job；Frontend 不枚举 Job ID、不全量抓取业务事实、不显示无权限卡片。Router/Service/Repository、严格 decoder 和五角色 Backend/Frontend 测试已通过 | CLOSED；2026-08-16 当前 checkout 的五角色浏览器导航、写入口可见性与未授权直达矩阵已通过；正式可访问性与 AC 未运行 |
| GAP-068（CLOSED 2026-08-14） | 审核执行 failed/cancel、终态更新、父对象一致性、high gate、报告迁移和锁序不唯一 | `CR-018-R1/RFV1-D-006～008` 已批准并同步执行/报告完整状态矩阵、直接 cancelled、同快照失败重试、跨父对象约束、待处理有效 high 门禁、同人职责分离和固定锁序 | CLOSED；020 Schema、审核/报告 runtime、真实 MinIO 与报告浏览器路径已验证，正式 AC/production 未运行 |

## 其他基线差异

| ID | 差异 | 当前安全口径 | 最迟关闭阶段 |
|---|---|---|---|
| GAP-001（CLOSED 2026-08-14） | 5 条 smoke、50 条 MVP/UAT 和 100 条正式评测集的层级关系不唯一 | `CR-018-R1/RFV1-D-004` 已批准并同步：5 条只作链路 smoke，至少 50 条为 MVP/UAT，至少 100 条为正式发布门禁；每档保存独立 run | CLOSED；5 条 smoke 已运行，50/100 数据集审批与运行仍待 TEST-001/正式验收 |
| GAP-002（CLOSED 2026-08-16） | 测试方案称字段提取阈值由首次运行确定，需求已给出发票 ≥95%、合同 ≥85%、JSON 合法率 ≥99% | `CR-019/CR019-D-006` 固定按 13 个归一化核心字段精确匹配和全部返回结构化响应 attempt 计算；首次运行不得降低阈值 | CLOSED；计算器已实现，代表性审批数据与正式运行仍待 UAT |
| GAP-003（CLOSED 2026-08-16） | 测试方案称性能阈值待基线运行确认，需求已给出多项 P95 | `representative-performance-evaluation-v1` 已按需求固定六项 P95、上传异步语义、三任务零丢失/零重复、nearest-rank 算法和同一 Profile 连续至少三轮；失败/超时样本不得被快速响应掩盖 | CLOSED；计算器已实现，已审批正式 Profile、代表性输入和三轮运行仍为 `NOT_RUN` |
| GAP-004（CLOSED 2026-08-07） | 原记录把 SRS 中截止于“后端架构设计前”的阶段性 `TBD-004` 继续视为未决冲突 | SRS 2022～2029 明确了关闭时点；随后发布的架构 1404～1420 和开发计划 39、233～245 均一致采用 Celery 5 + Redis，构成同一设计链的正式细化，无需另设 CR。关闭本 Gap 只确认技术选型，不代表 `BASE-006`、Worker、Outbox 或恢复验收已实现 | CLOSED |
| GAP-005（CLOSED 2026-08-16） | 开发计划将 `/metrics` 列为 P0，需求 P0 API 清单未明确 | `CR-019/CR019-D-005` 批准无业务字段的内部低基数指标端点、独立 Bearer 凭据和精确 Nginx 代理；不自动批准外部监控栈 | CLOSED；端点/权限/静态代理已验证，production 暴露、采集、告警和 SLO 仍待 DEP-006 |
| GAP-006 | `TEST-007` 依赖不存在的 `OPS-001～OPS-005` 标识 | 不在排期系统中创建虚假任务；由计划修订映射到真实 `DEP-*` 或明确 API 编号 | TEST-007 排期前 |
| GAP-007（CLOSED 2026-08-07） | 原记录把本地作品环境与生产环境的不同安全门槛视为冲突 | SRS 876～880 与架构 1567～1576 均明确允许本地作品环境使用禁外联、类型白名单、隔离存储或占位 Adapter，同时要求生产接入经验证的真实扫描方案；这是环境分级门禁而非二选一冲突。关闭本 Gap 不代表扫描状态枚举、Adapter 或生产扫描已经实现 | CLOSED |
| GAP-018 | 202 通则要求 Job ID、资源 ID、真实 `stage` 和 Job URL，13 个异步接口详情未全部满足 | 先冻结统一 `AcceptedJobResponse`，不得为各接口自造响应 | BASE-006 前 |
| GAP-019 | 可变主数据状态动作通则要求 `row_version`，POL-008/009/010 未携带 | 所有状态动作统一乐观锁语义后再实现 | KB-003 前 |
| GAP-020（CLOSED 2026-08-14） | Qdrant 初筛与 PostgreSQL 允许集先行顺序冲突 | `CR-018-R1/RFV1-D-003` 已批准并同步“PG 允许集 → Qdrant must-filter → PG 终审”；任一步漂移都拒绝采用 | CLOSED；Repository/Service、越权漂移负例与真实 Qdrant Gate 已验证 |
| GAP-021 | AI 发票明细 Schema 缺少页面要求的 `unit` 和行级价税合计 | 前端不得浮点推算；先由 API/数据库明确字段和 Decimal 计算来源 | INV-001 前 |
| GAP-023 | 部署文档 5.2 的变量清单遗漏 vLLM Profile 已使用的 `VLLM_IMAGE_TAG`、`VLLM_MODEL_PATH_OR_ID`、`VLLM_MAX_MODEL_LEN`；仓库 `Settings` 与 `.env.example` 已用空值占位完成本地缓解，但正式部署文档仍漂移 | 保留空值和启用时 fail-closed 校验，不启用真实模型；在 DEP-001 前把三项同步回正式部署配置清单 | DEP-001 前 |
| GAP-024 | 页面只规定桌面最低 1280px 与窄屏行为，未冻结实际 breakpoint | 基础 Shell 不宣称视觉定稿；正式页面实现前确定并截图验证断点 | FE-002 前 |
| GAP-025（CLOSED 2026-08-07） | 初始种子原先缺少单组织和首管理员的安全 bootstrap 合同 | `CR-001-R2/D-007` 已批准并同步离线、单事务、advisory lock、幂等/冲突 fail-closed bootstrap；禁止匿名 HTTP、默认组织/账号/密码和 argv/env 泄密 | CLOSED |

## 变更要求

### 2026-08-16 当前 checkout 覆盖说明

当前唯一事实快照：accepted Alembic head 为 `20260817_024`，ORM/runtime catalog 与隔离 PostgreSQL 覆盖 57/57 张核心物理表；`021` 封锁检索初始状态旁路，`022` 闭合无证据发票币种，`023` 增加风险解释与报告草稿持久事实，`024` 增加 Event v2 USD/CNY 通用 microunit 费用列及严格升降级门禁。用户与 Break-glass、文件/合同/发票/供应商、知识/RAG、审核/报告、工作台、依赖健康、AI 调用审计和内部指标均已有 Router → Service → Repository/Adapter 实现，主要 P0 Frontend 已接真实 API。真实 MiniMax-M3 Chat Adapter、Gateway、结构修复、预算/网络策略、Provider → EventSink → 业务事实原子采用、合同/发票提取、RAG、风险解释、报告草稿和 OPS-005 已接入并通过受限真实 smoke；`CR-021/022` 又将百炼 `qwen3.7-text-embedding`、Event/Policy v2、CNY 预算/实际费用和事务采用接入 Backend/Worker。首次受限付费 Embedding smoke 已验证 43 input tokens、2×1024 维输出和 CNY 22 microunits；随后单独授权的完整本地知识库 E2E 以 7 次请求、3231 input tokens、CNY 1616 microunits 完成 15 个 1024 维成员的 Worker 索引、Qdrant green、Top-5 和 5/5 smoke，索引在 100 条 `formal_release` 门禁下保持 `ready`，临时 Collection 与容器均清理。两次授权均已消耗。真实 Chat 与 Embedding 共用 Redis server time/Lua 跨进程并发、RPM/TPM 和目标级熔断门禁，许可顺序、并发旧成功不误关熔断、单 half-open 探针及覆盖探针租约的 TTL 已通过聚焦和真实隔离 Redis 测试。`/metrics` 已有独立 Bearer 权限和精确 Nginx 代理，但 production 采集、告警与 SLO 未运行。代表性合同/发票与 50/100 条业务审批集、`formal_release` 激活、production OCR/Scanner/TLS/Secret Manager、正式容量/恢复、AC/UAT 与 production 仍为 `NOT_RUN/BLOCKED`；以下段落全部是历史 checkpoint，只用于追溯，不覆盖本快照。

当前浏览器闭环的可复现证据采用 2026-08-16 当前 checkout 的最终干净重跑：`scripts/verify-financial-loop-browser-gate.ps1` 以本地不可变 PostgreSQL/Redis 镜像 ID 和 `--pull never` 启动隔离运行时，真实完成两类 DOCX 上传、合成 Scanner、Dispatcher/Celery Worker、合同/发票专用提取、完整事实修正、人工确认、供应商原子修正/激活与双来源复用、主合同确认、15 规则审核、财务复核、ready PDF/XLSX 和五角色权限矩阵。首轮非原子供应商两次写入被最终 manifest 拒绝并已清理，不计为通过；最终受保护 completion 精确接受 2 个文件/绑定、1 个合同/发票/供应商/关系/任务/执行/报告、1 条聚合纠错、2 次 resolve、1 次 update 与 5 个登录 Actor，页面 console warning/error 为 0，输出 `FINANCIAL_LOOP_BROWSER_GATE=PASS`，专用容器最终为 0。本地 MinIO 数据卷按启动器既有策略保留。该证据不把合成 clamd、确定性提取/答案、loopback HTTP、浏览器 PDF iframe 或 XLSX 下载动作外推为 production Scanner/OCR/TLS、真实 Provider、Excel 打开验证、正式可访问性、UAT、production 或 AC 通过。

当前本地镜像安全快照：Docker Scout 1.23.1 扫描 Frontend 为 `0C/0H`；Backend 在 `pypdf 6.13.0 → 6.14.2` 且移除 `pip/setuptools/wheel` 后由 `2C/6H` 降为 `2C/2H`，所有仍有修复版本的 C/H 为 0。剩余 4 项均是 Bookworm Perl 且标记 `not fixed`；production 镜像签署和严重/高危为 0 仍未完成。

AI-003 当前本地快照：合同/发票独立版本化 Prompt、精确 Hash、同解析版本/唯一证据定位与 canonical JSON 渲染已有 16 项聚焦回归；正文只作为不可信 user data，Prompt 禁止无证据补造和业务决定。Provider、Gateway/EventSink、业务输出 DTO/采用和数据库写入仍未运行；发票无证据币种要求 `null` 与当前非空/CNY 默认运行合同仍须先闭合，故只从 planned 升为 partial。

#### 历史 checkpoint（已被上方当前快照覆盖）

本节覆盖下方保留用于追溯的旧 checkout 数字：当前唯一 Alembic head 为 `20260814_016`，ORM/runtime catalog 为 30/57 表、剩余 27 表；PostgreSQL 16.14 current-head 完整目录连续两轮各 122 项通过，默认完整离线门禁为 Backend `2577 passed / 109 skipped / 1 warning`、Frontend 19 files/482 tests，并通过 Ruff、格式、mypy、pip check、typecheck 和 build。Scanner、正常解析/OCR、发票提取、合同发票写链与 Qdrant existing-collection primitive 使追踪矩阵更新为 `2 implemented / 48 partial / 36 planned`。随后 BOSS 批准 `CR-018-R1/recommended-forward-v1`，只关闭 GAP-001/008/020/064～066/068 的合同歧义；它尚未增加 Schema、Router 或运行证据，因此不完成剩余 27 表、Markdown/分块、完整索引/RAG、供应商、审核/报告、Compose、Provider、production 或任何正式 AC。

裁决必须同步评估需求、架构、数据库、API、页面、测试、部署、任务依赖和数据迁移；未经批准不得直接改写 Request 基线或已完成验收结果。

`CR-001-R2` 已关闭当时阻塞 `BASE-005` 的 GAP-010～011、013～014、016、025～027；`CR-004-R2` 已关闭 GAP-040～042 并交付空的 `async_jobs/async_job_steps/outbox_events`；`CR-003-R3` 已获九角色 9/9 批准且 Gate B 同步 PASS，关闭 GAP-034～037，并交付 008、两张空特权授权表的 ORM 与 PostgreSQL 16 storage-schema Gate C 证据。009 又按 Request 已冻结表合同交付空的 `supplementary_agreements/invoice_items/contract_invoices`；81/81、87/87 与 88/88 是该 head 的较早 checkpoint，当前唯一 head 的最新有效证据采用干净重跑：PostgreSQL 16.14、`server_version_num=160014`、双轮 integration/database 90/90、`POSTGRESQL_CURRENT_HEAD=PASS`、专用标签容器最终零残留，`TEST_DATABASE_URL` 与破坏确认变量恢复为 unset；首次未通过尝试不是有效证据。当前源码/ORM/运行时 catalog 对象为 18/57、其余 39 表；数据库目录已覆盖 Auth Repository→Service→HTTP 组合场景：真实 login/JWT→Actor/`financial.read`→八个已冻结财务只读 Router→对应 Service/Repository→同一 PostgreSQL，其中 authenticated HTTP 的精确重复候选只验证无 cursor 的 `ready + 空页` 与 `private, no-store`；独立真实 PostgreSQL Repository→Service 用例另行验证非空候选、三页 UUID keyset、self/deleted/voided/near-mismatch 排除、archived 保留及三元组逐字段数据库等值，strict cursor 由 unit/service 用例证明；随后 `system_admin` 再登录→Actor/`users.manage`→用户 Router/Service/Repository→同组织用户列表。最新完整 accepted-source checkpoint 为 Backend `2398 passed / 75 skipped / 1 warning`、Ruff check PASS、Ruff format 212 files、mypy 114 sources、Frontend 14 files/412 Vitest/129-module build。用户列表 Repository 在普通 `READ COMMITTED` Session 中分语句读取用户页、数据库时钟和固定角色，不构成单一 MVCC 快照；用户 `row_version` 不证明角色投影同快照。canonical cursor 可逆携带用户名，原始 query/cursor 必须保持日志、错误和 Trace 脱敏，但 operation-log、代理日志与 production 遥测尚未实现或验证。另行显式运行的本地 `financial-read-browser-v1` Gate 已升级并 PASS：test-only 同源宿主以授权 token、受限 loopback 端口、逐字 origin、`frontend/dist` 完整性和可丢弃 PostgreSQL marker 五重门禁，生成临时 Ed25519 PKCS8 私钥与 public keyring，经生产 keyring loader 和 `create_app` lifespan 构造真实 AuthService 与七条财务读链所用五个 QueryService；门禁断言 `dependency_overrides=0`、六个 Service state 在 lifespan 前不存在且进入后为真实类型。真实浏览器从 login 经 dashboard、发票列表/详情、当前主合同、合同列表/详情与补充协议原始 Header、合同当前主发票回到发票详情，对应此前七条已冻结只读 API 并精确命中隔离 PostgreSQL 合成事实，0 alert、0 console error；深链 reload 保持 URL 与用户，logout 后 reload 保持匿名且无远端退出状态未确认提示。该路径未导航 `/users` 或发票精确重复候选。正常 `CTRL_BREAK` 后 OwnedSyntheticRows、临时 key 目录、宿主进程和专用容器复核均为 0。该显式 Gate 仅证明 loopback HTTP 下的本地 test-only 财务读链，不证明浏览器用户列表/精确重复候选、TLS/Nginx、production secret mount/文件 ACL、部署、强杀/断电清理或任何 AC，默认离线编排仍固定 `BROWSER_E2E=NOT_RUN`。显式 real local MinIO quarantine Adapter Gate 仅证明合成对象的真实 PUT/核验/DELETE 和两个确定性精确补偿路径，不证明 FILE Router、数据库/去重事务、Job/Worker、Scanner、通用迟到 timeout reconcile、进程崩溃清理、容量、production 或 AC；因此这些证据仍不代表完整 AUTH、其他业务 runtime、production 或任何 AC 已完成。`CR-011-R4/R5/R6` 已批准、完成相应同步并交付 Stage1 与完整 Gate B 纯 contract/offline 证据，但 GAP-057～062 因 production startup、真实 HTTP/DNS/TLS/peer、环境签署值和持久运行时证据未完成而全部保持 open；`CR-012-R3` 已关闭 GAP-063 并交付空的 `contracts/invoices/suppliers`。当前正式 Request 基线仍为精确 57 张核心物理表。`CR-005-R1`～`CR-010-R1`、`CR-015-R2` 与 `CR-016-R1` 仍未批准，并继续跟踪 GAP-043～068 中其余文档、操作日志、文件/知识库、分块、供应商运行时、规则目录发布、工作台数据来源和审核执行状态机差异；其中 CR-006 还提议以第 58 张技术状态表解决封账后拒写。CR-015 不占 migration，但其当前 R2 缺少可恢复的历史依赖证据，必须由 successor 以前向 attestation 重建可签合同；CR-016 不占 migration，也不授权绕过 AUD-003 的 `BASE-005、AUD-002` 前置。其余 39 表、后续对象的完整 PostgreSQL 16 约束/触发器/bootstrap 演练及业务 runtime 完成后，才能评估 `BASE-005` 完成状态。追踪矩阵现为 86 项：2 implemented / 39 partial / 45 planned；差异清单为 41 open / 27 closed / 68 total。除上述显式 PostgreSQL、MinIO 与本地浏览器局部门禁外，默认 Docker/browser/Provider/remote/deploy/production 未运行，且本切片没有完成任何 AC。

本轮 accepted-source checkpoint 的完整工具边界还包括 `pip check PASS`。通用 Handler Registry meta Loader 的 45 项聚焦测试继续被纳入，但 6 份 synthetic JSON 与两路独立 `GO` 不等于生产 Registry/Schema/Input/Summary/Handler artifact 获批，也不等于 Job/Worker runtime 获授权或接线。PostgreSQL 组合测试写入真实临时 Ed25519 PEM/JSON 文件，经 keyring loader 与零 `dependency_overrides` 的 `create_app` lifespan 构造 AuthService、财务 QueryService 和 UserQueryService；该门禁仅以 ASGI `https://testserver` 模拟 HTTPS，不证明真实 TLS/Nginx、production secret mount/文件 ACL、部署、浏览器或 AC。

`invoice-exact-duplicate-candidates-read-v1` 只把 `INV-004` 从 planned 升为 partial：当前读链按源发票的持久化 `invoice_code + invoice_number + seller_tax_no` 逐字段数据库等值查询同组织候选，排除源票、软删除和 voided 候选并保留 archived，Frontend 只展示严格分页结果。`source_voided` 优先于 `incomplete_identity`，`ready + 空页` 不证明唯一、真伪或人工确认；本切片不写 `duplicate_status`、风险、审核任务或 operation-log。普通 `READ COMMITTED` 的源锚点与候选查询不是单一 MVCC 快照，现有索引前缀与 UUID tie-break 不是容量或性能证明；浏览器、production 与 AC-005 均为 `NOT_RUN`。

2026-08-13 的后续 accepted-source checkpoint 新增 `report-xlsx-writer-v1` 与 `report-pdf-writer-v1` 两个内部离线 primitive：前者锁定 `XlsxWriter==3.2.9`，从冻结 `ReportPayload` 生成固定 `Summary/Rules/Risks` 三表、有界且无公式/超链接/外部关系/宏/媒体/绘图的 XLSX 字节；后者锁定 `ReportLab==5.0.0`，生成固定 A4、有逐页非正式预览标识、可见控制符转义、仅未解析引用 ID、内嵌 `/ToUnicode` TrueType 字体且无主动内容的 PDF 字节。PDF 的 AOSP `android-15.0.0_r25` 两份字体、原始 NOTICE/README 与来源记录共 5 项通过离线 wheel 构建安装/入包，上游四份原始文件 exact hash PASS；PDF unit 9、PDF+XLSX 相关 80 项、代表性 3 页 Poppler/pypdf/pdfplumber QA 与两路独立审查均通过（审查结论 `GO`）。最新完整 `verify-local-offline` 为 Backend `2281 passed / 73 skipped / 1 warning`、Ruff 202 files、mypy 108 sources、Frontend 13 files/372 Vitest/128-module build。两个局部 Writer 都不涉及数据库/`report_versions`、MinIO、下载/Auth、浏览器 Excel/PDF viewer 或跨镜像复现，且 PDF bytes 不是正式报告。`REP-003` 保持 partial，`REP-001/002` 保持 planned，BETA-VS-07、AC-014、矩阵 `2 implemented / 38 partial / 46 planned` 和差异清单均不升级。

同日的 `local-minio-restart-persistence-v1` 在修复固定 MinIO 策略 `Action/Resource` 集合响应顺序不稳定导致的 `APPLICATION_POLICY_DRIFT` 后，连续两轮显式真实 Gate PASS；失败轮不计为通过。每轮均 bootstrap 七个 Bucket 与 quarantine 权限，以原应用身份写入固定合成 PDF 的精确对象键，root 核验逐字节内容、大小、Content-Type 与 SHA-256 metadata；不带 `-v` 地 `compose down` 后用 `up --pull never` 创建新容器，原 root/应用身份继续有效，root 再核验后由原应用身份删除并确认不存在。start/stop/wrapper 均 PASS；最终专用容器/网络为 0，managed/root/app/gate 环境变量均不存在，named volume 保留。该门禁只证明单服务本地 named volume 上一次受控冷重启的固定对象持久性与同一应用身份连续性；不证明 FILE 数据库/Router/Job/Scanner、通用迟到 timeout reconcile、崩溃恢复、一般耐久性/容量、备份恢复、完整 Compose、TLS、production 或 AC-016。GAP-050 仍按既有合同原因保持 CLOSED，其余 Gap、任务状态、矩阵 `2 implemented / 38 partial / 46 planned` 与所有 AC 均不升级。
