# Changelog

- 2026-08-21：将 P1/P2 浏览器脚本、公开质量测量、CR-031、运行代码、测试与机器证据冻结为应用 revision `140c311…`；活跃规格同步为 Alembic head 027、58 表和已存在的 origin/upstream。完整离线、PostgreSQL Full、文档纠错、制度撤销、发票重复、报告下载与 Excel/LibreOffice 门禁通过，持久栈重建为 `local-mvp-0.1.2` 并以新权威备份保护；YHBX 持久管理员复登和独立人工 UAT仍为待签，不把技术 Gate 冒充发布 `GO`。
- 2026-08-17：按 BOSS 明确批准将全局密码策略升级为 `auth-password-v2`，最小长度由 15 调整为 6；最大长度、UTF-8 上限、Argon2id、锁定策略和弱密码拒绝继续生效，并补充常见 6 字符弱密码。
- 2026-08-17：按 BOSS 明确批准将内置应用入口全局改为 HTTP；Nginx、Origin/CORS、Cookie、启动/恢复/验证脚本和浏览器门禁同步切换，停止生成或挂载 TLS 证书，并保留当前 `127.0.0.1` 限制。HTTP 不提供传输加密，局域网或公网发布前必须恢复受信任 TLS。
- 2026-08-18：BOSS 完成持久本地管理员强制换密并重新登录；数据库与真实浏览器验证普通会话、用户管理授权读取和文件管理 `AUTH_FORBIDDEN` 职责分离，临时恢复密码已清零删除。
- 2026-08-18：YHBX 明确签署 `Local MVP UAT通过`；新增独立 UAT 记录，接受范围仅为当前本机 loopback HTTP Local MVP，production 与正式 AC 状态不变。
- 2026-08-18：按 BOSS“当前只要本地运行”决定批准 `CR-025`，将 AC-001/002/015/016 调整为 Local MVP 口径；当前 HTTP 安全浏览器链与备份/隔离恢复/冷启动补跑通过，新增四项 AC 验收记录并全部标记 Local MVP `ACCEPTED`。当前实际使用者 1 人，系统仍支持多账号和五角色，不承诺多人并发容量。

本项目使用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格记录用户可感知或工程基线变更。

## [Unreleased]

- 收口 Local MVP 0.1.1：发票重复候选浏览器 Gate 在真实 in-app browser PASS 后由入口脚本自动重算四项源码 bytes/SHA-256，完整离线门禁恢复为 Backend `3127 passed / 147 skipped / 1 warning`、Ruff 520、mypy 283、Frontend 539/151。133 文件的 027 整批变更冻结为 `61b2c17`；历史备份恢复首次暴露当前 marker 与备份时管理员用户名漂移导致 `BOOTSTRAP_STATE_CONFLICT`，恢复脚本改为从已验证数据库读取唯一 bootstrap 身份后重跑 PASS。旧 backup `b94b77a1…` 的 024/22 行/MinIO 摘要经隔离迁移到 027、运维、冷启动和清理通过；持久实例以新 backup `0016f52d…` 为回滚依据重建到 `local-mvp-0.1.1`，行数 7→7。最终源码 revision `03b6bbc`，未 push，production 与持久管理员复登未运行。
- 闭合 `INV-004` 单请求 MVCC 与 native browser 缺口：精确重复候选由一个 `duplicate_source` CTE + candidate LEFT JOIN statement 同时投影 basis 与 UUID keyset 页，编译 SQL/真实 PostgreSQL statement 计数均为 1，Full 154×2 PASS。`local-invoice-duplicate-browser-v1` 又完成真实登录导航、候选渲染与显式 pair 对比；受保护 completion 精确观测两个 200 GET，source/candidate/共同身份可见、写动作 disabled、console 0。跨页 snapshot、代表性质量、容量、production、正式可访问性和 AC-005 仍未完成。
- 新增 `local-document-correction-crash-recovery-v1`：在全新 local/test HTTP Compose 栈中让合成补充协议通过 ClamAV/file_process，再阻塞 `manual_correction_snapshot` exclusion 读取并精确 SIGKILL Worker。退出码 137、未提交候选 Page/Block/Markdown 为 0、同容器恢复、60 秒 Lease+15 秒宽限后的 Maintenance `exhausted`、Job/step/Parse `failed/WORKER_LOST`、旧 active/原件不变和失败候选 409 均通过；机器证据绑定 runner/wrapper/Dockerfile，专用资源、Secret、环境变量和镜像标签残留为 0。当前完整离线门禁同步通过 Backend `3121 passed / 147 skipped / 1 warning`、Ruff 518、mypy 283、Frontend 539/151 modules。该门禁保持 CR-005 `max_attempts=1`，不增加自动重试，也不升级真实 Asset、主机断电、production、容量或正式 AC。
- 扩展专用 `SecurityBaseline` 为 `local-knowledge-performance-v5`：在全新 local/test HTTP Compose 栈中，以 Provider disabled、`fixed_test` 1024 维 Embedding 和真实 PostgreSQL/Qdrant 运行三轮、每轮 20 个完整 QA。RAG nearest-rank P95 为 `40.774/38.884/41.064 ms`，同一端到端值作为 Top-5 子阶段保守上界，均通过 15 s/2 s 本机工程门槛；数据库确认 60 条 answered Query、引用/命中和 60 条追加日志。源哈希绑定证据已固化，专用容器、卷、网络、runtime/Secret、环境变量和镜像标签残留为 0；完整离线门禁同步通过 Backend `3103 passed / 146 skipped / 1 warning`、Ruff 516、mypy 283、Frontend 539/151 modules。该合成 fixed_test 结果不升级代表性质量、50/100 正式门禁、Provider、production、正式容量、UAT 或 AC。
- 升级专用本地性能门禁为 `local-performance-baseline-v4`：保留三轮列表、单件、最大批次/重放、部分失败、超限、审核并发和 20 页合同门禁，并在每轮新增一张 13 字段/1 明细合成 DOCX 发票的 HTTP→ClamAV→file_process→invoice_extract→API 可见全链。发票三轮为 `635.969/603.018/605.273 ms`，数据库逐轮确认业务绑定、双 Job/Outbox、四个成功 Step 与 `draft/unconfirmed/unique` 精确事实；同轮 20 页合同为 `1165.315/830.661/576.725 ms`。机器证据绑定当前 runner/wrapper SHA-256，完整离线门禁为 Backend `3098 passed / 146 skipped / 1 warning`、Frontend 539；专用项目、卷、runtime/Secret 和测试镜像清理为 0。合成 Local Profile 不代表经审批 95% 发票质量、正式复杂合同、OCR/AI、production、参考环境容量或 AC。
- 修复文档版本写入对文件生命周期的遗漏：归档文件上的新纠错、候选激活和资源安全重评现在统一返回 `FILE_STATE_CONFLICT`，且不残留 Parse、Correction、Job、Outbox 或幂等事实；归档前已完成请求的相同幂等键仍只重放历史响应。聚焦 API 4 项、mypy/Ruff/format 及隔离 PostgreSQL 16.14 File 范围 16 项通过。
- 批准并交付 `CR-029-R1` 与 `CR-030-R1` local/test 闭环：前者增加四业务类型的最小活动 Block 纠错来源 keyset 读取、Parse/anchor/Cursor/角色/归档门禁和 system_admin 纠错专用 FileDetail；后者增加制度撤销待执行请求管理分页、两角色读取和免手工 UUID 的显式执行 UI，继续由 Backend 重验 SoD/CAS且不授予问答权限。两个 delta 各 `+1`；机器事实为 `/api/v1=97`、full OpenAPI=100。加入最新 local 回归后的当前完整离线门禁为 Backend `3121 passed / 147 skipped / 1 warning`、Ruff 518、mypy 283、Frontend 28 files/539 tests 与 151-module build；PostgreSQL File 16、Retrieval/Policy 10 和完整 154×2 通过。Provider、production、真实数据迁移、提交和推送未执行；发票 pair 浏览器是独立合成门禁。
- 批准并交付 `CR-005-R2`、`CR-010-R2/fixed_test`、`CR-027-R1` 与 `CR-028-R1` 的 local/test 前向闭环：025/026/027 将文档纠错、隔离 Asset 安全重评、制度两阶段撤销、Scanner Registry 第 58 张技术表及数据库守卫落地；CR-027 增加资源/Job 双版本、审核技术重试/取消与文件同 Job 重试。OpenAPI 当前为 98 个唯一 operationId，CR-005/027/028 delta 保持 `+3/+1/+2`。PostgreSQL 16.14 File 16、Audit 4、Retrieval/Policy 10 与完整 153 项连续两轮通过；Backend unit 3074、mypy 283 source、Frontend 28 files/536 tests 通过。fixed_test 只在可丢弃测试数据库显式安装并以无网络 Scanner/内存 Asset 存储运行，普通 local 仍 Profile-empty/503；Provider、production、真实数据迁移、提交和推送未执行。
- 完成 `Local MVP 0.1.0` 发布记录：YHBX 明确选择 CR-025 Windows 本机收口，应用发布决定为 `GO / LOCAL MVP ONLY`；13 个 TEST/DEP 工作包继续保持 `partial`，不声明 production ready。记录绑定运行范围、AC-001/002/015/016、UAT、备份 ID、回滚步骤和已知风险；`origin` 已配置到 BOSS 指定的 GitHub 仓库，`release/local-mvp-0.1.0` 已使用普通 push 发布且未 Force Push。远程实际为 `protected=false`、Rulesets 为 0，因此远程治理保持 `partial`。
- 修复本地 baseline 自测对已配置安全 remote 的当前仓库断言：无 remote 时仍要求 `NOT_RUN`，存在 remote 时要求 `NOT_VERIFIED`；两种状态都继续保持 `BASELINE_TASK_STATUS=PARTIAL`，不把本地配置检查升级为托管平台分支保护证据。
- 生成发布前第 4 份权威备份并通过只读完整性/新鲜度巡检，随后将持久 `finaudit-local` 按当前工作树重建为镜像修订 `local-mvp-0.1.0`；dependency、bounded logging、restart policy 与受管 metrics 运行门禁全部 PASS。
- BOSS 直接批准并执行 `CR-026`：仅在 local/test runner 复用现有百炼密钥并启用评测批次 20，production 默认仍逐题；硬上限为 100 请求、50000 input tokens、CNY 10、零重试和 50 条失败即停。Provider 前离线与 PostgreSQL 双轮门禁通过；真实运行以 5 次请求、4971 input tokens、CNY 0.002486 得到 49/50、授权泄露 0，因 1 个 no-answer 假阳性立即停止，100 条和激活未运行。Collection、专用容器和确认环境变量残留为 0。
- 第二次获同边界 CR-026 单次授权后，真实运行完全复现 5 请求、4971 input tokens、CNY 0.002486、49/50 和 1 个 no-answer 假阳性；100 条仍未运行。捕获的 runtime case UUID 因当次未保存到冻结 source case ID 的映射而不能用于反查，runner 随后只做离线稳定 ID 遥测加固且未第三次调用；第二次授权、Collection、容器和确认变量均已消耗或清理。
- 更新当前 checkout 完整离线 checkpoint：baseline/Git governance/分支保护 fixture/测试资产正负门禁、Backend `3086 passed / 142 skipped / 1 warning`、Ruff check/format 497 files、mypy 271 sources、pip check、Frontend typecheck/26 files/527 tests/147-module build 全部通过。wrapper 仍明确记录 PostgreSQL、Compose、浏览器、Provider、remote 和 production 为 `NOT_RUN`，不把独立运行证据混入离线结论。
- 重跑并通过当前 checkout 的财务与 HTTP 安全运行门禁：财务浏览器 completion 接受 2 个文件/绑定和唯一合同、发票、供应商、主关系、审核执行、ready 报告及 5 个纯角色 Actor；HTTP 安全栈通过 protected metrics、bounded logging、Worker 自动重启、ClamAV、制度双人审批、安全专用 100 条集、真实 Qdrant 与浏览器/数据库 Prompt Injection 拒答。门禁过程中修复 Nginx `/metrics` 重复 `nosniff`、验证器旧指标名和 detach 自动重启探针；专用栈、卷和受管测试 Secret 已清理。
- 新增 Local MVP 持续运维闭环：Compose 十个长期服务改用 Docker `local` 5×10 MiB 日志上限，`-OperationsReadiness` 在持久栈核对依赖、restart policy、日志和 metrics；新增只读备份完整性/新鲜度审计，三份备份通过，最新备份完成隔离恢复、派生重建、运维门禁与停止后冷启动。新增 OpenAPI operation meta-gate，当前 89 个 `/api/v1` operationId 唯一且均由 Backend 契约测试显式冻结。13 个 TEST/DEP 行已引用 `docs/testing/test-operations-evidence-2026-08-18.md`，未完成的代表性质量、正式安全/性能/RPO-RTO 与 production 边界继续保持 `partial/NOT_RUN`。
- 修复本地性能证据的 CR-024 漂移：首次三轮数值 PASS 暴露机器 JSON 仍写 `https-nginx-backend`，该轮不作为当前 transport 标签证据；改为 `http-nginx-backend` 并增加单测后，同栈新 run 三轮再次 PASS。列表和上传受理 P95、20 件批次/重放、部分失败、21 件 413 及三审核任务唯一结果均通过，专用栈和测试镜像已清理。
- 完成 `synthetic-benchmark-owner-delegated-review-v1`：BOSS 委托的 local/test Agent 技术复核处理原 18 条队列（12 条原样接受、6 条修订接受），并重写复核全部 20 条合理 `no_answer`；100/50 技术集明确 `human_review_claimed=false`。获批单次真实运行完成 36 条款处理、两批百炼索引并达到 `ready`，随后在 50 条门禁返回 `MVP-UAT-050_EVALUATION_FAILED` 并立即停止；100 条 `formal_release` 与激活未运行，零重试，Collection/容器残留 0。精确 usage 未持久化，只记录最多 52 请求、50000 input tokens、CNY 1 元安全上界；runner 已补安全失败遥测但不追溯补造数据。
- 新增 Chrome `151.0.7922.138` 本地原生键盘证据：登录表单 Tab、Enter 登录、authenticated shell/dashboard Tab 和 skip-link 聚焦 `MAIN#main-content` 通过，console warning/error 为 0。Edge 已安装但控制因 URL 安全策略未形成证据；屏幕阅读器和全 P0 路由矩阵仍为 `NOT_RUN`。
- 通过官方 winget 安装 LibreOffice `26.2.5.2`；PDF→ODG headless 路径可完成，但 Calc 对项目和最小单单元格 XLSX 的 headless 打开/转换均超过 180 秒未完成，UNO 连接未就绪；Windows GUI 直接启动也未暴露任何窗口或进入文件打开。测试进程和临时制品已清理为 0，因此没有把依赖安装升级为 XLSX 兼容性通过。
- 新增 `synthetic-policy-corpus-v1` 与 `synthetic-benchmark-candidate-v1`：纯标准库确定性生成报销、供应商、发票、合同、审批权限和审计留痕 6 个虚构制度族、12 个无重叠版本、36 条证据条款，以及 120 条绑定基准日期/允许制度/预期或禁止证据的候选。100 条提议集为 `60 answerable / 20 no_answer / 20 unauthorized`，固定 50 条 `mvp_uat` 为 `30/10/10`；覆盖同义改写、有效期、制度版本与跨权限。重复、矛盾、证据、权限和自然度 8 项自动检查均为 0 异常；18 条已整理为逐项问题/日期/权限/证据对照的人工复核工作单，但结论仍为空且状态保持 `pending_human_review / not_submitted`。生成与工作单阶段 Provider 请求为 0，未创建已批准数据集、未执行 `formal_release`、未激活索引或改变 AC。后续 50+100 两次评测复用兼容索引预估 150 请求/12497 input tokens/CNY `0.006249`，含一次 36 条语料索引则为 152 请求/16576 tokens/CNY `0.008288`；真实运行仍需独立授权。
- 新增 `synthetic-non-acceptance-v1` 本地检索候选资产与纯标准库生成器：从仓库 P-001、RET-001 和安全负例确定性生成 120 条候选，按规范化问题 SHA-256 去重为 100 条并固定 50 条子集；100 条覆盖 `60 answerable / 20 no_answer / 20 unauthorized`，50 条覆盖 `30/10/10`，四个制度锚点均衡分布。来源清单、覆盖矩阵和 8 项证据核验随资产固化；生成阶段 Provider 请求为 0，未访问数据库、未激活索引、未提交审批或设置运行 tier。按当前每题一次 Embedding 请求估算，复用索引需 50/100 次请求，重建四条源分块需 51/101 次，CNY 上界为 `0.001760/0.003458`；这不是正式质量、UAT 或 AC 证据。
- 在 BOSS 单独再授权的 local/test 边界内完成一次真实百炼知识库 E2E：受保护运行器按知识库构建 → Worker 索引 → Qdrant green → Top-5 → 5 条 smoke 评测执行，实际 7 次 Provider 请求、3231 input tokens、CNY 1616 microunits、15 个 1024 维成员、Top-5 返回 5 条、5/5 smoke 通过，并生成 7 个 completed attempt / 14 个 Event v2。100 条 `formal_release` 门禁未绕过，激活按设计拒绝且索引保持 `ready`；临时 Collection 与 PostgreSQL/Qdrant 容器均已清理。该授权已消耗，不构成代表性质量、production、UAT 或正式 AC。
- 批准并实现 `CR-022 / option-A / event-policy-v2 / USD-CNY-only / no-fx`：Event v1 保持回放兼容，Event/Policy v2、迁移 `20260817_024`、ORM、Repository 与 OPS-005 使用 `cost_currency + *_cost_microunits`，Chat 固定 USD、百炼 Embedding 固定 CNY，禁止汇率换算和跨币种汇总。真实 Embedding 结果只有在 completion 与 Query、索引批次或评测事实同一 PostgreSQL 事务提交时才可采用。
- 新增真实 AI 的 Redis 跨进程运行门禁：使用 Redis server time 与 Lua 原子维护 `rag/async_generation/embedding` 三个独立并发、RPM、TPM、burst 池和按模型目标隔离的滚动熔断/单 half-open probe。Chat/Embedding 在 durable reserve 前取许可，拒绝时不写 started；Provider 成功但 Redis 完成失败时结果不可采用。29 项聚焦测试和锁定 Redis 7.4.9 digest 的 3 项 Redis 专项集成测试通过，当前 Redis/Celery wrapper 共 4 项通过；临时容器已清理。两次分别授权的受限付费百炼运行均已通过并消耗授权，production 和正式 AC 仍为 `NOT_RUN`。
- 新增 `docs/testing/p0-current-completion-audit.md`，逐模块覆盖全部 86 个矩阵任务，并把本地实现/运行证据与正式 AC/production 阻断分开；矩阵仍为 `2 implemented / 84 partial / 0 accepted`，没有把局部测试升级为正式验收。
- 新增并同步 `CR-021` local/test 批准 Profile，随后由 `CR-022` 升级为 `minimax-m3-bailian-qwen37-local-v2`：保留 `MiniMax-M3` Chat，并将百炼 `qwen3.7-text-embedding`（OpenAI-compatible、1024 维、单批 20）通过 `AiGateway` 接入 Backend RAG、Worker 索引构建和检索评测；Policy、Settings、endpoint、模型、维度、批次、deadline、CNY 价格/预算与响应边界在启动时精确绑定。旧 Hash 索引不能与真实向量混用，切换后必须新建索引版本。首次获授权的付费 smoke 用固定两条短合成文本和单次尝试，验证 43 input tokens、2 个 1024 维向量、Event v2 `succeeded` 与 CNY 22 microunits 实际费用；未输出密钥、输入或向量。后续完整链证据见本节首项；两次授权均已消耗。代表性质量、production 与正式 AC 仍为 `NOT_RUN`。
- 加固离线应用启动测试：共享夹具显式固定 `AI_PROVIDER_CALLS_ENABLED=false`，不再受本机默认配置源影响而误进入 live Profile。纳入两套合成候选资产与人工复核工作单后的完整 `verify-local-offline` 通过 Backend `3038 passed / 141 skipped / 1 warning`、Ruff 493 files、mypy 271 sources、Frontend 26 files/527 tests/147-module build 和全部基线/负向门禁；独立 PostgreSQL 16.14 Full wrapper 在 `20260817_024` 上连续两轮通过。Provider 网络只在此前另行授权的受限 smoke/E2E 中运行；remote、production 和正式 AC 仍为 `NOT_RUN`。
- 接通 `MiniMax-M3` 本地/测试真实 Chat Provider：合同、发票、知识问答、风险解释和报告草稿统一经 `AiGateway -> DurableAiCallEventSink -> PostgreSQL` 审计链调用，Provider 请求在业务事务外执行，采用前重新校验事实版本并将业务事实与 AI 调用终态原子提交；新增内部 `OPS-005` AI 调用审计查询。真实合成烟测覆盖五条业务流并记录 7 次物理尝试，结果为 PASS；该证据不包含生产环境、真实业务数据、正式准确率或正式 AC。
- 增加真实 AI 输出的严格结构化解析、修复预算、网络白名单/解析后 IP 固定、响应大小/超时/Token/费用边界，以及合同/发票代表性数据集验收计算器。计算器固定合同/发票核心字段与 `85%/95%/99%` 阈值，但输出只判定指标是否达标，不签署数据集代表性、审批真实性或正式验收；当前尚无已审批正式合同/发票评测集，AC-001～AC-016 仍为 `0/16 ACCEPTED`。
- 新增受独立 Bearer Token 保护的内部 `/metrics` Prometheus 文本端点及 Nginx 精确路径转发，只暴露低基数 HTTP/进程指标；尚未接入生产采集、告警或 SLO 平台。数据库迁移头现为 `20260817_024`：既保存 AI 风险解释和报告草稿状态/摘要，也提供 Event v2 USD/CNY 通用费用投影与严格升降级边界。
- 新增代表性性能证据评测器，固定复算列表、上传受理、清晰发票、二十页合同、Top-5、完整 RAG 六项 P95，以及上传异步语义和至少三个审核任务并行的零丢失/零重复采用；输入必须记录同一参考环境/模型/文档/并发 Profile 下连续至少三轮原始样本，任一失败或超时样本都会使该轮失败。计算输出始终为 `formal_acceptance_status=NOT_DETERMINED`；当前没有已审批正式输入，不能替代正式性能、UAT 或 AC。
- 旧的 Chat-only live Profile 已由 `CR-021` 组合 Profile 取代；离线模式仍使用 `deterministic-hash-v1`，真实模式使用独立的百炼索引身份。正式检索评测、生产 Secret/CA/OCR/备份/监控、DAST、屏幕阅读器/完整键盘、代表性性能与 LibreOffice 验证继续保持 `BLOCKED/NOT_RUN`。
- `CR-021` 前的 2026-08-17 完整 `verify-local-offline` checkpoint 为 Backend `2985 passed / 136 skipped / 1 warning`、Ruff 478 files、mypy 267 sources、Frontend 26 files/527 tests/147-module build 全部 PASS；本轮未重复 Frontend。PostgreSQL 16.14 Full wrapper 的最近独立证据仍是唯一 head `20260816_023` 连续两轮各 146 项 PASS，本轮也未重复数据库门禁。上述仍不替代 production migration、UAT 或正式 AC。
- 建立 `main` 的首个本地 Git revision：纳入源码、活跃/归档规格、测试资产、治理脚本和文档绑定的 `.finaudit/approval-artifacts`，并忽略可再生的 `tmp/`、`.codex-artifacts/` 与本地锁文件。仓库级 hooks 和 commit template 已启用；没有配置 remote/upstream，没有推送、标签、发布或远程分支保护证据。
- 增加 `report-artifact-compatibility-v1`：用固定正式报告负载在宿主与当前本地 Backend 镜像分别生成 PDF/XLSX，比较 PDF 提取文本和 XLSX 规范化单元格指纹；两份 PDF 字节相同且均由 Poppler 渲染，三页视觉检查无裁切/重叠，两份 XLSX 均由 Microsoft Excel 以禁用宏、只读方式实际打开并核对工作表/关键单元格。XLSX 压缩包字节不同但业务内容一致；LibreOffice 未安装，production、UAT、AC-014 与正式 AC 仍为 `NOT_RUN`。
- 对报告页执行桌面与 390×844/375 px 窄屏自动化可访问性检查：唯一 `main`/`h1`、无重复 ID、标题层级、16 个可聚焦元素命名、PDF iframe title 和页面级零横向溢出均通过；主导航按既有设计在自身容器内横向滚动。浏览器控制层不能可靠模拟原生 `Tab` 路径，且未接屏幕阅读器，因此完整键盘与正式可访问性继续为 `NOT_RUN`。
- 完成共享 `PageTabs` 的局部键盘与 ARIA 增量：使用 roving `tabindex`，支持左右方向键循环和 `Home`/`End` 聚焦切换，发票与知识库详情的 `tabpanel` 关联到对应标签。257 个聚焦前端用例、typecheck、build 通过；一次隔离报告浏览器 Gate 在真实发票详情页核对切换前后 selected/tabindex/focus/`aria-labelledby` 并保持 console warning/error 为 0。浏览器控制层仍不能可靠重放完整原生 `Tab` 路径，屏幕阅读器和正式可访问性继续为 `NOT_RUN`。
- Docker Scout 1.23.1 对当前 Bookworm Backend 镜像 `44b77246577e` 复现 `2C/2H`，四项均来自 `perl-base` 且 `--only-fixed` 为 `0C/0H`；锁定官方 Python 3.10.20 Trixie digest 的候选构建仍是相同 `2C/2H`，因此已恢复原 Bookworm 基线并删除候选镜像。没有创建 VEX 或风险接受，production“严重/高危为 0”门槛仍未通过。
- 新增 `CR-003-R2` 特权授权 active-baseline successor 候选：绑定 R1 base snapshot 与 post-CR-011-R4 的 11 项 pre identity，补齐 action-time 资格矩阵、数据库唯一 time owner、trigger 安全边界、`20260807_008/down=007` 两空表 storage-schema Gate 及旧 R1-only 下游隔离。Gate A 静态签署性复核已 PASS，但仍为 `NOT APPROVED / 0/9`；未同步 Request/manifest/pin，未创建 008，也未授权 Repository/Service/API runtime、真实数据、网络、部署或 production。
### Added

- 接通合同详情的补充协议字段级真实 UI：Header 可打开严格详情，`financial.read` 保持只读，`contracts.manage` 可在服务端允许状态下用 CAS、原因、完整证据与幂等键替换整组变更并确认/拒绝；基准日期直接读取 Backend 有效字段并并列展示原始值、有效值和来源协议。Frontend 新增严格 decoder、权限/重试/冲突/Abort 测试，Backend 同步把“只有 bbox、无完整证据”在 API 边界拒绝为 422，避免落到 PostgreSQL 约束错误。独立真实浏览器门禁已在全新 PostgreSQL 中完成 `amount: 100.25 → 120.50`、完整 block/page/quote 证据、人工确认与基准日生效查询，受保护 manifest 核对行版本、追加日志和 PUT/POST 幂等记录后返回 `accepted`。2026-08-17 当前 checkout 又以两个浏览器标签真实复现旧版本 `PUT 409`，完成另一协议拒绝，并让系统管理员、财务、审计、合同和只读五个 Actor 逐一登录；最终 manifest 核对两协议版本、三条操作日志、三条成功幂等记录和 `PUT 200/409 + POST 200/200`，脚本输出 `SUPPLEMENTARY_AGREEMENT_BROWSER_GATE=PASS`，专用容器最终为 0。浏览器运行同时暴露并修复通用 `StatusTag` 对 `unconfirmed/pending_confirmation` 的中文映射缺口；正式业务 UAT、production 与 AC 仍为 `NOT_RUN`。
- 修复 390 px 窄屏下全局 224 px 侧栏挤压内容并造成 document horizontal overflow：760 px 以下改为单列 Shell、两行 Topbar、可横向滚动主导航、正常流侧栏、单列卡片/表单和内部滚动宽表。390×844 真实浏览器重测页面级无横向溢出，桌面构建未回归；完整正式可访问性仍为 `NOT_RUN`。
- 修复报告浏览器测试夹具把 `AuditTaskCreateRequest.invoice_ids` 传为 tuple 导致启动失败，改为符合严格 Schema 的数组。后续可丢弃运行已用于上述补充协议读路径和响应式检查，但 `/goal` 继续边界在 wrapper 正常收尾前终止，未产生新的 `REPORT_BROWSER_GATE=PASS`；精确归属容器/网络均已清理，不能把该增量检查升级成完整报告门禁通过。
- 更新上述切片后的当前 checkout 完整离线门禁：Backend `2895 passed / 133 skipped / 1 warning`、Ruff check/format 440 files、mypy 245 sources、pip check、Frontend typecheck/26 files/526 tests/147-module build 全部 PASS；baseline 和测试资产正负门禁也通过。离线脚本仍明确把 PostgreSQL current-head、Compose、Browser、Provider、remote 和 production 记为 `NOT_RUN`，正式 AC 保持 0/16。
- 重跑并通过 2026-08-16 当前 checkout 的 `financial-loop-browser-v1` 受保护门禁：真实浏览器完成合同/发票 DOCX 上传、Scanner、Dispatcher/Celery Worker、专用提取、完整事实修正与人工确认、供应商原子修正/激活及双来源复用、主合同关联、15 规则审核、财务复核、ready PDF/XLSX 和五纯角色权限矩阵。首轮把供应商保存与确认拆成两次写入，被最终 manifest 精确拒绝并清理；最终全新重跑只产生 1 条聚合纠错、2 次 resolve、1 次 update，completion 返回 `accepted`，页面 console warning/error 为 0，脚本输出 `FINANCIAL_LOOP_BROWSER_GATE=PASS`，专用容器最终为 0。报告 PDF blob iframe、XLSX 下载动作及数据库 hash/size 均已核对；Excel/LibreOffice 打开、production Scanner/OCR/TLS、真实 Provider、UAT、production 与正式 AC 仍为 `NOT_RUN`。
- 修复报告/财务浏览器门禁复用本地缓存 PostgreSQL/Redis 镜像时的标签解析误报：标签 inspect 不能给出唯一完整 SHA-256 ID 时，只从本地 `image ls --no-trunc` 解析唯一候选并按不可变 ID 复核；运行继续固定 `--pull never`，PostgreSQL data tmpfs 为 1 GiB。最终浏览器门禁输出不可变 PostgreSQL/Redis 镜像 ID；该修改不下载镜像，也不改变 production 配置。

- 修复显式 PostgreSQL current-head 门禁对本地缓存镜像的解析：标签 inspect 未返回唯一完整 ID 时，仅从 `docker image ls --no-trunc` 解析唯一 SHA-256 候选并按不可变 ID复核，`docker run` 继续使用 `--pull never` 且固定该 ID；负向自测覆盖 fallback、1 GiB tmpfs、Secret 不进入参数、身份清理和失败不得伪造 PASS。持久 Sink 适配加入后的当前 checkout 已在 PostgreSQL 16.14 上完成 Full wrapper 双轮 143/143，输出不可变 `POSTGRESQL_IMAGE_ID` 与 `POSTGRESQL_CURRENT_HEAD=PASS`；这仍不是 production migration、UAT 或正式 AC。
- 增加 `durable-ai-call-event-sink-v1` provider-disabled 适配 primitive：`AiCallReserveContext` 要求调用方显式冻结同源预算、绝对 monotonic deadline 和最小处理余量，`DurableAiCallEventSink` 将 AI-005 持久六态逐项映射到 AI-001 `AiCallEventSink` Port。只有首次 reserve 或同一实例/同一 scope/尚未发送的 commit-unknown 恢复窗口会产生一次性 `SendPermit`；采用许可还要求同 scope 已消费发送许可，跨 scope/新实例 replay、冲突、预算/deadline 耗尽、未发送 completion、`outcome_unknown` 和 `late_completion` 均失败关闭。24 个聚焦用例、隔离 PostgreSQL 16.14 `-Scope AI` 和完整离线门禁通过；离线结果为 Backend `2888 passed / 133 skipped / 1 warning`、Ruff 439 files、mypy 245 sources、Frontend 515 tests/145-module build。Gateway、共享事务业务事实采用、真实 Provider、Redis 限流/熔断、公共 OPS-005、production 与 AC 仍为 `NOT_RUN/BLOCKED`。
- 增加 `ai-extraction-prompt-v1` 内部 primitive：合同与发票分别固定独立 `purpose/prompt_id/v1/hash`，解析块只以单一 parse version、唯一 block/位置、稳定排序的 canonical JSON 进入 user content，正文不插值到 system instruction。两份 Prompt 都把块文本定义为不可信数据，只允许逐字证据、无证据返回 `null`，禁止模型确认、审批、激活、决定真伪/重复、输出风险或付款结论；16 个聚焦用例覆盖 identity/hash、稳定渲染、提示注入保留为数据、bbox 快照和失败关闭。AI-003 从 planned 升为 partial，矩阵为 `2 implemented / 83 partial / 1 planned`；完整离线门禁为 Backend `2864 passed / 133 skipped / 1 warning`、Ruff 437 files、mypy 244 sources、Frontend 515 tests/145-module build。Provider、Gateway/EventSink、业务输出采用与数据库写入仍未接线，且发票无证据币种与现有非空/CNY 默认合同的差异仍须先闭合。
- 升级 Backend 运行依赖 `pypdf 6.13.0 → 6.14.2`，修复不可信 PDF 内联图像解析可造成无限循环/资源耗尽的两个高危问题；Docker 构建完成后移除运行时不需要的 `pip/setuptools/wheel`。Docker Scout 1.23.1 对同一 revision 的本地镜像复扫显示 Frontend `0C/0H`，Backend 从 `2C/6H` 降到 `2C/2H`，且 `--only-fixed` 为 `0C/0H`；剩余四项均来自 Bookworm Perl 并标记 `not fixed`，因此 production 零严重/高危门槛仍未通过。新 Backend 镜像以 `10001:10001` 运行，目标依赖、构建工具移除、应用模块导入和完整离线门禁均已验证。
- 增加 `local-ai-audit-crash-recovery-v1` 显式故障注入门禁：只允许 Provider 关闭的 `finaudit-ai-audit-*` 专用 local Compose 栈；停止 Maintenance 后播种同一调用的 started/completed Outbox，以 `ai_call_logs` 排他锁确认唯一真实投影事务，再 SIGKILL 精确容器并确认退出码 137。释放本次锁后等待精确 PostgreSQL PID 消失，恢复前核对两条 Outbox 仍为 pending/attempt 0 且无审计日志；同容器受管重启后两条事件各投影一次，只形成一条 succeeded 日志，结构化日志无 event/operation UUID，测试事实与专用栈最终零残留。门禁调试中修复播种自阻塞、PostgreSQL 锁等待断连时序和日志常量断言，只有最终完整 PASS 计入证据。该结果不调用 Provider，也不证明 AI-001/业务采用、公共 OPS-005、Redis 限流/熔断、Docker 自动重启、主机断电、production 或正式 AC。
- 增加 `ai-call-audit-runtime-v1` provider-neutral 持久审计 primitive：`AiCallAuditRepository/Service` 以 PostgreSQL transaction advisory lock 完成发送前 durable reserve、物理尝试/单请求与总 Token/最坏费用/deadline 门禁、同 ID 幂等重放/冲突和 completion；Outbox 消费器按 sequence 原子投影到 `ai_call_logs`，Maintenance 补偿 `outcome_unknown` 并保留不可改写终态的 `late_completion`，异常终态只输出无 ID/载荷 WARNING。新增 `-Scope AI` 隔离 PostgreSQL 16.14 门禁，覆盖并发预算、业务事务回滚、乱序等待、消费者事务中断后新实例恢复、未知版本/坏载荷隔离和内部安全摘要；提交结果未知另有纯单测证明 Service 不返回伪成功。AI-005 从 planned 升为 partial，矩阵为 `2 implemented / 82 partial / 2 planned`；OS 级 Maintenance 故障证据由独立 local Compose 门禁补齐，AI-001/业务采用、公共 OPS-005、Redis 限流/熔断、真实 Provider、production 与正式 AC 仍为 `NOT_RUN/BLOCKED`。
- 增加 `local-knowledge-index-crash-recovery-v1` 显式故障注入门禁：合成制度真实经过 Nginx/ClamAV/Worker 提取和双人审批，停止 Worker 后排队唯一 `knowledge_index_build`，再以 `operation_logs` 排他锁把 attempt 1 稳定阻塞于最终 `ready + log + Job finish` 事务。强杀前逐项重算确定性 Embedding、最小 payload 和 float32 向量摘要，确认真实 Qdrant 点与 PostgreSQL 成员 Hash 一致；SIGKILL 精确 Worker 并终止唯一孤儿后，索引仍为 `building/row_version=1`、consistency/ready 日志为空，而物化 Hash 与 Qdrant 点保持同一聚合摘要。Maintenance attempt 2 以相同 Point ID 幂等 upsert，最终唯一 `ready/row_version=2` 索引、`failed/LEASE_EXPIRED → succeeded`、唯一 Outbox 和严格两条生命周期日志均通过。第一次运行在故障注入前因 UUIDv5 被误限定为 UUIDv4 安全失败；第二次旧镜像运行暴露校验辅助进程读取被锁表的自阻塞，精确释放锁、清理并修复后，只采用新 revision 全新栈的完整 PASS。专用容器、卷、网络、helper、Secret 和端口最终均为 0；该证据不证明 Qdrant 节点故障/容量、真实 Embedding Provider、自动重启、主机断电、production、正式 RPO/RTO 或 AC。
- 增加 `local-report-generate-crash-recovery-v1` 显式故障注入门禁：真实 `audit_execute` 与 medium/high 双角色复核完成后，在 Worker 停止时排队唯一 `report_generate`，以 `operation_logs` 排他锁把 attempt 1 稳定阻塞于最终事务。此时确定性 PDF/XLSX 已逐字节写入 MinIO，而数据库制品 locator/hash/size、ready 状态和生成日志仍未提交；精确 SIGKILL Worker 并终止唯一孤儿后，数据库保持零制品事实，两个对象仍以相同字节存在。Maintenance attempt 2 最终收敛唯一 ready 报告，客户端详情/列表/PDF 预览/XLSX 下载与响应头、`failed/LEASE_EXPIRED → succeeded`、唯一 Outbox/生成日志及精确对象身份均通过。运行中修复 Nginx 与 Backend 叠加 `X-Content-Type-Options`：API 代理现在隐藏上游值并由边缘统一输出一次。专用容器、卷、网络、helper、Secret 和端口最终均为 0；该证据不证明自动重启、主机断电、对象垃圾回收、production、正式 RPO/RTO 或 AC。
- 增加 `local-audit-execute-crash-recovery-v1` 显式故障注入门禁：在全新专用 local Compose 栈中播种 run-scoped 已确认合同/发票和完整 15 规则目录，由真实财务账号经 `/api/v1/audit-tasks` 创建任务，再以专用 PostgreSQL 会话持有 `rule_executions` 表锁。Job/Step 已提交为 attempt 1 `running/evaluate` 且 Worker 的业务事务形成唯一未授予锁请求后，对精确归属 Worker 执行真实 SIGKILL 并确认退出码 137；门禁在注入锁仍持有时精确终止唯一孤儿等待后端，确认事务回滚与等待锁归零，再释放注入锁。恢复前任务、执行、快照和 Job 保留，规则、风险与执行日志均为 0；Maintenance 随后以 attempt 2 收敛。客户端审核列表、任务详情和执行详情核对固定 15 条规则及 `RULE-002/high`、`RULE-004/medium` 两条待复核风险；数据库核对 attempt 1 `failed/LEASE_EXPIRED`、attempt 2 `succeeded`、冻结快照 Hash、15 条唯一规则、2 条唯一风险和 1 条追加式执行日志。依赖恢复 ready，专用容器、卷、网络、运行时 Secret、测试镜像和端口最终均为 0。该结果不证明自动重启、知识/报告 Job、主机断电、production、正式 RPO/RTO 或 AC。
- 增加 `local-invoice-extract-crash-recovery-v1` 显式故障注入门禁：在全新专用 local Compose 栈中，以 run-scoped PostgreSQL 会话持有 `invoices` 表锁，让正常完成文件处理后创建的唯一 `invoice_extract` Job 在 attempt 1 的 `extract` step 内稳定阻塞；对精确归属 Worker 执行真实 SIGKILL 并确认退出码 137。释放精确锁后，数据库核对发票、明细、文件关联和提取日志仍全部为 0；显式重启同一 Worker 后，Maintenance 在租约过期和宽限后以 attempt 2 收敛。最终核对 attempt 1 `failed/LEASE_EXPIRED`、attempt 2 `succeeded`、唯一发票、1 条明细、1 个文件关联、13 个字段证据、1 条明细证据和 1 条追加式提取日志；客户端详情、证据与上传时 SHA-256 对应的原件/ETag 一致。测试未向生产 Executor 增加故障分支；专用容器、卷、网络、运行时 Secret、helper、测试镜像、临时状态和端口最终均为 0。该结果不证明自动重启、知识/审核/报告 Job、主机断电、production、正式 RPO/RTO 或 AC。
- 增加 `local-contract-extract-crash-recovery-v1` 显式故障注入门禁：在全新专用 local Compose 栈中，以 run-scoped PostgreSQL 会话持有 `contracts` 表锁，让正常完成文件处理后创建的唯一 `contract_extract` Job 在 attempt 1 的 `extract` step 内稳定阻塞；确认 Job/Step 已为 `running` 且合同、字段、文件关联和提取日志均为 0 后，对精确归属 Worker 执行真实 SIGKILL 并确认退出码 137。门禁释放精确数据库锁、再次核对零事实、显式启动同一 Worker，再由 Maintenance 在租约过期后以 attempt 2 收敛；最终核对 attempt 1 `failed/LEASE_EXPIRED`、attempt 2 `succeeded`、唯一合同/关联/13 个证据字段/追加日志，以及客户端详情和上传时 SHA-256 对应的原件/ETag。测试未向生产 Executor 增加故障分支；专用容器、卷、网络、运行时 Secret、helper、测试镜像、临时状态和端口最终均为 0。该结果不证明自动重启、`invoice_extract`、知识、审核、报告 Job、主机断电、production、正式 RPO/RTO 或 AC。
- 升级本地性能门禁为 `local-performance-baseline-v2`：在原三轮列表、单文件受理和审核并发基础上，每轮新增默认最大 20 件真实 TLS multipart 批量上传及同键重放，全部 scan-only Job 必须完成；三轮后再验证 1 成功/1 `FILE_FORMAT_NOT_SUPPORTED` 的 207 部分失败与重放，以及 21 件请求固定 413 `BATCH_LIMIT_EXCEEDED`。PostgreSQL 按 run-scoped 文件名精确核对每个 run 仅有 61 个文件、61 个唯一 `file_scan` Job、61 个 attempt-1 scan step 和 61 个 published Outbox，超限名称零落库。全新栈三轮与同栈新 run 再三轮均 PASS；首个最终 run 的最大批次受理为 `415.348/426.256/464.568 ms`，均低于 3 s。两个专用项目的容器、卷、网络、运行时 Secret、helper、测试镜像与端口最终均为 0。该结果只覆盖小型合成 PDF 的 local 默认 20 件批次，不是正式参考环境完整容量、production 或 AC 证据。
- 升级 `local-worker-crash-recovery-v1` 为完整文件处理恢复门禁：标准库生成含 13 个合同字段的 run-scoped DOCX，以 `auto_process_requested=true` 创建唯一 `file_process`；暂停官方 ClamAV 后在 scan step 真实 SIGKILL 精确归属 Worker 并确认退出码 137，再显式启动同一容器。Maintenance 将同一 Job 回收为 attempt 2 并完成 `scan → parse → markdown`，正常 Worker 随后完成唯一 `contract_extract`。客户端核对 `stored/clean/succeeded`、逐字节原件/ETag 与未确认合同详情；PostgreSQL 核对两次 attempt 的完整 steps、提取 Job、文件合同绑定、13 个字段/证据及唯一追加日志。专用容器、卷、网络、运行时 Secret、helper、测试镜像与端口最终均为 0；该证据不证明 Docker 自动重启、提取 Job 本体强杀、主机断电、production、RPO/RTO 或 AC。
- 补齐本地浏览器 Prompt Injection 增量门禁：`local-security-baseline-v1` 现在额外创建一次性审计角色并输出不含 Secret 的 browser origin/username/run ID。浏览器正确拒绝短期自签名 CA，测试未绕过警告或修改系统信任；新增受显式 token 保护且只绑定 `127.0.0.1` 的 `local_tls_browser_relay.py`，把浏览器 HTTP 同源请求通过 TLS 转发到原 Nginx，并固定改写 Host/Origin/Referer、保留 Secure Cookie 属性和有界请求/响应。真实浏览器已完成登录、工作台到问答页导航、活动知识库加载、run-scoped 直接注入提交和可见 `PROMPT_INJECTION_DETECTED` 拒答，控制台 warning/error 为 0；新增数据库终审确认唯一 Query、问题 SHA-256、Trace、空答案/引用、零命中与追加式操作日志，依赖和容器日志复核通过。专用容器、卷、网络、运行时 Secret、镜像与中继最终均清理为 0；该证据不证明浏览器直接 CA 信任、正式 DAST、真实 Provider、production 或 AC-015。
- 扩展 `local-security-baseline-v1` 的上传内容 Prompt Injection 运行门禁：合成 PDF 真实穿过 loopback Nginx/TLS、官方 ClamAV、Worker、Markdown/Chunk、制度提交与独立审批、安全专用 100 条全 `no_answer` 合成集、索引构建/激活和真实 Qdrant；HTTP 直接注入在检索前拒绝，普通问题命中不可信制度后由 PostgreSQL 终审拒绝。两条响应、`qa_queries`、operation-log 与业务容器日志均通过无答案/引用/命中数/canary/系统提示泄漏检查，输出 `LOCAL_SECURITY_PROMPT_INJECTION_HTTP_QDRANT=PASS`；浏览器 Prompt Injection、代表性 50/100 条业务质量集、正式 DAST、Provider、production 和 AC 仍为 `NOT_RUN`。运行中先复现 Python float64 摘要与 Qdrant float32 持久化精度导致的正常成员误报 `INDEX_MEMBER_DRIFT`，现改为按 float32 字节规范化并保留非有限值/溢出失败关闭；同时修复 strict Pydantic 评测 DTO 拒绝合法 JSON enum/array 的公共 API 缺陷。两项修复均先有失败回归，再在全新隔离 Compose 项目完成整条安全基线 PASS。
- 增加上传内容 Prompt Injection 的 PostgreSQL 服务级集成回归：合成 PDF 真实经过文件摄入、Worker、Markdown/Chunk、制度独立审批、100 用例确定性正式评测和索引激活；直接注入问题在检索前拒绝，普通问题命中含注入内容的授权制度后在 PostgreSQL 终审拒绝。两条查询均持久化为 `refused/PROMPT_INJECTION_DETECTED`，无答案、引用、命中计数或哨兵/系统提示泄漏，操作日志仅保存安全状态。当前 Retrieval 8/8、完整 PostgreSQL 16.14 数据库目录 142 项连续两轮通过；测试使用内存向量 Adapter、确定性 Embedding/答案模型和合成 Scanner，不构成 HTTP/浏览器、真实 Qdrant、正式 DAST、真实 Provider、production 或 AC 证据。
- 新增 `20260815_021` 检索状态机完整性迁移：在旧 020 头上，隔离 PostgreSQL 16.14 回归真实证明可通过直接 `INSERT` 把索引、评测集和评测运行分别写成 `active`、`approved`、`passed`，绕过正式评测和转换门槛。新迁移先检查既有非初始事实，再以三个 `SECURITY INVOKER`、固定 `search_path` 的 `BEFORE INSERT` 触发器强制 `building`、`draft`、`running` 起步；不新增表、不改变 API。修复后 Retrieval 7/7、完整数据库目录 141 项连续两轮通过，正式 AC 仍为 0/16。
- 修复 `test-verify-baseline.ps1` 在 Windows PowerShell 窄终端中的误判：`Write-Error` 可能在单词中插入换行，旧自测按连续字符串匹配会把正确拒绝误报为“unexpected branch”。现在仅在断言错误短语、精确相对路径和合成敏感哨兵是否泄漏时折叠空白，不放宽退出码、失败语义、成功标记或脱敏要求；同一 TTY 负例门禁与完整离线门禁均通过。
- 增加 `local-security-baseline-v1` 显式本地安全门禁：仅允许独立 `finaudit-security-*` Compose 项目，真实验证 loopback TLS 与安全响应头、非法 Trace 上下文拒绝反射、Refresh Origin CSRF、五次失败锁定与防枚举、角色拒绝/存在性隐藏、篡改 Bearer、Trace 到操作日志映射、审计写入失败时业务事务回滚、操作日志 UPDATE/DELETE/TRUNCATE 不可变、合成密码哨兵不进入 Backend 日志，以及容器只读根文件系统、`no-new-privileges`、最小 capabilities 和唯一 loopback Frontend 端口。门禁发现并修复 Frontend/Nginx 未丢弃默认 capabilities 的加固缺口：现为 `cap_drop: ALL`，只补回官方镜像启动所需的 `CHOWN/SETGID/SETUID`。P0 单组织约束下无法构造跨组织 IDOR；该门禁未运行 Prompt Injection、完整审计链、正式 DAST、production 或 AC，提示注入另由独立 PostgreSQL 服务级回归覆盖。
- 增加 `local-performance-baseline-v1` 显式本地三轮门禁：仅允许独立 `finaudit-perf-*` Compose 项目，记录 Docker 29.6.1、20 CPU、8,223,621,120 bytes 内存、Worker concurrency 2，并固定每轮 20 次审核列表、20 次 TLS multipart 单文件上传受理和 3 个同时提交的审核任务。三轮列表 P95 `13.331/13.848/10.342 ms` 均低于 800 ms，上传受理 P95 `34.784/42.323/37.561 ms` 均低于 3 s；60 个 scan-only Job 全部完成，9 个审核任务均进入财务复核，数据库确认每个任务只有一个执行/Job/成功 step/published Outbox/snapshot 和 15 条规则结果。Worker concurrency 2 意味着该证据只证明三任务并发受理与无丢失/重复，不证明三任务物理同时执行。测试输入直接播种已确认审核事实，清晰发票、20 页合同、Top-5/RAG、批量容量、OCR、真实 Provider、正式参考硬件、production 与 AC 均未运行；专用容器、卷、运行时 Secret、helper 和精确测试镜像最终为 0。
- 更新 2026-08-16 当前 checkout 验证：完整离线门禁为 Backend `2848 passed / 133 skipped / 1 warning`、Ruff check/format 435 files、mypy 243 sources、pip check、Frontend 25 files/515 tests/typecheck/145-module build PASS；独立 PostgreSQL 16.14 current-head Full wrapper 对 `20260815_021` 当前完整 143 项连续两轮均 `status=ok` 并输出 `POSTGRESQL_CURRENT_HEAD=PASS`。默认离线门禁自身仍将 PostgreSQL、Compose、Browser、Provider、remote 和 production 记为 `NOT_RUN`，正式 AC 仍为 0/16。
- 增加 `local-worker-crash-recovery-v1` 显式故障注入门禁：在受管 local Compose 中暂停官方 ClamAV，使唯一 scan-only `file_scan` Job 进入 `running`，对精确归属 Worker 执行真实 SIGKILL 并确认退出码 137，再显式启动同一容器；Maintenance 随后回收过期租约并以 attempt 2 完成同一 Job。客户端核对 `stored/clean/succeeded`、逐字节原件预览和 ETag，数据库核对两次 scan step 为 `failed/LEASE_EXPIRED` 与 `succeeded`，恢复后依赖再次 ready；helper、临时状态、专用栈、卷和运行时 Secret 均清理为 0 残留。两轮门禁设计诊断分别证明手工 `docker kill` 不会触发 `unless-stopped` 自动重启，以及 PID namespace init 不能作为可证明崩溃路径；两轮都失败关闭且不计通过证据。最终结果只证明 scan-only 本地 SIGKILL + 显式受管重启 + 租约恢复，不证明自动重启、其他 Job 类型、主机断电、容量、production 或 AC。
- 扩展 `financial-loop-browser-v1` 隔离门禁：真实浏览器在合同/发票上传、专用 Worker 提取、人工修正/确认后，从合同创建并修正激活供应商，再由发票按同一税务身份精确复用该活动供应商，随后确认主合同关联、完成审核复核并读取 ready PDF/XLSX 报告。最终 PostgreSQL/MinIO manifest 强制唯一双来源绑定、单条聚合纠错、两次 `supplier.resolve`/一次 `supplier.update`、五个纯角色成功登录及完成任务/执行/报告制品；财务、审计、合同、只读、系统管理员的导航和禁止直达矩阵通过，页面日志 0 warning/error，PG/Redis/MinIO 测试资源清理为 0 残留。该证据仍是合成 loopback 本地 Profile，不证明正式可访问性、production 或 AC。
- 修复 PostgreSQL current-head 门禁的测试容量回归：当前 139 项完整数据库目录在旧 512 MiB data tmpfs 上可复现 WAL 峰值耗尽，PostgreSQL 明确以 `No space left on device` PANIC 退出；将专用 tmpfs 最小上调为 1 GiB，并由负例自检锁定该参数。PostgreSQL 16.14 对当前 head `20260814_020` 随后在同一隔离实例连续两轮各 139 项通过，输出 `POSTGRESQL_CURRENT_HEAD=PASS`，最终标签容器残留为 0；容量失败的中断运行不计作通过证据，也不代表正式容量/性能验收。
- 更新 2026-08-15 当前 checkout 完整离线质量 checkpoint：Backend `2738 passed / 129 skipped / 1 Starlette deprecation warning`、Ruff check/format 419 files、mypy 241 sources、pip check、Frontend typecheck/25 files/515 Vitest/145-module build 均 PASS；默认离线门禁仍将 PostgreSQL current-head、Compose、浏览器、Provider、remote 与 production 独立记录为 `NOT_RUN`，浏览器结论来自另行显式运行的隔离门禁。
- 增加 `file-upload-browser-v1` 显式本地门禁：真实浏览器 FormData/multipart 穿过 Auth Actor、FileIntakeService、PostgreSQL 文件/Job/Step/Outbox/operation-log 事务、应用 MinIO quarantine、Outbox Dispatcher、隔离 Redis/Celery Worker 与生产 ClamAV INSTREAM Adapter，使用合成 loopback clamd 端点把唯一文件/Job 恢复为 `stored/clean/succeeded` 并保持同一安全 Trace；reload/详情恢复、越权 `policy` 403 不落第二份事实、页面零 warning/error 及 PG/Redis/MinIO 清理均通过。MVP-VS-07/08 仅在本地隔离单文件范围标为 `VERIFIED`；production Scanner、解析后的合同提取/人工确认/关联/审核/报告完整业务闭环、批量上传、强杀/断电、容量、TLS/Nginx、production 和 AC 均未由该门禁证明。
- 更新 2026-08-15 当前 checkout 完整离线质量 checkpoint：Backend `2668 passed / 124 skipped / 1 Starlette deprecation warning`、Ruff check/format 382 files、mypy 218 sources、pip check、Frontend typecheck/21 files/488 Vitest/136-module build 均 PASS；Vitest 有一次 `ECONNRESET` 控制台噪声但测试与退出码通过。默认离线门禁仍明确把 PostgreSQL current-head、Compose、浏览器、Provider、remote 与 production 记录为 `NOT_RUN`，不能替代独立运行门禁或正式 AC。
- BOSS 于 2026-08-14 直接批准 `CR-018-R1/recommended-forward-v1` 并授权同步活跃 Request 后继续实施；四份活跃规格现已冻结供应商统一税务投影/三态/精确复用/锁序/聚合纠错，quote 与坐标缺失原因、禁 raw HTML 的 CommonMark/GFM table/复杂表格制品，P0 组织级知识权限、环境+Embedding 模型+维度 Collection、PG allowset→Qdrant must-filter→PG 终审、5/50/100 评测分级，应用内静态 15 规则整批发布，以及审核/high 风险/正式报告状态机。该同步关闭 GAP-001/008/020/064～066/068 的合同歧义，但不冒充对应 Schema、Router、运行证据或 AC 已完成；真实 Provider、部署和 production 仍未授权。
- 修复 `verify-local-celery-redis.ps1` 的测试资源清理：Docker label 校验改为解析完整 JSON，并同时核对容器 ID、`celery-redis` 用途标签与本次 run-id；身份不一致时拒绝删除，匹配时使用 `--volumes` 清理容器及匿名 Redis 数据卷，清理失败不再静默忽略。真实 Celery/Redis 集成 Gate 重跑为 `1 passed`、`CELERY_REDIS_BROKER_TRANSPORT=PASS`，最终同用途容器残留为 0；该结果不证明 production Broker、强杀/断电或完整 Compose。
- 交付 `qdrant-existing-collection-adapter-v1` 局部基础设施切片：Backend 新增不创建或枚举 Collection 的 Qdrant 1.10 HTTP Adapter，只接受配置中显式且已存在的 unnamed dense-vector Collection，启动式校验 `green`、向量维度与 distance；有界 upsert/delete 固定 `wait=true`，查询必须由调用方提供非空 Point ID 允许集，并在返回后再次拒绝任何越权 ID。单元测试 19 项及一次性真实 `qdrant/qdrant:v1.10.0` 容器的双点写入、单点授权检索、删除与空结果 round trip 均通过，容器最终零残留。该切片不选择 Collection 粒度，不创建 PG 索引成员/ACL/有效期/版本事实，不接 Embedding、激活、重建、评测或 RAG，因此 `GAP-001/008/020/065`、BETA-VS-05 和 AC-008/009/016 继续保持阻断/未完成；`KB-008` 仅从 planned 更新为 partial。
- 同步 2026-08-14 当前 checkout 证据：唯一 Alembic head 为 `20260814_016`，ORM/runtime catalog 为 30/57 表；PostgreSQL 16.14 current-head 完整目录连续两轮各 122 项通过并输出 `POSTGRESQL_CURRENT_HEAD=PASS`，专用 PostgreSQL/Qdrant 容器最终均零残留。完整默认离线门禁为 Backend `2577 passed / 109 skipped / 1 Starlette deprecation warning`、Ruff check/format 304 files、mypy 167 sources、pip check、Frontend 19 files/482 tests/typecheck/136-module build PASS；Vitest 进程记录一次 `ECONNRESET` 控制台噪声但无测试失败。矩阵依据已实现 Scanner、解析/OCR、发票提取、合同发票写链和 Qdrant primitive 的可执行证据更新为 `2 implemented / 48 partial / 36 planned`；剩余 27 表、供应商、Markdown/分块、完整索引/RAG、审核/报告、Compose、Provider、production 与所有正式 AC 状态不升级。
- 交付 TECH §6.14 `invoice-exact-duplicate-candidates-read-v1`：Backend 新增 `GET /api/v1/invoices/{invoice_id}/duplicate-candidates`，先重验数据库 Actor、`financial.read` 与源发票组织边界，再按当前持久化 `invoice_code + invoice_number + seller_tax_no` 逐字段数据库等值查询；排除源票、软删除和 voided 候选，保留 archived，以 canonical `{id,v}` UUID keyset、`LIMIT page_size+1` 和 `private, no-store` 返回严格列表。Frontend 发票详情新增独立候选区块、严格 cursor/响应 decoder、前后翻页、loading/empty/401/403/404/Trace/retry/Abort/stale 状态，并继续禁用全部重复写操作。`source_voided` 优先于 `incomplete_identity`，`ready + 空页` 只代表本次读取未命中；本切片不做规范化/模糊匹配，不确认重复或真伪，也不写 `duplicate_status`、风险、审核任务或 operation-log。普通 `READ COMMITTED` 的源锚点与候选查询不构成单一 MVCC 快照，现有索引前缀和 UUID tie-break 不是容量或性能证明。独立 PostgreSQL 16.14 current-head Gate 连续两轮各 90/90 PASS；浏览器、production 与 AC-005 仍为 `NOT_RUN`。`INV-004` 从 planned 升为 partial，矩阵更新为 `2 implemented / 39 partial / 45 planned`，其他任务与全部 AC 状态不变。

- 交付 TECH §7.5 `user-list-read-v1`：Backend 新增 `GET /api/v1/users`，先重验数据库 Actor 与 `users.manage`，只按 Actor 组织读取未软删除用户和 active 组织，使用 PostgreSQL `CITEXT username ASC,id ASC` keyset、`LIMIT page_size+1` 与 canonical `{id,username,v}` cursor，公开六字段并只投影当前有效的长期固定角色；响应固定 `private, no-store`。Frontend `UI-014` 删除全部 Demo 用户、筛选、创建表单和 Break-glass 假事实，接入严格 decoder、cursor 前后翻页、loading/empty/401/403/Trace/retry/Abort/stale 状态，所有写动作保持禁用并明确未接入。隔离 PostgreSQL 16.14 current-head Gate 连续两轮各 89/89 PASS，覆盖真实 admin login/JWT → Actor/`users.manage` → Router/Service/Repository → 同一 PostgreSQL；容器残留为 0，`TEST_DATABASE_URL` 与破坏确认变量恢复为 unset。当前 Repository 在 `READ COMMITTED` 下分语句读取用户页、数据库时钟和角色，不构成单一快照；cursor 可逆携带用户名，原始 query/cursor 必须保持日志脱敏边界。现有本地浏览器 Gate 尚未覆盖 `/users`，用户写 API、operation-log、production 和 AC-001 均未完成或 `NOT_RUN`，矩阵状态与全部 AC 不变。

- 交付 `handler-registry-loader-meta-v1` 离线元数据 Loader：仅接收调用方显式传入的 Registry Schema、Registry JSON 与精确 Input/Summary Schema bundle，验证严格 UTF-8/JSON、严格整数、Draft 2020-12、JCS/raw SHA-256、精确 bundle、排序、scope→step、summary 引用及可解析的本地 `$ref/$dynamicRef`，禁止远程 retrieval。6 份 test-only synthetic JSON 的聚焦测试 45 项通过，两路独立审查均为 `GO`。未接 Settings、package data、callable、Job、Celery 或 runtime import；生产 Registry/Schema/Input/Summary/Handler 制品仍为 `PENDING / NOT GENERATED / NOT APPROVED`，Job/Worker runtime 仍为 `NOT AUTHORIZED`，矩阵 `2 implemented / 38 partial / 46 planned` 与全部 AC 不变。

- 交付 `local-minio-restart-persistence-v1` 显式真实运行门禁：固定 MinIO 策略 `Action/Resource` 集合的规范化比较后，连续两轮真实 Gate 均 PASS；每轮均 bootstrap 七个 Bucket 并验证 quarantine 权限，以原应用身份写入固定合成 PDF 的精确 quarantine 对象键，由 root 核验逐字节内容、大小、Content-Type 与 SHA-256 metadata；随后执行不带 `-v` 的 `compose down`、`up --pull never` 并确认新容器，原 root/应用身份仍可复核对象，最后由原应用身份删除、root 确认对象不存在。start/stop/wrapper 均 PASS；最终专用容器与网络为 0，managed/root/app/gate 环境变量均不存在，named volume 保留。修复前曾有一次运行因 MinIO 返回策略集合顺序不稳定而报 `APPLICATION_POLICY_DRIFT`，该失败轮不作为通过证据。该门禁只证明单服务本地 named volume 上一次受控冷重启的固定对象持久性与同一应用身份连续性；不证明 FILE 数据库/Router/Job/Scanner、通用迟到 timeout reconcile、崩溃恢复、一般耐久性/容量、备份恢复、完整 Compose、TLS、production 或 AC-016。任务状态、追踪矩阵 `2 implemented / 38 partial / 46 planned` 与全部 AC 保持不变。
- 交付 `report-pdf-writer-v1` 离线内部 Profile：新增 `backend/app/reports/pdf_writer.py` 与共享 `artifact_payload.py`，以精确锁定的 `ReportLab==5.0.0` 把冻结 `ReportPayload` 写为固定 A4、逐页标明“内部离线预览（非正式审核报告）”的有界 PDF；控制符、双向控制符和换行分隔符按可见字面量转义，引用仅展示未解析 ID，128 页/8 MiB 及共享负载边界失败关闭。两份 AOSP `android-15.0.0_r25` 字体在渲染前校验固定 SHA-256，作为内嵌 TrueType 字体携带 `/ToUnicode`，原始 NOTICE/README 与本地来源记录共 5 个资源已通过离线 wheel 构建、安装和入包检查；上游四份原始文件的字节与哈希逐项一致。PDF unit 9 项、PDF+XLSX 相关聚焦 80 项通过；代表性 3 页 A4 制品经 Poppler 全页渲染、pypdf/pdfplumber 文本提取及字体/主动内容静态检查通过，两路独立审查均为 `GO`。该 Profile 只证明冻结负载到 PDF bytes 的内部离线 primitive，不是正式报告；数据库/`report_versions`、MinIO、下载 API/Auth、浏览器 PDF viewer、跨镜像和 production 均为 `NOT_RUN`，`REP-003` 保持 partial，`REP-001/002`、BETA-VS-07 与 AC-014 不升级。
- 交付 `report-xlsx-writer-v1` 离线内部 Profile：新增 `backend/app/reports/xlsx_writer.py`，以精确锁定的 `XlsxWriter==3.2.9` 把冻结 `ReportPayload` 写为固定可见的 `Summary/Rules/Risks` 三表 XLSX；只写显式字符串/布尔/安全整数/空值，关闭字符串公式、URL 和数字自动转换，并对规则/风险映射、UUID/枚举/引用顺序、摘要一致性、单元格/全簿文本量、输出字节和 XML 字符设置失败关闭边界。OOXML 静态检查确认无公式、超链接、外部关系、宏、媒体或绘图，固定输入在同一运行环境生成字节相等的可打开 ZIP/XML；该相等性不外推到跨 Python/库/操作系统/镜像。聚焦报告套件 158 项通过，独立审查 `GO`。PDF Writer 后续已作为独立内部 Profile 交付；数据库/`report_versions`、MinIO、下载 API/Auth、浏览器 Excel/LibreOffice、跨镜像和 production 均为 `NOT_RUN`，`REP-003` 保持 partial，`REP-001/002`、BETA-VS-07 与 AC-014 不升级。
- 更新最新完整离线质量 checkpoint：`verify-local-offline` 为 Backend `2398 passed / 75 skipped / 1 Starlette deprecation warning`、Ruff check PASS、Ruff format 212 files、mypy 114 sources、pip check、Frontend typecheck/14 files/412 Vitest/129-module build PASS。该 checkpoint 已纳入 `invoice-exact-duplicate-candidates-read-v1`；默认 PostgreSQL current-head、Compose、浏览器、Provider、remote、部署和 production 仍为 `NOT_RUN`。独立显式 PostgreSQL 16.14 current-head Gate 连续两轮各 90/90 PASS；这只把 `INV-004` 从 planned 升为 partial，使矩阵成为 `2 implemented / 39 partial / 45 planned`，不改变其他任务或任何 AC 状态。
- 交付 TECH §6.13 `contract-primary-invoice-list-read-v1`：Backend 新增 `GET /api/v1/contracts/{contract_id}/primary-invoices`，先重验数据库 Actor、`financial.read` 与可见父合同，再复用 accepted 009 `(contract_id,invoice_id)` active-pair 条件唯一索引，以 `invoice_id ASC`、canonical `{id,v}` cursor 和 `LIMIT page_size+1` 有界读取当前 `confirmed_primary` 发票摘要；严格 DTO 复用 `InvoiceListItemData`，排除关系 ID、候选、匹配依据、历史与写语义。Frontend 新增 `/contracts/:contractId/primary-invoices`，由真实合同详情进入、按严格升序分页并继续访问真实发票详情；请求与响应均逐字校验 canonical base64url UTF-8 JSON cursor，写与候选保持不可用。UUID 顺序只用于稳定遍历，不表示业务时间。
- 交付 TECH §6.12 `supplementary-agreement-header-list-read-v1`：Backend 新增 `GET /api/v1/contracts/{contract_id}/supplementary-agreements`，以可见父合同为锚点、按 `effective_date DESC,id DESC` canonical keyset 读取 accepted 009 当前未软删除原始 Header；公开项精确七字段，两个状态独立原样展示，不返回字段级 change、证据、`row_version` 或有效性投影。Frontend 在真实合同详情内增加独立分页区块，覆盖 loading/empty/401/403/404/Trace/retry/Abort/stale/valid→invalid，并明确 Header 不代表协议已生效、适用或进入审核快照；所有相关写操作保持禁用。
- 升级 authenticated financial read API PostgreSQL 组合测试：真实临时 Ed25519 PEM/JSON 文件依次经过 keyring loader 与 `create_app` lifespan，在零 `dependency_overrides` 下构造 AuthService 和七条财务读链所用五个 QueryService；真实 login/JWT 经数据库 Actor、`financial.read`、七个 Router/Service/Repository 读取同一 PostgreSQL，logout 后再次 refresh 固定返回 401 `AUTH_REFRESH_EXPIRED`。ASGI `https://testserver` 模拟还验证凭据响应 `no-store` 及 Refresh Cookie 的 `Secure/HttpOnly/SameSite=Strict/Path=/api/v1/auth`。该用例不证明 TLS、Nginx、production secret mount/文件 ACL、部署、真实浏览器或 AC。
- 修复 application startup 资源边界：lifespan 的 `try/finally` 覆盖 Auth Engine 之后的 Service 构造，AuthService 构造失败时也会精确执行 `engine.dispose()`，并新增回归。
- 升级显式本地 `financial-read-browser-v1` Gate：test-only 同源宿主 `backend/tests/manual_financial_read_browser.py` 在精确授权 token、受限 loopback 端口、逐字 origin、`frontend/dist` 完整性及可丢弃 PostgreSQL marker 五重门禁内，生成临时 Ed25519 PKCS8 私钥与 public keyring，经生产 keyring loader 和 `create_app` lifespan 构造真实 AuthService 与七条财务读链所用五个 QueryService；门禁同时断言 `dependency_overrides=0`，六个 Service state 在 lifespan 前不存在、进入后为真实类型。真实浏览器已通过 `login → dashboard → invoice list/detail → primary contract → contract list/detail + supplementary raw header → contract primary invoices → invoice detail`，对应七条已冻结只读 API，精确命中隔离 PostgreSQL 合成事实，0 alert、0 console error；深链 reload 保持 URL 与用户，logout 后 reload 保持匿名且无远端退出状态未确认提示。运行中修复 `ApiClient` 默认原生 `fetch` receiver，改用 `globalThis.fetch.bind(globalThis)` 并新增回归测试。正常 `CTRL_BREAK` 后精确清理 OwnedSyntheticRows、临时 key 目录、宿主进程与专用容器，复核均为 0。该显式 Gate 仅证明 loopback HTTP 下的本地 test-only 链路，不证明 TLS/Nginx、production secret mount/文件 ACL、部署、强杀/断电清理或任何 AC；默认离线编排仍固定 `BROWSER_E2E=NOT_RUN`。
- 固定本地浏览器宿主失败边界：Uvicorn 0.52.1 的端口 bind failure 发生在 lifespan startup 后，必须显式 shutdown 才清理已播种合成事实；不把它表述为 bind 前无副作用，也不把正常 `CTRL_BREAK` 清理结果外推为强杀或断电保证。
- 历史 `user-list-read-v1` 之前的验证 checkpoint：完整 `verify-local-offline` 为 Backend `2252 passed / 73 skipped / 1 Starlette deprecation warning`、Ruff check/format 197 files、mypy 105 sources、pip check、Frontend typecheck/13 files/372 Vitest/128-module build PASS；PostgreSQL 16.14 对唯一 head `20260807_009` 的完整数据库目录在 startup cleanup 修复后最终干净重跑连续两轮 88/88，`POSTGRESQL_CURRENT_HEAD=PASS` 且专用容器零残留。首次未通过尝试不是有效证据，该阶段结论只采用最终修复后重跑 PASS。另显式 real local MinIO Adapter Gate PASS，使用合成 PDF 验证真实 quarantine PUT、精确内容/长度/SHA metadata 读取、DELETE、完整 PUT 后摘要错配补偿，以及合成 post-commit exception 的精确补偿。该 Gate 不证明 FILE Router、数据库/去重事务、Job/Scanner、通用迟到 timeout reconcile、进程崩溃清理、容量、production 或 AC；追踪矩阵保持 `2 implemented / 38 partial / 46 planned`。
- 交付 `invoice-primary-contract-read-v1`：Backend 新增 `GET /api/v1/invoices/{invoice_id}/primary-contract`，通过数据库 Actor、`financial.read`、发票锚点与合同/发票双组织及三端软删除边界，只读取 accepted 009 条件唯一索引保护的当前 `confirmed_primary` 关系；无主合同稳定返回 200 `null`，跨组织/不存在/软删除统一 404，未知 Query 422，公开 DTO 仅复用严格合同摘要并排除任意 `match_reasons_json`、关系/Actor/历史与写语义。Frontend 从真实发票详情以 canonical UUID 进入主合同页，展示真实合同或空态并可继续访问合同详情；删除原顶级对象无关入口及全部 Demo 候选/冲突/写模拟，保留 401/403/404/通用安全错误、Trace 重试、Abort 与迟到响应保护。完整离线门禁更新为 Backend `2175 passed / 67 skipped / 1 Starlette deprecation warning`、Ruff check/format 186 files、mypy 101 sources、pip check；Frontend typecheck、11 files/325 tests 与 124-module build 通过。新增 `InvoicePrimaryContractQueryService → FinancialReadRepository → PostgreSQL` 组合用例因未设置 `TEST_DATABASE_URL` 安全跳过；历史 81/81 PostgreSQL 证据早于本用例。候选查询/解释、建议、确认、替换、取消、历史、真实浏览器、production 与 AC-006 仍未实现或 `NOT_RUN`。
- 交付第二组受真实认证保护的业务只读链 `contract-list-read-v1 → contract-detail-read-v1`：Backend 新增 `GET /api/v1/contracts` 与 `GET /api/v1/contracts/{contract_id}`，复用数据库 Actor、`financial.read`、组织与软删除边界，并以 `effective_date DESC NULLS LAST + id DESC` canonical keyset cursor 提供无深 OFFSET/count 的分页；公开 DTO 仅投影 accepted `contracts` 当前持久化行，严格处理 nullable 文本/币种/日期、3+5 状态闭集、Decimal 与 BIGINT 字符串，明确不把主合同原始字段冒充补充协议生效后的有效字段。Frontend 删除合同列表/详情的 Demo fixture、假筛选/指标/证据/供应商/附件/历史，接入严格 decoder、cursor 栈、canonical UUID 详情、加载/空/权限/Trace 重试/Abort/迟到响应保护，未接写入与关联保持禁用。该合同切片收口时的完整离线门禁通过：Backend `2156 passed / 66 skipped / 1 Starlette deprecation warning`、Ruff check/format 182 files、mypy 99 sources、pip check；Frontend typecheck、10 files/312 tests 与 123-module build 通过。新增合同和既有发票 `QueryService → FinancialReadRepository → PostgreSQL` 组合用例在本轮因未设置 `TEST_DATABASE_URL` 而安全跳过；历史 81/81 PostgreSQL 证据早于这些用例。筛选/汇总、补充协议有效字段、供应商、主合同写入、浏览器、production 与 AC-003/004 均未实现或 `NOT_RUN`。
- 交付首个受真实认证保护的业务只读闭环 `invoice-list-read-v1 → invoice-detail-read-v1`：Backend 新增 `GET /api/v1/invoices` 与 `GET /api/v1/invoices/{invoice_id}`，按数据库 Actor、`financial.read`、Actor 组织和软删除边界读取发票摘要/稳定明细；列表采用 canonical base64url JSON keyset cursor、`invoice_date DESC NULLS LAST + id DESC`、`limit+1` 与最多 100 项边界，不使用深 OFFSET/count，异常 cursor 和跨字段分页矛盾失败关闭。公开 DTO 使用严格枚举、非指数 Decimal 字符串和无精度损失的详情 BIGINT 字符串，隐藏组织、供应商、证据哈希与操作人内部字段，并提供显式 OpenAPI 错误与 `private, no-store`。Frontend 删除发票 Demo fixture、假筛选/指标/主合同，改为真实 cursor 分页列表；严格解码排序和分页不变量、处理加载/权限/Trace 重试/Abort/迟到响应，并用 canonical UUID 进入真实详情，未接通区块和写动作继续禁用。该发票切片收口时的完整离线门禁通过：Backend `2128 passed / 65 skipped / 1 Starlette deprecation warning`、Ruff check/format 176 files、mypy 95 sources、pip check、Frontend typecheck/9 files/295 tests/122-module build。新增真实 `InvoiceQueryService → FinancialReadRepository → PostgreSQL` 列表/详情组合用例，但本轮未设置 `TEST_DATABASE_URL`，因此安全跳过；既有 81/81 PostgreSQL 证据早于这些测试。真实筛选/汇总、浏览器路径、Docker/MinIO、production、写链及 AC-005/007 均未实现或 `NOT_RUN`。
- 批准并同步 `OQ-09: archived-reupload=conflict-v1 + local-minio-v1`：同组织 SHA-256+大小命中 archived 时固定 409 `FILE_ARCHIVED_DUPLICATE` 且零文件/Job/业务对象/MinIO 副作用；固定 MinIO tag+manifest digest、内部 HTTP、七 Bucket 与运行时凭据注入。代码新增严格 MinIO endpoint/TLS/Bucket 配置、quarantine-only Adapter、服务端 UUID 对象键、pending intake 与文件类型权限门禁，以及仅绑定 loopback 的单服务 Compose 和独立应用身份/Bucket bootstrap；Adapter 在 SDK 实际读流时绑定精确长度/SHA metadata，并对 PUT 不确定结果或摘要错配补偿删除，补偿失败返回脱敏 cleanup-required locator。FILE 精确 10 文件 185 项、bootstrap 10 项、Backend unit 2072 项、Compose `config --quiet`、`LOCAL_MINIO_LAUNCHER_TESTS=PASS`、Ruff/format（170 files）、mypy 91 sources 与 pip check 通过；该切片收口时的完整离线门禁为 Backend `2090 passed / 63 skipped / 1 Starlette deprecation warning`、Frontend typecheck/260 tests/121-module build PASS。Docker daemon、固定镜像/七 Bucket/真实 MinIO 对象读写、files/knowledge_bases Schema、FILE Router/幂等、具体 Job Handler Registry/Scanner、PostgreSQL+MinIO 上传链、浏览器、production 与 AC-002/016 均未实现或 `NOT_RUN`，因此完整 MVP-VS-07 仍为 `BLOCKED`。
- 批准并交付 `OQ-08: auth-mvp-v1 + p0-permissions-v1` 的认证主链：Backend 实现 Argon2id 密码策略与五次锁定、Ed25519 Access/一次性换密 Token、7/30 天绝对 Refresh 旋转与重放处置、五个 Auth API、同源 Origin/`AUTH_PUBLIC_ORIGIN` Cookie 边界、凭据响应防缓存、数据库 Actor以及 26-code/五角色/deny-overrides；Frontend 以仅内存 Access Token、single-flight refresh、generation-safe replay/logout、局部换密 Token 和统一 permission UX 替换 `signInMock/sessionStorage`，并防止旧会话延迟 `/auth/me` 响应覆盖新账号投影。完整离线门禁为 Backend `2021 passed / 63 skipped / 1 Starlette deprecation warning`、Ruff 166 files、mypy 90 sources、pip check、Frontend typecheck/260 tests/121-module build PASS；此前瞬态 Windows junction 解码 warning 未在聚焦或最终完整复跑中重现。PostgreSQL 16.14 对唯一 head 009 的完整集成目录（含 Repository→Service→HTTP、7/30 天、锁定/换密和并发调用场景）连续两轮各 81 项通过，临时容器残留为 0。调用前 Barrier 仅证明并发调用回归，不宣称确定性进入同一行锁临界区；PostgreSQL Gate 的 Windows 负向自测改用非交互 `where.exe` 后通过。用户创建/启停/重置/角色替换、break-glass Application runtime、operation-log 审计链、业务 Router 授权消费、真实浏览器、production 和 AC-001 仍未实现或 `NOT_RUN`；矩阵调整为 `2 implemented / 36 partial / 48 planned`。
- 接通 MVP-VS-02 财务只读视图到 MVP-VS-03 合同发票候选规则的最小 Application Service：按指定发票逐字映射税号、名称与日期事实，复用既有三态原因和稳定排序，并对缺失或重复发票选择失败关闭；存在生效补充协议但字段级投影不可得时同样失败关闭，避免用主合同旧字段生成候选。候选与 Service 聚焦套件 79 项通过；完整离线门禁为 Backend 1948 passed/62 skipped/1 warning、Ruff accepted scope 149、mypy 81、pip check、Frontend typecheck/230 tests/119-module build PASS；PostgreSQL 16.14 对唯一 head 009 的完整数据库目录（含 Repository→Service 组合路径）连续两轮各 80 项通过，临时容器残留为 0。未新增归一化、API、认证、数据库写入或确认副作用；Compose、浏览器、Provider、远程、production 与 AC-006/007 均明确为 `NOT_RUN`，其中补充协议字段级持久化和后续真实链仍受合同阻塞。
- 将 `Request/` 从九份覆盖式文档收敛为四份活跃开发规格：入口、产品需求、技术规格和实施计划；旧九份文档按原字节归档。新增当前清单校验，解耦测试资产对“86 个标题”的依赖，并把数据库手册与根 README 改为当前事实源导航。活跃规格共 1,250 行，包含 16 个 AC、21 个垂直切片及明确的 8 个 READY/13 个最小 BLOCKED；基线正负用例、测试资产门禁、归档哈希、链接和编码就绪结构检查通过。
- 加固共享 Pydantic 校验错误脱敏边界：固定 `FinAuditValidation` title/type/message，仅保留代码 allowlist 的顶层字段位置，丢弃 `input/ctx` 与动态位置，并在 `except` 外重抛以清除 `cause/context`；ValueError、自定义 PydanticCustomError、恶意 title、动态 extra key 与 Settings/Policy 回归聚焦 144 项通过，独立终审 `GO`。完整离线门禁更新为 Backend 1816 passed/54 skipped/1 warning、Ruff 140 files、mypy 77、Frontend 230 tests/119-module build；PostgreSQL current-head、Compose、browser、Provider、remote 与 production 本轮 `NOT_RUN`。
- 固定 production route config regression：`AppEnvironment.PROD` 下 `/health` 返回 200，而 `/openapi.json`、`/docs`、`/docs/oauth2-redirect`、`/redoc` 全部返回 404；`test_app.py` 聚焦 20 项通过，独立审查为 `GO`。该结果只证明配置 seam，真实 production Policy、runtime 与 deploy 仍为 `NOT_RUN`。
- 加固 `FILE-001` 空上传边界：`measure_upload_stream` 对无 chunk、单空 chunk 与多空 chunk 统一返回 400 `FILE_SIGNATURE_MISMATCH`，落实 `files.size_bytes > 0`；FILE 六文件聚焦套件 135 项通过。最近一次完整离线门禁为 Backend 1816 passed/54 skipped/1 Starlette warning、Ruff accepted scope 140、mypy 77、pip check、Frontend typecheck/230 tests/119-module build PASS。multipart Form/API、严格布尔文本解析、默认离线门禁中的 PostgreSQL current-head、MinIO、Scanner、Job/Worker、浏览器、Provider、远程、production 与所有 AC 仍为 `NOT_RUN`。
- 接受 `CR-014-R3` governance-only BOSS direct approval `item-572`，绑定 snapshot `11956 bytes / e5c00e6e47081d0e7418b48957d7895fe5a4705a1d5008256a7bb291d15b7cd9`；create-only 本地回执仅为 `NON_AUTHORITATIVE_EVIDENCE_ONLY`，身份 `1801 bytes / 7421b7913ed31b12ba37da52f2a74d3ac28d7eab01bfeea0efc5bcde2c9bd08b`。已生成 `CR-006-R4` source snapshot `8945 bytes / 159dd1588d9366c85e8aa1ffc5f9b910ea9319732a491785ce2fdf10d8096e8c` 与 `CR-013-R3` source snapshot `9496 bytes / 6cecc269e6517bb5b30d535350cdcb7e9aa2a00cf827199c2c2cb3789eae3d1a`；`CR006_R4_SOURCE_A1` 已 `RECEIVED / VALID`，其 create-only 本地 receipt 仅为 `NON_AUTHORITATIVE_EVIDENCE_ONLY`，身份 `1807 bytes / d98c2556a07deb73b6683fdc8c325498a19815acb0034cbf2ac3f7905f2aab1c`，`source_message_text_sha256=69886a5974497a981f8649f8071f476487319fe2f0abdfc9636b3618ef331695`；`CR013_R3_SOURCE_A1` 已通过 platform event `item-684` `RECEIVED / VALID`，其 create-only 本地 receipt 仅为 `NON_AUTHORITATIVE_EVIDENCE_ONLY`，身份 `2607 bytes / 1454233d212979663e26e2699b924689cb081e893b456daa30530e81a778b9c8`，`source_message_text_sha256=572c102327f313c2a6e82ad582c4369f827e62c613c74ab14aecfe6b8971a37b`；两份 A1 source approvals 已闭合。CR-013-R3 blocklist A2 仍为 `NOT GENERATED / NOT RECEIVED`，原因是获准本地目录未找到合同精确 source/license bytes；resolver 可内嵌于 A1 已冻结的两条隔离单文件脚本并由 implementation-manifest → 十键 payload 传递绑定，无需新 resolver CR，现有两份 A1 保持有效；expected-post/inverse bundle 仍为 `NOT GENERATED`，joint authorization 仍为 `NOT RECEIVED`。临时 transport drafts `CR-014-R4/CR-006-R5/CR-013-R4` 已撤回且不可批准；010/011 migration 仍为 `NOT GENERATED / NOT AUTHORIZED / NOT RUN`，runtime 与 AC 仍为 `NOT AUTHORIZED / NOT RUN`，20/58 仍只是后续门禁全部成立时的候选结果。
- 加固前端 `ApiClient` 的 multipart 与响应身份边界：FormData 请求删除调用方 `Content-Type`，由浏览器生成含 boundary 的 Header；`AUTH_PASSWORD_CHANGE_REQUIRED` 在所有路径固定消息并丢弃不可信 `details`，只有精确 `POST /api/v1/auth/login`、HTTP 403 与合法 `data` 可不可枚举地保留受限 Token；成功响应 body 与错误响应 body/header Trace ID 只接受标准小写 UUID，不可信候选安全降级且排除与 Token 相同的 UUID 形态值。ApiClient 聚焦 28 项与前端全量 230 项通过，独立审查未发现 Critical/High/Medium 问题；真实认证 API、浏览器业务链和 AC 仍未运行。
- 对齐 `AUD-005/REP-003` 纯函数安全边界：风险汇总的 `risk_id` 只接受精确 `UUID`，非 UUID 与 UUID 子类失败关闭；表格文本公式中和扩展到 `= + - @` 与 TAB/CR/LF 七类前缀并保持幂等。两文件聚焦测试合计 54 项通过，但数据库、风险生成、真实 XLSX/下载、浏览器和 AC 均未运行。
- 加固 `AI-001` Fake Sink 事件链边界：在 completed/late_completion 记录或签发 permit 前复用完整链校验，跨事件字段不一致固定返回 `CONFLICT`，且不记录冲突事件、不签发 permit；events 47 项与 sink 22 项合计 69 项，当前 Stage1 重跑为 275/275、negative runner PASS、zero-socket attempts=0。2026-08-10 的 273/273 仍保留为历史封板证据。
- 加固 `CON-003` 字段值边界：顶层或嵌套的 Decimal `NaN/sNaN/+Infinity/-Infinity` 均失败关闭，有限 Decimal 在冻结投影与隔离物化后仍保持精确值；当前 `contract_effectivity` 聚焦套件为 52 项。加固 `AI-002` repair validator 边界：coroutine、同步 generator 和 async-generator 在 initial/repaired 路径均失败关闭、主体不执行，固定返回无 cause/context 的 `INTERNAL_ERROR`；structured output 46 项 + repair 56 项合计 102 项，四个 AI 文件合计 241 项（46 + 56 + 25 + 114）。两项均为离线领域/contract 证据，不代表业务 runtime 或 AC。
- 收窄默认离线门禁的 Ruff accepted-source 范围为精确 `app + alembic/versions + tests`，`backend/alembic/recovery` 匹配 0 个 accepted files，并明确为 `NOT_APPLICABLE`；9000-byte verifier 固定 SHA-256 为 `455ba2ea80534d594dabce6869bc8bf441892ac3f36414783d1c47df90a94f43`，负向自测封堵动态 scope 增宽、额外 Ruff 调用与伪造失败原因。最近一次完整门禁为 Backend 1816 passed/54 skipped/1 warning、Ruff 140 files、mypy 77 sources、Frontend 230 tests/119-module build，并已纳入当前 52 项 CON-003、54 项 risk/spreadsheet、28 项 ApiClient 和 FILE 六文件 135 项聚焦边界。本轮另行显式在 PostgreSQL 16.14（`server_version_num=160014`）对唯一 head `20260807_009` 连续两轮通过 71/71，输出 `POSTGRESQL_CURRENT_HEAD=PASS`，专用标签容器最终 0 残留；这是独立历史证据，默认离线门禁本身仍未运行 PostgreSQL current-head，Compose/browser/Provider/remote/production 与所有 AC 均为 `NOT_RUN`。
- 以未改动的 recovery candidate DDL 合同为来源接入 accepted 线性迁移 `20260807_009`，新增空的 `supplementary_agreements/invoice_items/contract_invoices`、精确 SQLAlchemy ORM 与聚焦契约测试；accepted 文件仅有 Ruff 所需的字符串换行格式差异。PostgreSQL 16.14 current-head Gate 连续两轮 71/71 通过，当前 `BASE-005` 为 `PARTIAL / 18/57`、尚缺 39 表。该切片不开放 Repository/Service/API/Worker，不包含存在 `change_seq` 歧义的 `supplementary_agreement_changes`，也不完成任何 AC。
- 登记 `GAP-068`：审核执行状态机缺少进入 `failed` 的合法边，AUDIT-008 又使用执行状态闭集之外的 `cancel_requested`；终态更新白名单、跨父对象一致性、high 风险完成门禁、报告状态迁移和 H 簇 downgrade 锁序也未冻结。合同闭合前不创建猜测性的 010/H-root Schema。
- 为 `BASE-001` 增加项目工作流 synthetic/offline 分支保护 fixture、纯标准库严格验证器及封闭负向自测，覆盖 `main/develop`、禁止 Force Push、必须 PR、至少一次独立 Review 与 static/test 类别，并纳入默认离线编排。验证器不接受文件路径，只读取固定 `--stdin` 有界字节流，并拒绝重复键、类型强转、分支/check 非规范顺序和额外参数；它不访问托管平台，不是 Request-exact、远程配置或分支保护演练证据，`BASE-001` 仍为 `partial`、remote 仍为 `NOT_RUN`。
- 批准 `CR-011-R4` active-baseline successor 并完成九份 Request、正式 baseline manifest 与 active-baseline pin 的 11 文件同步；九角色审批为 9/9，Gate A 为 PASS。2026-08-10 封板 Gate B Stage1：strict parser/companion/resolver/retry/response/network policy、Event DTO 与 Fake Sink 的纯 contract/offline runner 为 273/273、zero-socket attempts=0，负向 runner PASS；随后在 test-only 精确锁和隔离物化依赖上，完整 Gate B 以 Node selftest PASS、27/27（2 个正向 + 25 个负向）、negative vectors 25/25、Python/Node Schema calls 20+20、engines=2/2、zero-socket guard PASS/attempts=0 与 anti-forgery 11/11 通过。该 Gate 不进入默认离线编排，也不授权 Gate C 持久运行时、Provider、真实数据、部署、canary 或 production。全量本地质量门禁为 Backend 1625 passed/35 skipped、Ruff/format 126、Mypy 70、Frontend 216/typecheck/build PASS；默认 PostgreSQL/Docker/browser/Provider/remote/deploy/production 仍为 NOT_RUN。`BASE-004`、`AI-001` 与 `TEST-001` 保持 partial，GAP-057～062 保持 open；P0 仍为 2 implemented / 30 partial / 54 planned，差异清单仍为 47 open / 20 closed / 67 total，数据库仍为 13/57，任何 AC 均未因此完成。
- 批准 `CR-004-R2/REL-D-001～005` effective contract 并完成九份 Request、正式 baseline manifest 与 active-baseline pin 的 11 文件原子同步，关闭 GAP-040～042；新增 `20260807_007` 空 `async_jobs/async_job_steps/outbox_events` DDL/ORM 及约束 Gate。显式 PostgreSQL 16.14 current-head 门禁连续两轮均通过 51/51 项、标记容器残留为 0，`BASE-005` 仍为 `PARTIAL / 13/57`、其余 44 表，差异清单为 47 open / 20 closed / 67 total。默认离线门禁另行通过，但故意不设置 `TEST_DATABASE_URL`，因此在该运行中 PostgreSQL current-head 仍为 `NOT_RUN`。Registry/Input/Summary/Handler artifact 均仍 PENDING；Handler/Job runtime、Redis/Broker、真实数据、Provider 网络、部署与 production 仍未授权，不完成 P0 或任何 AC。
- 登记 `GAP-067`：UI-002/FE-002 要求五角色工作台摘要、最近任务和当前用户失败 Job，且刷新后必须由 Backend 恢复、禁止前端全量拉取；现有 AUDIT-001 排除 `system_admin` 且未冻结五角色摘要，OPS-001 只支持已知 `job_id`，没有授权 Job/dashboard discovery 合同。批准 Request 裁决前不编造 Mock DTO、不枚举 Job ID、不继续拆孤立组件；登记时差异清单为 50 open / 17 closed / 67 total。
- 同步当时未批准可靠性候选的治理事实：`CR-004-R2` effective contract 逐字绑定 R1 base snapshot `387ed8d9eafbddcf1ca768abb770b13d1383a5c751077194ea3b7ca63523e7c6` 与 R2 addendum snapshot `77a8a2e63a4668a5e7e6a028b18cc04f34ddc824d04096db8bede7bce844241b`。该阶段仍为 `NOT APPROVED / NOT SYNCHRONIZED`；获批后的范围也仅是 11 文件原子同步、`20260807_007` 空表 DDL/ORM 和合成 PG16 Gate，不授权 Job runtime、网络、真实数据、部署或 production。README、requirements-map、baseline-gaps 与项目 MEMORY 同步移除 R1-only 和 CR-015 依赖已满足的陈旧表述；该次候选记录未改变 Request、migration、matrix 或任何授权状态。
- 为 `BASE-005` 增加 57 表实施就绪账本：机械对齐 SRS 11.1、数据库第 5 章、Alembic `001～006` 与 SQLAlchemy metadata，固定当前 10/57、剩余 47/47 唯一表及 12 张 dependency-minimal root；按 owner、跨簇 FK、GAP/CR 和 PostgreSQL 16/并发/原子降级 Gate 记录后续条件，但不新增 migration、不改变追踪状态，也不授权真实数据、Provider、部署或 production。
- 批准 `CR-012-R3` 的 FIN-D-001～FIN-D-006 并原子同步九份 `Request/`，关闭 GAP-063；新增 `20260807_006` 空表 `contracts/invoices/suppliers` 迁移、SQLAlchemy 元数据及离线/集成回归。显式门禁已在 PostgreSQL 16.14（`server_version_num=160014`）连续两轮均通过 26/26 项并输出 `POSTGRESQL_CURRENT_HEAD=PASS`，当前 `BASE-005` 为 `PARTIAL / 10/57`、剩余 47 表。GAP-064 与未批准的 `CR-015-R2` 继续阻断供应商运行时；真实数据、Provider/其他外网、部署和 production 均为 `NOT_AUTHORIZED / NOT_RUN`，没有任何 AC 因本切片完成。差异清单现为 49 open / 17 closed / 66 total。
- 收紧 P0 追踪矩阵 CSV 门禁：修复 AI-001 evidence 的未引用逗号，使用严格 CSV parser 要求表头及每条任务记录都恰为 6 列，并新增第 39 个隔离负例，避免 `Import-Csv` 静默丢弃尾部证据。
- 将 `TEST-001` 扩展为 `PARTIAL / S1-S9`：新增显式 PostgreSQL 16 current-head 门禁及失败关闭自测，只使用本机缓存镜像、随机 loopback 端口/凭据/tmpfs，连续两轮验证当时 `20260807_005` 的 7 表 head 的 `base → head → base → head`，并以容器 ID+label 精确清理。该阶段在 PostgreSQL 16.14 上两轮各 16 项通过且残留容器为 0；默认离线门禁仍不启动 Docker，Compose、当时其余 50 表、浏览器、Provider、远程和 production 仍为 `NOT_RUN`。
- 登记 `GAP-066` 并增加未批准的 `CR-016-R1`：冻结候选七类/15 行规则元数据、封闭 Draft 2020-12 输入 Profile、无主合同仅执行五条规则、15 项静态 key→predicate/adapter、release/source hash、0/exact-15 原子发布与 RULE-001 全版本/current 目录；decision snapshot 为 `be30da8109e140dcac558abb3dab1871ef6228cf09d09bd9762fb430618fe8b1`。它不新增 API/表/migration/P0，仍为 `NOT APPROVED / NOT AUTHORIZED`，不能绕过 `BASE-005、AUD-002`，也不授权真实数据、Provider、网络或 production；差异清单现为 50 open / 16 closed / 66 total。
- 将 `AUD-003` 扩展为 `PARTIAL / S1-S9`：补齐 RULE-005 精确重复发票身份、RULE-006 已确认主合同核心事实完整性、RULE-013 正常检索后的制度引用缺失和 RULE-014 标准名称/税务身份冲突四条纯谓词；现有 15/15 纯谓词不包含上游事实规范化、输入 Schema、实现键/代码哈希、规则注册发布、执行器、持久化、风险生成或 AC-007 运行链路。
- 登记 `GAP-065`：数据库/API 的 `quote` 结构块过滤枚举、DOC-003 块坐标不可得原因载体与投影、DOC-007 Markdown parser/HTML 白名单/复杂表格格式尚未形成唯一合同；不把 `TBD-018` 或 PDF 报告渲染选择扩大为全部解析路径前置，并按表内状态校正当时清单为 49 open / 16 closed / 65 total。
- 将未签署的 `CR-015-R1` 提升为 `CR-015-R2`：补齐 CON-005/INV-005 人工税务确认证据、既有 candidate 显式处理、确认元数据、跨组织闭包、candidate/source CAS 与响应版本归属、only-USCC no-op、拒绝审计、脱敏 summary 固定向量和严格 CR-012 批准/九 Request 同步证据顺序；新 decision snapshot 为 `9682e039971c0e922b847b370f64b961af382d7be1b8f31a59e8f1c8733fff77`，仍为 `NOT APPROVED / NOT AUTHORIZED`，不授权 runtime、网络或 production。
- 增加未批准的 `CR-015-R1` 推荐：后置绑定 CR-012-R3，冻结供应商统一读投影、AI generic tax 只写 `tax_number`、三态候选状态机、人工确认触发精确复用、单条 `supplier_field` 聚合纠错、原子锁序、三个新错误码和脱敏 Gate；不新增 endpoint、核心表、Alembic revision 或 P0 工作包，也不授权 Request 同步、Provider 网络或 production。
- 增加 `AI-004 PARTIAL / S1`：新增严格冻结的风险解释与 RAG 输出 DTO，规则事实不可改写，引用必须逐字段匹配本次候选；回答、拒答和服务降级使用封闭状态矩阵，空候选只接受固定 `NO_RELEVANT_EVIDENCE`，但 Prompt、模型前短路、权限/有效期过滤、活动索引/哈希复核、API、数据库、Sink 与真实模型仍未实现。
- 增加 `AUD-005 PARTIAL / S1`：按未 `dismissed` 风险的最高 `effective_level` 计算当前总体风险，并分别给出有效 high 与待复核 high；本切片不生成风险、不冻结制度引用，也不决定最终完成权限或历史曾 high。
- 增加 `DOC-008 PARTIAL / S1`：以精确 `Fraction` 计算证据来源映射完整率和有效结构块覆盖率，批准排除项只从结构分母移除，缺失 ID 稳定返回且零分母失败关闭；Markdown 解析、映射真实性、安全校验、激活、API、数据库和并发仍未实现。
- 增加 `AUD-003 PARTIAL / S1`：实现 RULE-004 合同日期闭区间与 RULE-015 非红字正金额两个纯谓词，各覆盖至少 5 个通过、5 个命中以及 `not_applicable`/畸形输入；其余 13 条规则、注册表、执行器、持久化、风险生成和 AC-007 运行链路仍未实现。
- 将 `AUD-003` 扩展为 `PARTIAL / S1-S2`：新增 RULE-008 单合同/单发票币种精确比较、RULE-010 已确认主合同存在性和 RULE-011 规范化合同编号谓词；不做币种归一化/换汇、多发票聚合、输入规范化、注册表、执行器或风险生成。
- 将 `KB-011` 与 `TEST-002` 扩展为 `PARTIAL / S2`：新增无答案高置信误召回率纯函数，以调用方冻结的有限 `Decimal` 阈值按严格 `score > threshold` 逐例计数，并以 `Fraction` 保留精确比率；权限/日期/状态过滤正确率合同仍不闭合，阈值版本绑定、批准数据集、真实检索和 10% 发布门禁未运行。
- 增加 `AI-002 PARTIAL / S1`：实现不调用模型的确定性结构化输出清理，只接受单一外层 Markdown 围栏或全文唯一 JSON object，拒绝重复 key、非有限数、超长整数与过深结构，并把 Pydantic 校验错误压缩为 JSON Pointer 与固定安全类别；两次模型 repair、共享预算、事件 Sink、Provider 和网络仍未实现。
- 增加 `KB-011 PARTIAL / S1` 与 `TEST-002 PARTIAL / S1`：新增权限过滤后文档/证据 Hit@K、Recall@K、macro Precision@K 和 MRR 纯函数，以制度版本加锚点 ID 的复合身份防止跨文档伪命中，保留稀疏真实排名并对零可回答分母失败关闭；权限过滤准确率、无答案误召回、正式数据集、EVAL API/数据库/异步运行和 Qdrant 均未实现。
- 将 `FILE-001` 扩展为 `PARTIAL / S1-S3`：新增精确 `bytes` chunk 的单遍流式大小与 SHA-256 计量，以及调用方显式注入的 `1..max_batch_file_count` 数量门禁；恰好边界通过，超一字节立即停止消费并返回 `FILE_TOO_LARGE`，批量超限返回 `BATCH_LIMIT_EXCEEDED`。未裁决 50MB 换算，也不实现扩展名/MIME/Magic、multipart、逐文件结果、持久化、去重或 Job/Worker。
- 为未批准的 `CR-003-R1`、`CR-004-R1`、`CR-005-R1` 生成并独立复算首次 decision snapshot：特权授权、Job/Step/Outbox 可靠性和文档/Asset 完整性合同现可供签署，但仍为 `NOT APPROVED / Request NOT AUTHORIZED`；没有创建对应 migration/runtime，也未授权 Provider 网络或 production。
- 修正 `CR-009-R1` 的 CHUNK-008 API 计数合同：保留当前 122 API 为基线锚点、本 CR 只登记 `api_delta=+1`，最终总数按同步时全部有效 CR delta 累加，避免与 CR-005 分别硬编码 123。
- 增加 `FILE-001 PARTIAL / S1`：冻结四种上传业务类型、制度文件与目标知识库 UUID 的双向约束、默认 `true` 的严格布尔处理意图和额外字段拒绝；仅为纯 Schema，不实现 multipart、文件校验、持久化、去重锁、Job/Worker 或知识库运行时验证。
- 加固 `BASE-003` Trace ID 复制组件：属性切换会复位反馈并使旧异步复制结果失效，成功/拒绝/不可用状态均以可访问方式公告且不回显异常；真实浏览器剪贴板权限和屏幕阅读器验收仍未执行。
- 将 `TEST-001` 扩展为 `PARTIAL / S1-S7`：新增失败即停的单一本地离线质量编排与缺失 Python 负例，聚合现有 baseline/Git/资产正负门禁、Backend pytest/Ruff/Mypy/pip check 和 Frontend typecheck/Vitest/build；Frontend 子进程显式禁用 Vite `.env*` 加载，隔离的 `127.0.0.1` 预期失败连接单独披露，PostgreSQL current-head、Docker、浏览器 E2E、Provider、远程分支保护和 production 仍明确为 `NOT_RUN`。
- 增加 `FILE-002 PARTIAL / S1`：版本无关的文件安全扫描六态 Enum 与纯解析准入门禁，仅已校验的 `clean` 枚举实例可进入解析；旧 `error`、未知字符串、裸字符串和错误类型均失败关闭，不实现 Scanner、状态转换、数据库、Worker、网络或 production 扫描。
- 补齐 `BASE-003` 全局状态标签：Request 页面出现的 62 个去重正式状态码现在都有简体中文与 `aria-label`，未知 code 仍原样显示；本改动不新增业务页面，也不替代浏览器视觉验收。
- 将未批准的 `CR-011-R2` 提升为 `CR-011-R3`，并保持 16 个 contract-offline JSON 制品：Policy/Event Schema、12 条 companion validation 规则、正向 Policy 与精确 JCS pre-hash、25 个失败关闭向量、8 个事件向量、固定 CIDR 注册表及逐文件 manifest。R3 仅把 Event 数值限定为 I-JSON 安全数学整数并要求 DTO/JCS 规范输出，Policy raw source 的词法合同不变；decision snapshot 为 `b03ca205d74a6abfb6ec1fbe1606ee327600d622bed392e8eccb5591b331a0be`，artifact manifest hash 为 `f4249f6e9fa08f7503c8b3265807f8febfd3c820b7add10d158034597776f84c`。late-event DTO/Schema 继续在本 CR 内自包含，CR-004 仅保留为后续 runtime 兼容依赖；Sink 行为证据、合同批准、Request 同步、Provider 网络和 production 仍未授权。
- 增加 `AI-001 PARTIAL / S10`：六类 P0 AI purpose 到 `rag/async_generation/embedding` 三个独立限流池的不可变纯映射；未知值和裸字符串失败关闭，不实现 Redis、计数器、semaphore、Provider 调用或实际限流。
- 增加 `BASE-005 PARTIAL` 的第五个线性迁移：创建空的稳定案件主表 `audit_tasks`，落实 16 列 M1 字段、组织内任务号唯一、三态状态、软删除理由和五个即时父表外键；`current_execution_id` 按 Request 延迟到执行版本表迁移再加外键，005 downgrade 先独占锁且非空时失败关闭，当前数据库进度为 7/57。
- 增加未批准的 `CR-008-R1`～`CR-011-R3` 与 `CR-012-R3` 推荐：分别登记操作日志动作注册表、分块配置完整性、Scanner Registry Profile、AI Policy/Transport，以及合同编号/供应商税务身份的可实施性合同；CR-012-R3 移除 API 错误/读写投影与未批准 action registry 依赖，并把对应业务运行时差异拆为 GAP-064。草案不授权 Request 同步、对应迁移、Provider 网络或 production，其中 CR-008 候选统计、CR-009 固定 JCS/hash 和 CR-011 机器制品都不等于最终获批。
- 增加 `AI-001 PARTIAL / S9`：严格不可变的 CR-002 pre-hash Policy payload、六类 operation 与 Profile/retry/breaker/rate/outbound 初始值校验、`AI_PROVIDER_CALLS_ENABLED=false` 启动门禁和环境变量示例映射；最终 `policy_hash`、目标路由身份、线级 retry/HTTP/网络和事件 Schema 继续由 GAP-057～062 阻塞。
- 增加 `BASE-005 PARTIAL` 的第四个线性迁移：创建空的不可变 `audit_rules` 规则版本表、规则代码/版本唯一与风险等级约束，并由 PostgreSQL 触发器拒绝 UPDATE/DELETE/TRUNCATE；迁移不写真实规则，downgrade 先独占锁表且非空时失败关闭，当前数据库进度为 6/57。
- 增加 `BASE-005 PARTIAL` 的第三个线性迁移：独立创建 `idempotency_records`，实现组织/用户/Key 唯一、固定长度请求哈希、脱敏响应 JSON 与有效期约束；不写入任何种子，当前数据库进度为 5/57。
- 增加 `BASE-006 PARTIAL` 离线 Worker 基础：严格 `job_id + event_schema_version` 消息、七个确定性 Celery 队列路由、JSON-only/UTC、late ack、Worker 丢失重投和禁用 Result Backend 事实语义；尚不包含 Job/Outbox 持久化、真实 Task、Redis Worker 或恢复验收。
- 增加未批准的 `CR-003-R1` 推荐，登记 break-glass 决定/撤销证据、过期临时授权唯一性、环境化职责分离和管理员资格链四项实施前合同缺口；本 CR 不预占 Alembic 编号，批准前不创建特权授权迁移，也不把两表设计冒充 AUTH-005 或 AC-001 完成。
- 增加未批准的 `CR-004-R1`～`CR-007-R1` 推荐，分别登记 Job/Outbox 可靠性、文档完整性、操作日志完整性以及文件/知识库生命周期的实现前合同缺口；草案不授权 Request 同步或迁移，其中 CR-006 提议把核心父表调整为 58，CR-004 仍是 CR-006/007 的依赖。
- 增加 `BASE-005 PARTIAL` 身份核心迁移：四张 `organizations/users/roles/token_sessions` 表、对应 SQLAlchemy 元数据、固定五角色幂等种子、CITEXT 活动唯一、单组织、强制换密和 Refresh Token 哈希/有效期约束；离线升级/降级 SQL 与元数据测试不创建默认组织、用户或密码。
- 批准 `CR-001-R2` 与 `CR-002-R4` 的 contract 范围并同步九份 `Request/` 事实来源：P0 基线更新为 57 张核心物理表、122 个 API、86 个工作包；冻结可恢复 Job、上传意图、强制换密、六态扫描、break-glass、离线 bootstrap、规则/分块所有权，以及 AI Provider 包络、预算、错误/fallback、审计、修复与出站安全合同。`fixed_test_provider` 和 `production` 仍为 `PENDING`，`AI_PROVIDER_CALLS_ENABLED=false`，不授权网络调用或生产放行。
- 增加 `AI-001 PARTIAL / S4-S6`：LLM 请求强制分离必填的 system/user 内容，Embedding 请求与结果支持有序不可变的非空批次并校验返回数量，Gateway 的未知 Adapter、契约错误和未规范化 Adapter 异常统一为携带 `trace_id` 的固定脱敏错误；该离线切片不冻结 HTTP 包络、向量维度、批量上限、网络调用或自动重试/fallback。
- 增加 `AI-001 PARTIAL / S7`：版本化不可变 `PricingProfile`、显式 `external_usd/internal_unmetered` 模式和 CR-002 规定的逐物理请求整数 `micro_usd` 向上取整公式；内部不计费必须零价，计算不触发网络、睡眠或预算预留。
- 增加 `AI-001 PARTIAL / S8`：无默认值的不可变 `BudgetProfile`、下一物理请求的最坏 Token/费用 preflight，以及 Provider usage、价格版本和实际整数费用对账；预留不会修改或释放共享预算，且不调用数据库、Redis、网络或睡眠。
- 初始化 Git 仓库与 `backend`、`frontend`、`infra`、`docs`、`scripts` 顶层结构。
- 固定 Request 基线清单、开发工作流、项目 Agent 规则与长期项目记忆。
- 增加 secret-safe `.gitignore`、行尾规则、编辑器规则、提交模板和 PR 检查清单。
- 增加幂等本地 Git 治理初始化、最小 Conventional Commit hook、`vMAJOR.MINOR.PATCH` 标签校验和临时仓库自测；不创建提交、标签或 remote，远程分支保护仍待配置和演练。
- 增加可重复执行的仓库结构、Request 哈希和高置信 secret 基线检查脚本。
- 增加 9 份 Request 文档的职责、P0 范围、实施顺序和 AC 导航。
- 增加 FastAPI 模块化单体骨架、统一响应与错误结构、Trace ID 中间件、健康检查和安全配置校验。
- 增加 Vue 3 严格 TypeScript 工程、UI-001～UI-014 路由、Mock 登录、固定角色拦截、全局布局、通用状态组件和统一 API 请求层。
- 增加 Backend/Worker 与基础设施 `.env.example`；补齐 vLLM Profile 变量，并在 `GAP-008` 关闭前禁止默认固化 Qdrant Collection。
- 增加 SQLAlchemy 2、Alembic、psycopg 依赖与 secret-safe 迁移配置；首个 revision 仅启用 `pgcrypto`、`btree_gist`、`citext`，并提供真实 PostgreSQL 往返测试门禁。
- 初次登记 `BASE-005` 完整迁移契约阻断项、`CR-001` 草案和迁移运行手册时，尚未创建业务表、业务种子、默认组织或默认管理员；后续批准合同将正式表数从 55 调整为 57。
- `CR-001` 初次收敛为八项推荐合同时，覆盖可恢复 Job、上传意图、首次换密、扫描状态、break-glass、bootstrap、规则种子和分块配置，并补齐角色/人员/时长与迁移/回滚签署入口；该阶段审批仍为 `PENDING`。原 GAP-009 经证据复核确认不存在第二个持久化活动索引事实，已从 CR 移出。
- 增加 `TEST-001 PARTIAL / S1-S2` 测试策略、86 个 P0 任务追踪矩阵、29 条固定合成 JSON fixture、14 个确定性二进制文档/图片 fixture、SHA-256 清单、可复现生成器及离线正/负向资产校验；不实建账号，不声称解析/OCR/上传 API 或环境重建已通过。
- 将 `TEST-001` 扩展为 `PARTIAL / S1-S3`：新增无来源映射、空分块、超长分块构造规格、缺失目标字段、冲突金额和伪造候选引用 6 条固定合成负例，JSON 数据集升级至 1.2.0 共 35 条；二进制资产仍为 14 个，账号、环境和运行时验收边界不变。
- 将 `TEST-001` 扩展为 `PARTIAL / S1-S4`：JSON 数据集升级至 `1.4.0`，锁定六个固定活动账号的角色/状态/Request 职责边界、C-001 双方名称、RET-001 五条固定问题及前三条证据/数值 Top-5，并固定五个标量安全输入、校验三个结构化安全输入的最小不变量；数量仍为 35 条 JSON 与 14 个二进制资产，二进制契约保持 `1.0.0`。

- 将 `TEST-001` 扩展为 `PARTIAL / S1-S5`：JSON 数据集升级至 `1.5.0` 共 37 条，补齐 AI 详细设计明列的用户直接注入与 HTML/Markdown 隐藏指令，并以固定 ID、类别、输入、预期、哈希和隔离漂移负例保护；14 个二进制资产及其 `1.0.1` 契约不变。
- 将 `TEST-001` 扩展为 `PARTIAL / S1-S6`：JSON 数据集升级至 `1.6.0` 共 39 条，新增表格单元格角色覆盖与用户伪造 `candidate_id` 两个独立结构化负例；正向门禁固定 ID、分类、输入、预期和哈希，负向门禁增至 38 个，二进制资产仍为 14 个且契约保持 `1.0.1`。
- 增加 `AI-001 PARTIAL / S1-S3` 的 provider-neutral LLM/Embedding Adapter Protocol、不可变调用契约、调用方注入的 Transport Policy、显式单次 Gateway 分派、六类 P0 调用的只读主/备候选映射和离线 Fake 测试；前四类缺失备用模型失败关闭，报告草稿的备用候选必须显式启用，Embedding 始终单候选，映射不执行自动调用或 fallback，Gateway 拒绝不一致 Outcome，并切断未规范化 Provider 异常的原始异常链。
- 登记 AI 调用超时、5xx、上下文超限、Provider Profile、fallback 必填性、跨主备/重试/修复的总调用与费用预算、日志所有权、结构化输出修复次数、依赖健康状态和 `TEST-001` 形式依赖不足的基线差异；`CR-002` 初次形成时冻结为待审批的 Chat/Embedding 包络、Bearer、固定尝试、Token/费用、`5/60/30/1` 熔断、Redis 失效、Outbox、出站安全、迁移和回滚合同。该阶段仍为 `DRAFT / PENDING`，没有启用真实模型调用。

### Fixed

- 修复测试资产门禁在 PowerShell 7.6 下的 JSON 整数宽度误报：`ConvertFrom-Json` 将小整数解析为 `Int64`，而冻结的 PowerShell 字面量是 `Int32`，导致合法的 P-001-V2 页码和 RET-001 `top_k` 被错误判为漂移。标量门禁现只在期望值为整数时兼容 `Int32/Int64` 宽度并按 `Int64` 值比较，字符串、Boolean、浮点和其他类型继续失败关闭；正向资产门禁与既有 39 个负向篡改用例用于回归。
- 修复合同和发票完整事实写 API 的证据 UUID 边界：严格请求模型此前拒绝同一读取 API 返回的 canonical UUID 字符串，使真实前端带字段证据修正固定返回 422；现仅在 `block_id/parse_version_id` 边界显式解析 canonical UUID，继续拒绝非 canonical 或非字符串输入，并新增合同/发票 HTTP 回归测试。隔离浏览器已实际证明两类修正成功写入并保留来源证据。
- 修复 `test-verify-baseline.ps1` 在 Git 环境重定向负例后把原本不存在的 `GIT_DIR/GIT_WORK_TREE` 恢复为空值环境项的问题；现在显式移除原本不存在的变量，并在运行真实项目基线前拒绝任何泄漏的 Git override 名称。负向自测与当前基线重跑均 PASS，远程分支保护仍因无 remote 保持 `NOT_RUN`。
- 加固 `BASE-001` 本地 Git 治理门禁：分支名同时满足项目模型与 Git 原生 ref 规则；baseline 将仓库元数据绑定到指定项目根并拒绝 Git 环境重定向；remote URL 使用保留记录边界的本地配置读取，并对 Git 解析后的 fetch/push URL 再校验，拒绝凭据、控制字符、百分号编码、remote-helper、本地/UNC 路径、明文协议及不安全 `insteadOf/pushInsteadOf` 重写。新增真实临时项目根、内嵌换行、marker 伪造和配置重定向负例；全程不 fetch/push、不联网，`BASE-001` 仍为 `partial`、远程分支保护仍为 `NOT_RUN`。
- 修正 PostgreSQL 迁移集成测的 trigger 目录查询：`information_schema.triggers` 不包含 `TRUNCATE` trigger，现改用 `pg_trigger/pg_class/pg_namespace` 且排除内部 trigger；真实 PG16 已确认 `trg_audit_rules_immutable` 和 `trg_audit_rules_no_truncate` 都存在并拒绝 UPDATE/DELETE/TRUNCATE，无需改写历史 004 migration。
- 修正 P0 追踪状态：`BASE-004` 当时只完成严格 Settings 与 Provider 默认关闭的 pre-hash Policy 子集，因此从 `implemented` 调整为 `partial`。后续 CR-011-R4 虽已批准、同步并交付 Stage1 纯 contract/offline 证据，但 production startup、完整双 engine Gate B 和持久运行时仍未完成，`BASE-004` 继续保持 `partial`。
- 修正 002～005 migration 的 CHECK constraint token，避免 Alembic 命名约定产生 `ck_*_ck_*` 双前缀；离线 SQL 门禁现在逐 revision 断言最终名称并拒绝双前缀，ORM 与数据库名称保持一致。
- 修复 AI Policy 离线校验边界：所有 ValidationError 表示移除原始输入，JSON 重复 key 与布尔/整数 `Literal` 混用失败关闭，外部计费 Profile 强制 HTTPS；补齐 CR-002 已批准的错误分类/5xx retry 语义和 future model-repair 请求格预留，不读取 secret、不执行网络或睡眠。
- 修正 `TokenSession.refresh_token_hash` ORM 类型与数据库合同/迁移不一致的问题，统一为固定长度 `CHAR(64)`。
- 关闭 `CR-001-R2` 对应 GAP-010～011、013～014、016、025～027 与 `CR-002-R4` 对应 GAP-022、029～030、032～033 的跨文档合同冲突；保留“合同存在、代码实现、离线测试、固定 Provider、production”五类证据边界，不把 Request 同步记为任务或 AC 通过。
- 更正 `CR-002` 对现有 AI S1～S6 的治理表述：R4 只记录当前工作区离线边界，不以未批准草案授予实施、保留或扩展权限；任何待批决策仍须先完成审批与 `Request/` 同步。
- 复核关闭误报的 `GAP-004` 与 `GAP-007`：异步框架 TBD 已按其既定截止点由系统架构和开发计划一致落为 Celery 5 + Redis；本地隔离/占位扫描与生产真实扫描是 SRS 和架构共同规定的环境分级门禁。两项关闭均不冒充 `BASE-006`、文件扫描或生产验收已经完成。
- 更正 `BASE-001` 完成状态：本地治理门禁通过不能替代远程分支保护配置与演练；校验脚本改为输出 `BASELINE_LOCAL_VERIFY=PASS` 与显式 `BASELINE_TASK_STATUS=PARTIAL`，追踪矩阵在远程证据产生前保持 `partial`。
- 修复 PostgreSQL 配置可被 `host`、`hostaddr`、`port`、`dbname`、`service`、`servicefile` 查询参数覆盖已校验目标的问题；Settings 与 Alembic 迁移统一 fail-closed，并拒绝可能导致错误数据库迁移的 DSN。
- 修复 `chunk_overlap_chars >= chunk_max_chars` 仍可启动的问题，避免分块步长为零或负数。
- 修复 Celery 七个逻辑队列和 MinIO 七个隔离 Bucket 允许空白、不可见控制字符或组内重名的问题；无效配置现在启动失败，避免任务被静默路由到错误队列或未扫描文件与正式制品共用 Bucket。
- 为当前 `/health` OpenAPI 增加中文摘要、中文响应说明和合成示例，并增加 Router 禁止直接导入数据库、存储或模型层的 AST 静态回归。
- 修复破坏性迁移测试目标门禁可被 SQLAlchemy/libpq URL query 参数覆盖的问题；测试 DSN 现在拒绝全部 query 参数，并覆盖普通参数及 `host`、`hostaddr`、`dbname`、`service` 回归路径。
- 增加 Alembic revision 图离线门禁，要求单一 base/head、完整线性链且无分支、merge 或跨分支依赖；不固定 revision ID/数量，也不把图完整性误报为 55 张业务表或 `BASE-005` 完成。
- 加固迁移测试的破坏性门禁：要求严格的本机测试库命名、显式重置确认和数据库可丢弃标记；同时拒绝非 psycopg DSN，并为迁移连接增加 10 秒默认超时。
- 增加 Alembic 离线 SQL 回归门禁：当前输出只允许版本表与 `pgcrypto`/`btree_gist`/`citext` 三个扩展，并验证 DSN 不进入输出。
- 补充 Alembic 离线降级 SQL 回归：回退 `20260806_001` 只删除 revision 记录，不创建或删除共享扩展、业务表，也不输出 DSN。
- 修复前端把任意生产构建一律标记为“生产环境”的问题；环境标签现在仅根据显式 `VITE_APP_ENV` 展示，未知值显示“环境未配置”。
- 将高危公告范围内的传递开发依赖 `glob` 从 10.4.5 锁定到 10.5.0，并补充可选 Qdrant/OCR 密钥的占位符拒绝校验。
- 修复成功响应的数据解码失败被误报为网络故障的问题；现在返回保留 Trace ID 的 `API_RESPONSE_INVALID`。
- 修复前端统一请求层把合法 HTTP 204 当作无效 JSON 的问题；no-content 必须由调用方显式声明，JSON/204 双向契约漂移均返回保留 Trace ID 的 `API_RESPONSE_INVALID`。
- 固定前端请求层只能访问同源 `/api/v1`，在读取 Token 和发送请求前拒绝外部基址、路径遍历、反斜杠、编码路径字符及双重编码，避免 Authorization 进入错误目标或代理规范化歧义。
- 统一 FastAPI 参数校验 422 的运行时与 OpenAPI `ErrorResponse`；`APP_ENV=prod` 时关闭 `/openapi.json`、`/docs` 和 `/redoc`，非生产环境保留契约检查入口。
- 收紧测试资产校验：从正式计划动态提取 86 个 P0 任务，校验测试层、AC-001～AC-016 全覆盖、fixture 路径边界、哈希、全局 ID 唯一性和敏感字段，并避免在失败输出中回显值；账号、核心业务、安全负例与 RET 冒烟集进一步固定 Request 已明确的语义、两个版本轴、JSON 数值/Boolean 及二进制 manifest 整数/数组类型，并由 38 个隔离负例（含矩阵任务集、manifest 固定集合、安全 ID 漂移，以及同步必要 manifest 哈希的版本/语义/类型篡改）验证门禁。
- 修复 `.env.example` 因扩展名不在内容扫描列表而绕过高置信 secret 门禁的问题；新增隔离正/负向自测，失败只报告文件位置，不回显检测值。
- 取消 `Request/` 与 `Demo/` 的 secret 扫描整目录豁免，并补扫常见 `.pem/.key` 私钥文件；隔离哨兵覆盖文档、HTML 与私钥头，失败仍只报告相对路径而不回显匹配内容。
- 收紧 BASE-004 连接配置：数据库、Redis/Celery 与 HTTP 服务地址现在校验协议、主机、必要路径、控制字符和非零端口；HTTP base URL 拒绝内嵌 userinfo、query 与 fragment，数据库使用 SQLAlchemy 同源解析并拒绝歧义 userinfo；Settings 启动与类级校验错误的结构化表示不再保留原始输入，且 Qdrant 与 Embedding 向量维度不一致时启动失败。
- 修复 AI 运行时契约允许错误结果类型、非有限数值及二进制、映射、无序或一次性 iterable 被解释为 Embedding 向量、非枚举错误分类和非法超时值进入 Gateway 的问题；合法有序序列会复制为不可变 tuple，同时移除未知 Adapter 查找产生的敏感 `KeyError` 上下文。
- 为确定性二进制 fixture 生成器增加 Python 3.12.13 精确版本门禁，避免其他解释器产生不同字节后被误诊为资产损坏或误覆盖基线。
- 在 S4 之前曾将 JSON 数据集升级至 `1.3.0`，补齐 SRS 固定的 P-001-V2 制度名称与 5.1 条款；该中间版本随后由 `1.4.0` 取代，二进制现物已包含制度内容且契约版本与字节未因该次修正变化。
- 更正检索评测口径说明：SRS 同时保留 MVP/UAT 最低 50 条与正式 Hit@K 不少于 100 条的分级要求，恢复 `GAP-001` 待正式修订或 CR 裁决；`RET-001` 5 条仍仅是冒烟集。
- 修正 C-001 扫描合同、C-002 DOCX 和多文档 P-001 PDF 中与正式模拟数据不一致的主体/制度名称；生成器改为复用已受 Request 门禁保护的核心业务 JSON，二进制契约由 `1.0.0` 修订为 `1.0.1`，JSON 数据集仍为 `1.4.0`。
